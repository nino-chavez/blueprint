# Correction: "SE Docs Front Door" is real, and it's the missing archetype

**Status:** factual correction to `03-structure-comparison.md` and the Codex-round responses. No conclusions in `00`–`06` are re-litigated here.

## What was claimed

During the Codex review rounds, "SE Docs Front Door" (named twice by Codex as a required test consumer) was flagged as nonexistent — "no such consumer exists in `consumers.yml` or on disk" — and substituted out of the test set as possibly confabulated.

## What is true

The initiative exists: `~/Workspace/dev/wip/se-docs-frontdoor`, scaffolded 2026-07-09 by the blueprint-init stamper (Pattern A, tier 1, pilot_profile slug `commerce-se-solution-research`). Stage 1 closed the same day; Stage 2 (DESIGN.md) is the next pending stage. The registry check failed because the initiative was never added to `consumers.yml` — a registry gap, not a phantom. This is the same class of staleness `00-evidence-inventory.md` already recorded for ai-content-engine/atelier/tna, and one more datum for the registry-reconciliation ride-along.

## Why it matters to the contract work

Codex's reason for wanting it in the test set stands: it is a **configuration/pilot archetype** — the recommendation is configure-first (Claude Tag pilot), so there may never be a built application. Its portal-relevant shape under candidate B:

- Viewers with real evidence: the operator, the next agent, SE leadership (sponsorship decision — an issued brief already exists: `docs/se-team-brief.html`, published as a claude.ai artifact), and senior SEs recruited as champions.
- Nothing recurring to `run` until the pilot starts — a live test of the intrinsic-default cut (recover + boot only).
- Its Stage-1 reviewer log already shows Pattern-B gates misfiring on a Pattern-A initiative and 12 REPLACE_FOR_PROJECT portal markers pending — the same route-conformance-without-readers signal the inventory documented fleet-wide.

## Disposition

Candidate B's ratification package is unchanged; this note adds se-docs-frontdoor as a candidate specimen for the post-ratification wave (a config/pilot archetype walk alongside film-room and the self-app), and flags the `consumers.yml` addition for the registry reconciliation.
