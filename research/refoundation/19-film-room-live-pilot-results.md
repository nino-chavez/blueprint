---
canonical: false
status: ux-closure-observation-open-operator-feedback-deferred
date: 2026-07-23
depends_on:
  - research/refoundation/17-fleet-live-pilot-results.md
  - research/refoundation/film-room-ab/03-results.md
  - research/refoundation/18-final-recommendation.md
informs:
  - research/refoundation/20-film-room-method-validation.md
consumer_changed: true
consumer_branch: codex/film-room-blueprint-observation-acbc2d0
consumer_commit: 9c22428803a26c8b96c4095e1a2aed790aa08beb
consumer_product_base: acbc2d024e875143b4afc0bc593615c490419535
template_changed: false
---

# Film Room live native-pilot results

## Resumed observation boundary

Film Room Steering later completed a separate UX audit, redesign, comparable
benchmark, semantic-task, packaged-sample, and look-and-feel program while the
Blueprint contract and receipts were frozen. Product revision
`acbc2d024e875143b4afc0bc593615c490419535` was promoted to Film Room
`origin/main`. Decision 0019 then resumed Blueprint on isolated branch
`codex/film-room-blueprint-observation-acbc2d0`; semantic commit `9c22428`
changes only `blueprint-native.yml` and Decision 0019 relative to that exact
product.

The resumed contract preserves all historical receipts, pins new package,
native-window, founder, matrix, launch, signing, and second-operator claims to
`acbc2d0`, and validates with zero kernel errors or warnings. The exact app was
opened at Ingest and exposed its workflow and controls through the native
accessibility tree. Operator feedback and the uninterrupted real-event outcome
were then explicitly deferred. No native or founder outcome receipt is issued;
both claims remain open.

A newer independent Film Room Steering candidate, `463bf0b`, adds dynamic-state
work after `acbc2d0`. It changes no Blueprint file, inherits no observation,
and is not part of this pilot without separate authorization.

The method-level interpretation is recorded in
`20-film-room-method-validation.md`: the pilot validates evidence control and
candidate isolation, but not a complete end-to-end steering method.

## Verdict

Film Room is the second committed Blueprint native-contract consumer and the
first active packaged-product pilot. Its migration, compiler, kernel,
compatibility-discrepancy, isolation, and rollback gates pass. The misleading
first-run entry exposed by its first founder encounter was repaired, but the
next founder encounter exposed default-Review routing plus a dynamic-origin
native-dialog capability mismatch. The following repair reached real folder
selection but exposed a hidden manual `operator.json` prerequisite at Analyze.
All three failures are preserved. The operator-onboarding candidate reached
analysis but exposed a fourth wayfinding finding; the later UX-closure product
`acbc2d0` addresses that history under a separate Steering plan. Blueprint's
replacement founder and native-window claims for `acbc2d0` are open after
operator feedback was explicitly deferred. Assisted-beta readiness remains
contradicted.

That mixed result is the point of the refounded core:

- exact UX-closure contract and unsigned package inspection are
  `satisfied`;
- exact UX-closure native-window observation is `open`;
- the exact UX-closure full visual matrix is `contradicted`;
- exact UX-closure launch information is `contradicted`;
- exact UX-closure signing preflight is `contradicted`, and support
  designation is `open`;
- the historical `83d85fc`, `e1c21d9`, and `5cc4fd2` founder workflows remain
  `contradicted`, the exact `5496edd` founder workflow remains `open`, and the
  active exact `acbc2d0` founder workflow is also `open`; and
- second-operator first value is dependency-blocked, making assisted-beta
  readiness `contradicted`.

The pilot is a successful control-contract migration, not a claim that Film
Room is ready to distribute.

## Frozen source boundary

The migration follows the verified integration of the Film Room D-series
mainline and preserved release-readiness branch:

- product parent:
  `c39a7311bd0998ef9cf3126e6578c569ee44658f`;
- release-readiness parent:
  `3c4419fbd3d3616203800f5e2873863ba2df37e4`; and
- two-parent integration:
  `83d85fc92edcae2de323c5e7575cbd5acd0e385e`.

The integration suite passed release stage/runtime, first run, sample,
lifecycle, upgrade/rollback, startup isolation, rendered-data/CSP, visual
source contract, current D-series captures, adversarial state, accessibility
proxies, Playwright, window contract, byte-identical interchange golden
results, full golden results without baseline refresh, portal checks, Rust
checks, actual unsigned app build/bundle verification, package-child startup,
and secret scan.

Open integration evidence was retained: signing identity, exact merged
window-level observation, full current-candidate state-matrix recapture, final
launch information, signed-DMG cold install, assistive-technology evidence, and
second-operator evidence.

## Applied change

An in-repository worktree created branch `codex/film-room-native-pilot` from
exact `83d85fc`.

Commit `b227625` added exactly:

- `blueprint-native.yml`; and
- `decisions/0011-native-blueprint-pilot.md`.

Follow-up commit `5de85b0` changed one status line in Decision 0011 from
`proposed` to `accepted`. Encounter commit `0e89411` appended one contradictory
founder receipt to the native source after the maintainer could not enter the
real-footage path from first-run setup. Commit `0db4285` then pinned all
candidate-sensitive claims and the six existing receipts to exact product
revision `83d85fc`, preventing later product work from laundering them onto a
new candidate. The native-pilot branch retains its two-file rollback surface at
that exact remote tip.

The repair branch `codex/film-room-first-run-entry-repair` starts at `0db4285`.
Product commit `e1c21d9` implements Decision 0012 and changes the setup UI,
focused tests, and only the two affected current setup captures. Semantic
follow-up `c5ecf1e` changes only `blueprint-native.yml`: it preserves the six
historical `83d85fc` receipt blocks byte-for-byte, adds four exact-`e1c21d9`
tool receipts, and advances the active checkpoints to candidate-specific
claims. The remote branch resolves exactly to `c5ecf1e`; no pull request was
opened and Film Room `main`, the integration branch, and the native-pilot
branch remain unchanged.

The restarted encounter contradicted the exact `e1c21d9` founder claim:
the app launched in Review with the practice sample installed, and Ingest's
visible camera-card chooser produced no native folder panel or visible result.
Evidence-only commit `5ec5398` preserves that transition without changing
product bytes.

Decision 0013 is implemented on isolated branch
`codex/film-room-native-ingest-repair`. Exact product commit `5cc4fd2` defaults
normal launch to Ingest, scopes Tauri's dialog capability to dynamic
`127.0.0.1` ports, and surfaces recoverable picker failures. Semantic tip
`2a72970` changes only `blueprint-native.yml`, advances active claims to exact
`5cc4fd2`, and preserves every earlier receipt block byte-for-byte.

The next maintainer encounter proved that exact `5cc4fd2` opens in Ingest and
that the real macOS folder panel populates the chosen camera-card path. Analyze
then aborted because `config/operator.json` was missing and instructed manual
filesystem copying and JSON editing. Evidence-only commit `0cd7aef` preserves
both the native-window support receipt and the founder contradiction.

Decision 0014 is implemented on isolated branch
`codex/film-room-operator-config-onboarding`. Exact product commit `5496edd`
separates practice-library state from real-footage readiness, preserves the
Ingest draft through focused Settings recovery, provisions validated operator
choices atomically in-app, and accepts the retried Analyze request without
terminal/file-copy/manual-JSON intervention. Semantic tip `ada6cf6` changes
only `blueprint-native.yml` and preserves all 17 prior receipt blocks
byte-for-byte. In-progress encounter commit `a370647` then adds one exact
native-window support receipt after keyboard Back restored the preserved draft
and the retried Analyze request began running; it leaves founder operation open.

The native source is authoritative on the pilot branch for charter, native
claims, native receipts, checkpoints, and modules. `blueprint.yml` remains
frozen compatibility/configuration, `launch-contract.yml` remains the detailed
lifecycle contract, and `actor-output.yml` remains the imported legacy outcome
and output inventory.

## Derived state

| Checkpoint | State | Why |
|---|---|---|
| `native-migration-boundary-present` | satisfied | all five declared release-program artifacts exist |
| `legacy-actor-output-view` | contradicted | clean-clone actor-output gate has three path errors and three human pendings |
| `exact-unsigned-package-inspected` | satisfied | exact operator-onboarding entry, unsigned build/bundle, and packaged-process receipts are compatible with `5496edd` |
| `founder-operated-ready` | open | the exact `5496edd` founder claim has no human receipt yet; all three earlier product contradictions remain historical |
| `assisted-beta-ready` | contradicted | native window is satisfied, but launch information is open; the exact `5496edd` matrix is contradicted; signed package, support, and second-operator proof are incomplete |

The operator-onboarding shadow at exact product source version `5496edd` is
K1-valid with zero errors and zero warnings. It contains 44 claims, 22 native
receipts, compatibility receipts, and four conditional modules.

## First founder encounter

The maintainer opened the exact integrated unsigned application at first-run
setup and attempted to select real footage through **Use my footage**. They
reported that they could not click the footage-selection control.

Live UI and source inspection established the exact boundary:

- the element is rendered as a disabled-looking link while setup is incomplete;
- clicking it does not open a footage picker;
- it only focuses the archive setup field and writes a precondition message;
- the actual camera-card chooser is available later in Ingest after archive,
  finished-work, and camera-pattern setup are saved.

The label and card affordance therefore led the maintainer to expect immediate
footage selection at a point where the product offered only a setup
precondition. The operator did not reach Ingest or complete the real event.
Receipt `founder-live-workflow-blocked-at-footage-entry-20260723` correctly
contradicts `founder-live-workflow`; no agent coaching, package receipt, or
later explanation was converted into a pass.

## First repaired candidate

Decision 0012 turns the pre-setup affordance into an enabled native button
labeled **Set up for my footage**. Its helper copy distinguishes Archive and
finished-work locations from later camera-card selection. Activation scrolls
the setup section into view, visibly focuses Archive, and announces the next
action. After valid setup, the same control becomes **Use my footage** and
routes keyboard activation to Ingest.

The exact `e1c21d9` candidate passed both staged keyboard encounters, the
intended Playwright suite 13/13, release stage/runtime, first-run,
rendered-data/CSP, visual-system, window-contract, Rust-shell, Tauri-build,
manifest, and final bundle verification. Source, staged, and packaged
`setup.html` were byte-identical. The packaged child started from bundled
Python on a dynamic loopback port, reported first-run plus FFmpeg and FFprobe
healthy, and left no process or listener after termination.

This evidence satisfied the exact repaired entry, unsigned package, and
packaged-process claims but did not satisfy native-window observation. The
restarted maintainer encounter then found that normal launch still entered
Review and the Ingest chooser remained dead. That exact `e1c21d9` founder claim
is contradicted; neither its passing browser simulation nor its package proof
was reinterpreted as human success.

## Native-Ingest candidate

Read-only diagnosis after the second failure found two independent causes:

- the practice sample made normal launch default to Review; and
- Tauri's dialog capability still authorized fixed port `8766` after the native
  shell moved to dynamic loopback ports.

Exact product `5cc4fd2` makes normal launch enter Ingest while preserving
explicit sample-tour and Review deep links. Its capability accepts only
`127.0.0.1` with the shell-selected dynamic port. Rejected Ingest and Setup
pickers restore focus and show an actionable drag-or-paste fallback.

The exact rebuilt package passes the focused success/rejection encounter,
release stage, Tauri capability parsing, Rust checks, bundle inspection,
packaged-process doctor, and listener cleanup. It does not yet prove that the
real native folder panel opens, that the maintainer can select footage, or that
the real event completes. Those remain the next native and human boundaries.

## Third founder encounter

The exact `5cc4fd2` app opened directly in Ingest. The maintainer used the real
macOS folder panel, selected the camera-card folder, and Film Room populated the
path plus event name. This satisfies that product's native-window/folder-panel
claim.

After the maintainer clicked Analyze, Film Room stopped before analysis because
the packaged profile lacked `config/operator.json`. The message required
copying and manually editing an example JSON file in application support. That
developer-file intervention contradicts the founder workflow even though the
native dialog succeeded.

## Operator-onboarding candidate

Read-only diagnosis found that sample/library presence was incorrectly reused
as proof of real-footage setup. Decision 0014 makes operator readiness
independent: complete existing configuration remains authoritative; missing or
invalid setup returns structured recovery; the Ingest draft survives the
Settings round trip; and Settings validates and writes choices atomically.

Exact product `5496edd` passes absent, partial, malformed, unsafe, and
existing-valid configuration checks; sample-installed recovery; restored
Ingest drafts; in-app provisioning; browser and backend Analyze acceptance
beyond the setup gate; release/lifecycle/startup/security regressions; the
declared release build; and final bundle verification. It used disposable
synthetic paths and did not access maintainer footage. A fresh native encounter
is still required.

That encounter is now in progress. The maintainer completed in-app setup. The
bottom-of-page confirmation exposed no visible return action and said
**Choose your first move below** without an adjacent action, a real usability
finding. Keyboard Back nevertheless restored the preserved Ingest draft, and
Analyze began running. Therefore the exact native-window/recovery claim is
satisfied while founder workflow remains open pending the event's later
analysis, review, and export results.

## Compiler finding

The Film Room authoring pass exposed a semantic compiler gap before the
consumer write: `package-inspection` could require only the hidden shadow
evaluator, so a named release agent could not author its real inspection
receipt.

The root research compiler now permits an explicitly declared actor on a
package-inspection claim and binds the receipt to that actor's profile role. A
new positive fixture proves a named agent's package receipt compiles, validates,
and satisfies only its exact package claim/checkpoint. The full compact
self-test passes. This is research-layer hardening; `template/` and public CLI
behavior remain unchanged.

## Clean-clone compatibility discovery

The first pre-application shadow against the integration worktree reported two
actor-output path errors because that worktree still held an ignored local
`data/filmroom.db`. The truly isolated pilot worktree exposed the complete
condition:

- `data/filmroom.db` is absent;
- `data/reports/decision-aid.html` is absent;
- `data/reports` is absent; and
- three declared human validations remain pending.

All three paths live under the intentionally gitignored `data/` boundary.
Decision 0011 accepts this as a contradicted legacy compatibility checkpoint
for the methodology-only pilot. It does not manufacture the artifacts, weaken
the gate, or reinterpret the absence as package failure.

The future product decision is explicit: regenerate issued outputs before the
gate, represent issuance with a durable compatible receipt, or stop requiring
gitignored local paths to resolve from a clean checkout.

## Rollback proof

- `git diff --check` passed.
- The native-pilot branch at `0db4285` retains exactly its native contract and
  Decision 0011 relative to `83d85fc`.
- Decision 0012 and product commit `e1c21d9` are a separate visible repair
  boundary; semantic commits `c5ecf1e` and `5ec5398` respectively record tool
  evidence and the second founder contradiction.
- Decision 0013 and product commit `5cc4fd2` are a second separate repair
  boundary; semantic commits `2a72970` and `0cd7aef` respectively record tool
  evidence and the third founder result.
- Decision 0014 and product commit `5496edd` are a third separate repair
  boundary; semantic commits `ada6cf6` and `a370647` respectively record
  machine evidence and the in-progress native recovery receipt.
- The committed repaired native source byte-matches its Blueprint research
  proposal, and every tracked path outside `blueprint-native.yml` is
  byte-identical between `e1c21d9` and `c5ecf1e`.
- The Film Room repair worktree is tracked-clean; remaining build artifacts
  and caches are ignored and reproducible.
- Generated overlay, normalized K1, and shadow reports exist only under
  Blueprint's disposable research output.

Semantic rollback is an append-only decision that marks the native contract
dormant and restores legacy top-level authoring authority. Mechanical branch
removal remains an additional escape hatch. Neither path deletes native
evidence from history.

## Phase 4 disposition

Film Room's live native migration is complete. The first three Phase 4 founder
claims at `83d85fc`, `e1c21d9`, and `5cc4fd2` are contradicted and preserved.
The operator-onboarding repair remains at `5496edd` with its founder claim
open. The active observation boundary is now exact UX-closure product
`acbc2d0` / semantic tip `9c22428`; its native-window and founder claims are
open because operator feedback was deferred. Package inspection and
second-operator truth remain honest; actor-output discrepancy is explicitly
accepted.

No maintainer encounter is currently scheduled. If observation resumes, a
successful real-event run may satisfy only the exact candidate observed, and a
failure must be appended as a new contradiction. The independent `463bf0b`
product candidate is not an implicit continuation of `acbc2d0`. No older
package, browser, coached, simulated, or human proof can decide either newer
boundary.

Public experimental distribution remains ineligible even though two native
consumer migrations now exist. Prospective steering-recipe evidence,
longitudinal burden controls, upgrade/doctor/fleet support, and a declared
compatibility/support window still remain before an opt-in methodology wave.
