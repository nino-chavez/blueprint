# Decision 0019 — resume Blueprint observation on the UX-closure candidate

Status: accepted after explicit operator authorization of the Film Room main
promotion and Blueprint observation resumption.

## Context

Film Room Steering completed its UX audit, comparable benchmarks, semantic task
context, packaged-sample correction, and look-and-feel closure without changing
the Blueprint native contract. The exact candidate
`acbc2d024e875143b4afc0bc593615c490419535` was promoted to `origin/main` by a
verified non-force fast-forward from `c39a731`; its inherited
`blueprint-native.yml` remains byte-identical to evidence tip `a370647`.

The earlier founder encounters and receipts apply only to their named product
revisions. They cannot establish the outcome of this redesigned candidate.

## Decision

- Resume Blueprint observation on isolated branch
  `codex/film-room-blueprint-observation-acbc2d0` from exact product revision
  `acbc2d0`.
- Preserve every earlier claim and receipt as historical evidence.
- Advance active package, native-window, founder, visual-matrix, launch,
  signing, and second-operator checkpoints to new claims pinned to exact
  `acbc2d0`.
- Accept machine receipts only for boundaries actually rerun on this candidate.
  Automated/browser evidence may satisfy source, package, and packaged-process
  claims; it cannot satisfy native-window, founder, assistive-technology, or
  second-operator outcomes.
- Keep product, UX, launch, and benchmark bytes frozen on the Blueprint
  observation branch. Film Room Steering may continue later work only on
  separate branches, and later product revisions cannot inherit these
  receipts.

## Initial evidence boundary

The candidate has passing source, visual-system, staged release, Playwright,
portal, Rust, bundle, FFmpeg-source, packaged-child doctor, and listener
teardown evidence. Thirteen current captures exist, but required real dynamic
Ingest/Publish/Handoff states and parts of the historical matrix are not
current. Launch information remains blocked on final candidate notes/package
receipt and a real support contact/schedule. The package is ad-hoc/unsigned and
has no Developer ID identity.

A fresh native observation and uninterrupted founder real-event encounter are
required. VoiceOver, actual 200% zoom, cold second operator, signed/notarized
DMG, support authority, and public install identity remain separate gates.

## Rollback

Semantic rollback is append-only: mark the `acbc2d0` observation claims dormant
and restore the previous active claim set without deleting any receipt. The
mechanical observation-branch delta is limited to this decision and
`blueprint-native.yml`; product rollback remains owned by Film Room Steering.
