---
canonical: false
status: root-dogfood-and-cold-author-passed-live-write-pending
date: 2026-07-22
depends_on:
  - research/refoundation/14-refoundation-decision-and-migration-plan.md
  - research/refoundation/15-phase-0-hardening-results.md
template_changed: false
consumer_changed: false
---

# Root dogfood and live-pilot readiness

## Verdict

The compact research contract reproduces Blueprint's own root semantic shadow
exactly, and a context-isolated author passed the sealed usability exercise.
The first live-consumer package is prepared against a clean Fleet Observability
checkout; applying it requires the separately authorized external-repository
worktree and an honest operator receipt boundary.

No `template/`, consumer, release, or deployment file changed.

## Root dogfood result

The source later promoted to root `blueprint-native.yml` compiles the root charter, seven actor-output
outcome claims, two current tool claims, three checkpoints, and three
conditional modules. The generated comparison retains actor authority, exact
evidence contracts, receipt compatibility, and all claim/checkpoint states.

| Check | Result |
|---|---|
| K1 validity | pass |
| semantic projection | exact |
| derived states | exact |
| explicit root overlay | 116 nonblank lines |
| compact root source | 40 nonblank lines |
| reduction | 65.5% |

Decision 08 subsequently promoted the proven compact source as the root
governing contract. Legacy root files remain bounded compatibility and
configuration inputs; normalized K1 and reports remain disposable.

## Receipt correction result

K1 and compact authoring now implement append-only correction. The positive
replay retains an erroneous support receipt in history, retracts it through an
authorized disposition, and lets the same-claim corrected contradiction govern
current state. Negative fixtures reject missing correction authority and
cross-claim replacement.

## Cold-author readiness

`research/refoundation/cold-author/AUTHOR-PACKET.md` is a sealed, ordinary
single-operator exercise. Its scorer checks compilation, K1 validity, exact
claim/checkpoint states, receipt compatibility, evidence-boundary explanation,
authoring size, elapsed time, questions, assumptions, and creator
interventions.

The harness first passed 34/34 checks against a temporary known-good submission;
that submission was deleted and the score reset. A separate context with no
conversation history then authored a new 55-line contract and passed 34/34 in
two attempts and 56 recorded seconds. It asked zero questions and recorded zero
methodology-creator interventions. The final explanation correctly kept the
scheduled claim `unobservable` rather than promoting manual execution.

## Fleet pilot readiness

The Fleet checkout is clean and aligned with its remote at `2989aa4`. The
read-only compact shadow is K1-valid and finds artifact claims satisfied,
scheduled operation open, and the current doctor claim contradicted.

The proposed native source narrows checkpoints so implementation presence,
legacy doctor conformance, and actual scheduled operation remain independently
visible. The proposed decision explains why the inherited portal and scanner
findings do not prove or refute scheduled delivery and refuses to invent an
operator receipt.

The cold-author gate is cleared. Applying the package now requires the
authorized isolated Fleet worktree/branch and an actual operator observation
for the scheduled-delivery claim; absent that observation, the checkpoint must
remain open rather than delaying the structural migration or inventing support.
