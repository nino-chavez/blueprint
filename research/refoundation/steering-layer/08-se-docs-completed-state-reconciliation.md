---
canonical: false
status: reconciled-root-retrospective-evidence
date: 2026-07-27
scope: root research only
template_changed: false
consumer_changed: false
prospective_external_threshold_met: false
reconciles:
  - research/refoundation/steering-layer/06-se-docs-session-pilot-preregistration.md
  - research/refoundation/steering-layer/07-se-docs-session-pilot-results.md
---

# SE Docs completed-state reconciliation

## Verdict

Integrate the bounded selector correction, fixtures, and K1 transition specimen
into the root-only steering research line. Do not carry the pilot's claim that
SE Docs chose the wrong variant at founding into Blueprint's factual account.

The pilot froze consumer history through `bb4c459` while the re-founding session
was still in progress. The completed consumer at exact revision `95f6191`
records a narrower sequence:

- greenfield was defensible when the believed deliverable was a configured
  channel;
- sponsor evidence later changed the problem, rather than merely adding
  evidence to the founding problem;
- the initiative then moved from greenfield to research by hand; and
- the authored research artifact would have been overwritten by a real
  research re-stamp.

The intake fixture remains useful as a synthetic authority-boundary test for
the experimental evaluator. It is not evidence that charter-first intake would
have improved the actual SE Docs founding sequence. The re-charter fixture and
K1 specimen remain faithful retrospective representations of the completed
transition.

Nothing here authorizes a public charter-first stage, the steering evaluator,
or native semantics. Decision 08's distribution ceiling and Decision 09's
promotion hold remain controlling.

## Evidence boundary

The preregistered pilot used:

- the founding JSONL;
- an in-progress re-founding JSONL snapshot through
  `2026-07-27T20:47:59.807Z`; and
- consumer history only through `bb4c459`.

The separate consumer run subsequently completed at `95f6191`, with the
relevant authored sequence in `5289a86..95f6191`. This reconciliation inspected
that exact Git state read-only:

- `blueprint.yml` says, “Greenfield was defensible at founding,” then explains
  the later change to research;
- `METHODOLOGY-AMENDMENTS.md` classifies the case as “right-variant,
  changed-problem-mid-flight,” explicitly distinguishing it from known
  wrong-variant-at-init cases;
- `HANDOFF.md` records the changed problem, the hand-applied transition, zero
  mechanical reviewer blocks, and the still-unrun judgment gates; and
- `docs/decision-memo.md` is a draft that has not been sent or accepted.

The final state therefore proves neither a sponsor outcome nor prospective
steering benefit. It supplies exact artifact and history evidence for a
completed re-charter and for the non-overwriting transition defect.

The integrated K1 specimen's supporting receipt is a new, read-only Blueprint
inspection of exact Git object `eac9ff6`, completed at
`2026-07-27T22:14:51Z`. It is not represented as a historical consumer receipt,
a sponsor receipt, or prospective validation. The earlier pilot specimen
timestamp preceded the source commit and is superseded by this auditable
follow-up observation.

## Reconciled findings

| Finding | Completed-state evidence | Disposition |
|---|---|---|
| Founding greenfield was the wrong variant | The final manifest calls it defensible for the believed configured-channel deliverable; the amendment calls this a right-variant, changed-problem case | Retire this attribution as a consumer fact; keep intake only as a synthetic counterfactual test |
| Sponsor input required a re-charter | The problem statement supersedes the founding frame; old work is retained and downstream actions are explicitly invalidated | Supported; K1 already represents the transition without a new primitive |
| Ready decisions were mislabeled as machine work by the root evaluator | The frozen intake fixture reproduced the ordering defect, and moving ready-decision selection ahead of blocked-machine fallback fixes only that recipe choice | Integrate into root research with adversarial preservation tests |
| Public reviewer/parser/documentation defects were exposed | Each defect is mechanically reproducible without steering semantics | Handle on the separate public correctness branch, each with a regression test |
| Re-stamping to research risks authored work | `5289a86` records that `research/personas-and-jtbd.md` would be copied unconditionally | Treat as a consequential transition capability, separately preregistered and implemented; do not patch the stamper destructively |
| A universal problem-statement stage is required | The evidence is a re-charter of an existing initiative, not a universal founding sequence | Reject; charter revision and disposition are the general mechanism |

## Root-only integration

The warranted integration is bounded to
`research/refoundation/steering-layer/` and
`research/refoundation/k1/specimens/`:

- preserve the pilot preregistration and results as the historical snapshot;
- add this completed-state qualification;
- retain the three SE Docs fixtures as retrospective/synthetic boundary cases;
- retain the decision-before-blocked-machine selector correction;
- retain the re-charter K1 specimen; and
- keep all generated outputs disposable.

The selector still cannot authorize a frame, create evidence, issue a receipt,
or mutate a consumer. No `template/`, public CLI, doctor, fleet, upgrade, or
stamped-default surface changes on this branch.

## Next prospective external pilot

A fresh, contrasting external initiative must test charter-first intake before
any scaffold is created. The boundary must be preregistered before the run and
must include:

1. the governed outcome choices and starting claims in reader-compatible
   language;
2. journey/revision identity, expected recipe transitions, and the authority
   route for ambiguity or changed intent;
3. an operator-touch budget, incident-cluster and stop thresholds, a rollback
   boundary for the opt-in research contract, and a support owner;
4. a cold author/interpreter who is not the methodology creator;
5. a declared comparison showing whether the selected recipe improved the real
   work sequence, avoided irrelevant work, and kept revision history legible;
   and
6. an actual operator/reader encounter whose receipt remains distinct from
   mechanical or synthetic evidence.

Source roles are required only if that pilot also opts into the separate
source-influence/holdout experiment. Cleanup inventories and authored-artifact
preservation are required only if it also exercises the separately
preregistered variant-transition capability. Neither contract becomes
universal charter-first ceremony.

Passing that pilot would trigger a fresh promotion decision; it would not
automatically publish charter-first intake, steering, or native semantics.
Decision 08 still requires contrasting live consumers, continued cold-author
success, upgrade/doctor/fleet support, and a documented support/rollback
window. Decision 09 still requires compact authoring, K1-aligned authority,
compatibility, and exercised public support/rollback behavior.

## Rejected expansions

- no universal problem-statement stage;
- no SE Docs-specific variant;
- no portal requirement; and
- no model allowlist.

## Integrated verification

- current version-2 steering suite: 163/163 assertions;
- K1 research suite: 19/19 expected verdicts, including the exact SE Docs
  re-charter specimen;
- source-influence preservation suite: 26/26 assertions;
- current Blueprint core and stamp smoke: pass; and
- root-research diff and whitespace checks: pass.

The 148/148 count in `07-se-docs-session-pilot-results.md` remains the exact
historical result on the older pilot branch. It is not the current integrated
version-2 suite count.

## Support and rollback

This branch changes root research only. Rollback is a normal revert of the
selector/specimen integration and this reconciliation; it requires no
consumer translation, evidence deletion, package release, or fleet sync.
SE Docs Front Door remains unchanged, and its draft memo remains unsent.
