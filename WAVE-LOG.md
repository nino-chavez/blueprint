# Blueprint wave log

Methodology changelog. Moved out of `CLAUDE.md` on 2026-05-29 to keep the agent charter scannable; full history preserved here verbatim. The `## Wave log` heading below is the parse anchor for `template/tools/wave-digest/digest.mjs` — run it with `--source=WAVE-LOG.md --since=N` / `--keyword=<regex>` to filter.

## Wave log

Methodology changes ship in waves so consumers can sync coherently:

- Wave 2 (`0731ccb`) — pilot profile lock + confident preview + monetization axis
- Wave 3 (`ecedef3`) — Pattern B chrome canonical split + restamp + reviewer
- Wave 4 (`86baf7c`) — manifest-driven Pattern B chrome + Rally HQ leak excision
- Wave 5 (`53fe1f0`) — 6 chrome/docs bug fixes from blog consumer
- Wave 6 (`0cc1f9b` → `b9ecc90`, 4 fixes) — amendments from blueprint-redesign consumer: repo-role declaration convention, `<title>` suffix dedupe, prose-doc audit for stale `rally-*` storage keys, manifest-driven `prep-deploy.sh` + ADR-0003
- Wave 7 (`4a4aa7e` → `1072308`, 4 fixes) — amendments from website-nc-v3 consumer (two failures, one root cause): ADR-0004 JTBD continuity + forge-pipeline provenance, `research-completeness-reviewer` JTBD-per-persona extension, new `prescription-jtbd-traceability-reviewer` (Stage 2→3), new `prototype-forge-provenance-reviewer` (Stage 3 completion)
- Wave 8 (`1c11968`) — amendment from blueprint-redesign consumer (dogfood-v1): design-discipline track added to METHODOLOGY.md — Stage 1 design-discovery sub-track (4 required brownfield artifacts + greenfield variant), Stage 2 design-system dictionary section (L0-L4 atomic-design), Stage 4 solo-initiative degrade-path. Three independent dogfoods (rally-hq, signal-dispatch blog, blueprint-redesign) converged on the L4-absent finding.
- Wave 9 (`ae8c574`) — amendment from blueprint-redesign consumer: multi-theme registry as canonical chrome. `shared.css` gains 4 `[data-theme]` blocks (slate default + coral + forest + minimal); `theme-switcher.js` promoted from initiative-side to canonical chrome; `stamp.mjs PATTERN_B_CHROME_FILES` extended. Operator reframe during dogfood: "implement all four as options to apply" turned L0 brand decisions from per-consumer pickup to methodology-owned registry.
- Wave 10 (`0a3b28a`) — reference artifacts from blueprint-redesign: 5 EXAMPLE/template files in `template/methodology/design/` (audit template + surface audit example + design system example + Stage 4 fact-check example) + `template/methodology/voice/` (voice rules example), plus 2 README files documenting consumer use.
- Wave 11 (`0af1a35`) — completes multi-theme integration: `blueprint.yml` schema gains `prototype.theme` field (slate | coral | forest | minimal | custom; default slate); `stamp.mjs` accepts `--theme` CLI flag + substitutes `<html lang="en">` → `<html lang="en" data-theme="{theme}">` so the default applies before JS loads.
- Wave 12 (`5990226`) — brand kit + wedge-page template fold from blueprint-redesign: `template/brand/blueprint-brand-kit.json` (forge-brand-compatible canonical kit for Blueprint-the-product), `template/portal/templates/wedge-page.html` (canonical L4 template for comparison-toggle archetypes), 2 README files.
- Wave 13 (`59ab781`) — wave-log entries for 8-12 + freeze-lift acknowledgment in this CLAUDE.md.
- Wave 14 (`9c91d36`) — amendment from rally-hq consumer (commit `1aa3c35` in apps/rally-hq, 3 amendments captured 2026-05-26): `--mode=audit-chrome` read-only diff command + `--portal-dir` CLI flag + `blueprint.yml prototype.portal_dir` schema field + `restamp-chrome` overwrite gate (refuses to run on diverged chrome without `--accept-overwrite=`). Closes RH §1 gaps 1+2 (silent-destruction risk against 4 brownfield consumers). Validated against rally-hq's portal; gate correctly refused destructive restamp.
- Wave 15 (`b7b3495`) — extends wave 14 with git-history per-file classification: each diverged chrome file labeled LAG (matches an older canonical commit @ SHA + date) or CUSTOMIZATION-OR-ROT (no canonical match in last 30 commits). Audit output produces actionable restamp command for LAG files + manual-review list for CUSTOMIZATION-OR-ROT files. Closes RH §1 gap 3. Validated against rally-hq: 5 LAG / 2 CUSTOMIZATION-OR-ROT correctly identified.
- Wave 16 — `prototype.brand_axes` schema in `blueprint.yml` for multi-tenant / multi-axis brand models (closes RH §3 — wave 9-11 multi-theme registry assumed single-axis consumers but rally-hq has chrome × per-tournament-accent). Documentation-only this wave; future waves wire CLI flags + chrome-canonical-reviewer to interpret the axis declarations.
- Wave 17 (`74fa02f`) — four promo-initiative amendments promoted as a single bundled wave. **Commit message labels itself "wave 14" — that label is a numbering bug; wave 14 was already taken by `9c91d36` (audit-chrome). The commit belongs at wave 17 in the wave log.** Amendments: (1) scaffold contamination fixes — `stamp.mjs` blanks the subs-initiative archaeology `WORKER_URL` at stamp time + upgrades `BANNER_FILES` text from warning → block; `ArchaeologyChat.tsx` renders a disabled "substrate not configured" button when `WORKER_URL === ''`; `strategy/delivery-fork.astro` + `strategy/index.astro` replaced with project-neutral skeletons. (2) `template/docs/methodology/current-state-research-prompt.md` — "what needs to be true" research frame. (3) `template/docs/methodology/architect-challenge-pattern.md` — expression-surface comparison pattern. (4) `template/docs/methodology/reference-sessions.md` — promo-initiative dogfood session as canonical first-run reference entry. Authored on a parallel session/machine in May 2026 (commit `74fa02f`, dated 2026-05-26 23:05); pushed to origin before the 2026-05-27 session could promote the same amendments. Waves 18-22 below extend wave 17 with the gates / reviewer enforcement / schema fields / methodology-stage placement that wave 17 stopped short of.
- Wave 18 — Runtime `ARCHAEOLOGY_READY` gate added to `template/apps/portal/src/layouts/Layout.astro`. Defense-in-depth above wave 17's stamp-time `WORKER_URL` blanking + wave 17's component-side empty-URL guard. Reason a third layer is needed: wave 17 ensures the widget is safe when present and when `WORKER_URL` is empty, but does not give the operator a single-flip gate to control whether the widget mounts at all (e.g., during a phase where the substrate is deployed but the operator does not yet want the widget visible to stakeholders). The flag pattern + conditional dynamic import keeps the bundle clean when disabled. Documented preconditions to flip the flag are inline in the file. Authored in the same multi-session collision as wave 17.
- Wave 19 — Enforces what wave 17 declared. Wave 17's `stamp.mjs` upgraded the `REPLACE_FOR_PROJECT` banner text from "warning" to "block" but the portal-pattern-{a,b}-conformance-reviewers never grep'd for the banner — declaration without enforcement. Adds new check section to both reviewers: grep `REPLACE_FOR_PROJECT` across the portal source; `>0` matches BLOCKS at the Stage 3 → Stage 4 gate. Each reviewer's STATUS report block gains `REPLACE_FOR_PROJECT_BANNERS` line. Without wave 19, the promo-initiative failure mode (scaffold pages with subs-initiative business content shipping to live stakeholder deploy) could recur — the banner text says "block" but nothing blocks.
- Wave 20 — Enforcement layer for wave 17's research-prompt + architect-challenge docs. Wave 17 added `template/docs/methodology/current-state-research-prompt.md` + `template/docs/methodology/architect-challenge-pattern.md` but did not (a) introduce a schema field to declare which initiatives need the platform-feature treatment or (b) add a reviewer step to enforce the architect-challenge artifact exists at Stage 1 → 2. This wave closes both: new top-level `initiative_type: consumer-app | platform-feature` field in `blueprint.yml` (default consumer-app, backward-compatible — existing consumers without the field are implicitly consumer-app); `research-completeness-reviewer` gains step 8 (gated on `initiative_type: platform-feature`) that verifies `research/current-state/architectural-options.md` exists, addresses all 5 dimensions from `architect-challenge-pattern.md` (expression surface / validation surface / authoring surface / escape hatch / evaluation cost), reaches an explicit choice with reasoning, AND cites confirmed platform capabilities per the "what needs to be true" prerequisite. Fail codes: `ARCHITECT_CHALLENGE_MISSING` / `_INCOMPLETE` / `_NO_ARGUMENT` / `_UNCITED`. Step references wave 17's pattern doc directly so the two waves compose cleanly.
- Wave 21 — Stage S-A (Archaeology Substrate) added to `METHODOLOGY.md` as a new "Optional Capability Stages" section. Documents the 4-step lifecycle (scaffold → ingest → wire URL → flip gate), per-step Done-criteria, why-it's-a-stage rationale, and how waves 17 + 18 enforce correct sequencing across three defensive layers (stamp-time `WORKER_URL` blanking + component-time empty-URL guard + operator-time `ARCHAEOLOGY_READY` flag). Pipeline diagram updated to surface capability stages running alongside the main pipeline. Cross-Cutting Disciplines section gains a closing note distinguishing the substrate (discrete lifecycle, stamp-gated UI) from ambient disciplines (always-on, no UI surface). Resolves the promo-initiative question "when does the agent ask substrate get implemented correctly? if that is a later stage then we should suppress the agent ui in the portal until then as a stage gate" — yes it's a later stage; waves 17-18 are the three-layer suppression; wave 21 documents the lifecycle so the operator flag flip is earned, not arbitrary.
- Wave 22 — Prototype-vs-production traceability sweep pattern promoted from rally-hq. Synthesizes the v1 amendment (rally-hq commit `7629d2f`, 2026-05-26 — "Prescription encodes known drift; intent traceability needs its own recurring sweep") + the 17-meta fan-out (rally-hq commit `a4383a6`, `blueprint/audits/traceability-sweep-2026-05-26.md` — 152 items, 7 HIGH bugs, 10 recipe-friction notes + 5 tooling proposals). New canonical doc at `docs/case-studies/prototype-vs-production-traceability-sweep.md` — 4-link chain walk (research → meta → prototype HTML → production code), 5-verdict taxonomy (bug / refinement / open-question / already-reconciled / **structural-divergence** [new from friction note #3]), 2 pre-flight steps (destination check from #2, research-gated sort from #6), 9 meta-schema extensions (`currentState.expected_size`/`expected_section_count`, `decomposedInto[]`, `strategy.shipped.route`/`verified_date`, `affected_handlers[]`, `strategy.answer`, `currentState.findings_refs[]`/`findings_status`), per-sweep workflow + output shape + 5 tooling proposals. Added to `METHODOLOGY.md` Cross-Cutting Disciplines table with activation threshold "post-major-arc closure OR quarterly baseline" (cadence-gated, not flag-gated). Synthesizes from durable artifacts (Option 2 per session decision) rather than waiting for rally-hq AMENDMENTS v1.1 — the friction notes are already crisp and committed at `a4383a6`. Closes the rally-hq amendment promotion; rally-hq's local AMENDMENTS file remains at v1 (`7629d2f`) but the methodology now carries v1.1.
- Wave 23 — `METHODOLOGY.md` gains a `## State of the methodology` opening section between the title block and `## Overview`. Establishes dated-stance epistemic state at the front door — names the six consumer initiatives, points to the twenty-two waves logged here, routes readers who find gaps to `template/docs/methodology/methodology-amendments-convention.md`, and explicitly flags that body sections stay declarative because that's the right register for operator-mode while the methodology itself is still being shaped. Closes a framing asymmetry surfaced during the promo-initiative runbook exercise (2026-05-27): the wave log + reviewer prompts already carry contributor-mode "still figuring out which gates matter and which are noise" voice; `METHODOLOGY.md`'s top-level framing did not, which produced an adopt-or-reject reading register where a contributor-invitation register was warranted. The fix is asymmetric — front matter inherits the wave-log voice; the rest of the doc stays declarative because operator-mode wants the gate spec, not the meta. Doc-only change; no reviewer, schema, or stamper impact. No consumer-sync needed.
- Wave 24 — Generative output formats for the prototype-vs-production traceability sweep (wave 22) + subs-initiative `tools/state-derive/` mechanical-evidence pattern promoted as canonical companion. Extends `docs/case-studies/prototype-vs-production-traceability-sweep.md` with new section "Generative output formats (wave 24)" covering: (1) per-verdict draft artifacts — each verdict produces a draft amendment instead of a diagnostic entry (`bug` → draft P-item, `structural-divergence` → draft meta patch, `already-reconciled` → draft `strategy.shipped.verified_date`, etc.); (2) mechanical-evidence derivation — TypeScript capability catalogs + check primitives (`file_exists`, `grep_count`, `schema_has_table`, `commit_message_grep`) produce `_state.json` updated per-commit, narrowing what sweep agents must read; (3) activation order for consumers running both layers (state-derive on every CI build; sweep per cadence; generative drafts from both). Promotion driven by 2026-05-27 consumer-evidence audit (see `docs/_archive/2026-05-27-loom-inspiration-candidates.md` + `docs/_archive/2026-05-27-loom-market-comparison.md`): three distinct drift surfaces across two consumers — rally-hq wave-22 sweep + rally-hq waves-14-16 audit-chrome (LAG/CUSTOMIZATION-OR-ROT classification is generative-output-shaped) + subs-initiative `tools/state-derive/` (already-built reference implementation predating the sweep). Closes inspiration-candidate C2. Reference implementation: the lift to `template/tools/state-derive/` already happened (commit `780932b`); the template ships the engine (check primitives + catalog loader + renderers) with subs-initiative's capability catalog stripped out. Consumers add their own `catalog/*.ts` files; subs-initiative's `tools/state-derive/catalog/` remains as the originating-consumer reference. [Doc-claim correction: wave 24's original framing said the lift was premature; subsequent verification found commit `780932b` had already landed it. Doc + CLAUDE entry corrected; wave 24 commit `bca76d5` left as-is.] Doc-only change in this repo — extends one existing doc + adds wave-log entry + cross-references in the candidates/market-comparison docs. No reviewer, schema, or stamper impact. Also fixed in passing: wave-22 doc's status block and `structural-divergence` row both referenced "wave 20" — corrected to "wave 22" per the actual wave that promoted the pattern (commit `fc184b2`). Freeze acknowledgment: rally-hq + subs-initiative both at rest; no consumer-side methodology edits in flight. No consumer-sync needed (additive, opt-in per consumer; existing wave-22 sweep continues unchanged).
- Wave 25 — Operator-handoff pattern + template promoted from converging consumer evidence. New canonical doc `docs/patterns/operator-handoff-pattern.md` distills the common shape (state header → what's-live → what's-done → what's-pending → sequencing → local refs/secrets → out-of-scope) from rally-hq's `blueprint/HANDOFF-blueprint-template-gaps.md` (2026-05-25, cross-repo dispatch — 5 template additions handed to a fresh tools/blueprint session for execution) + subs-initiative' `HANDOFF.md` (2026-05-06, session-restart — Hive bootstrap continuation context). Both consumers independently produced docs with overlapping structure within the same week, signaling the shape is general enough to ship as a template rather than re-derive per initiative. New template subdir at `template/methodology/handoff/` with `README.md` + `handoff-template.md` matching the existing `template/methodology/{design,voice}/` convention. Scope refinement vs the original C1 candidates-doc framing: evidence supports OPERATOR-CONTEXT handoffs broadly (stage transitions, session restarts, cross-repo dispatches), not STAGE-TRANSITION handoffs specifically; doc acknowledges all three transition types as the trigger set. No reviewer gate this wave — demand is operator-driven (operator decides when handoff cost > template-fill cost), and the audit surfaced two *successful* handoffs (no failure-mode evidence yet to justify a forced gate). A future amendment can add a Stage N→N+1 reviewer when ≥2 consumers report stage-transition failures attributable to missing handoffs. Closes inspiration-candidate C1 from `docs/_archive/2026-05-27-loom-inspiration-candidates.md`. Doc + template additions; no reviewer, schema, or stamper impact. Freeze acknowledgment: rally-hq + subs-initiative both at rest; no consumer-side methodology edits in flight. No consumer-sync needed (additive; existing consumers can copy the template at their own cadence, or keep their existing hand-written HANDOFF docs as-is since both pre-date the template and remain reference examples per the README). Wave-log ordering correction in passing: wave 25 was originally inserted above wave 24, breaking ascending-order convention; this edit restores 24 → 25 → 26 order.
- Wave 26 — Wave-log digest pattern + tool promoted from wave-23 self-evidence (methodology grew past one-sitting reading) + rally-hq's 738-line onboarding burden (blueprint/CLAUDE.md 332 + STATE.md 406). subs-initiative' CLAUDE.md is only 48 lines and does NOT exhibit C3 friction — promotion accepted on single-consumer + methodology-as-consumer evidence per the candidates-doc "borderline" verdict. Wave shape (intentionally minimal): new canonical doc `docs/patterns/wave-log-digest-pattern.md` documents the convention (the wave log is the load-bearing onboarding text; the digest tool is a filter over it). New tool subdir `template/tools/wave-digest/` with `README.md` + `digest.mjs` (small Node script, no dependencies, ~60 lines) — parses `## Wave log` section of any CLAUDE.md, filters by `--since=N` and/or `--keyword=<regex>`, emits markdown or plain text. Tool scope deliberately narrow: deterministic filter only; no LLM, no semantic understanding, no "what's load-bearing for surface X" feature. Composes with wave 23's `## State of the methodology` front matter (front matter sets reading register; tool surfaces relevant subset). Defers richer features (semantic summary, cross-repo digest, wave-impact diff) to second-consumer evidence under the same gating discipline waves 24-25 applied. Closes inspiration-candidate C3 from `docs/_archive/2026-05-27-loom-inspiration-candidates.md`. Doc + small tool additions; no reviewer, schema, or stamper impact. Freeze acknowledgment: rally-hq + subs-initiative both at rest; no consumer-side methodology edits in flight. No consumer-sync needed (tool is opt-in run; doesn't change any existing artifact). With C1+C2+C3 promoted across waves 24-26, only C5 remains as a borderline candidate from the 2026-05-27 audit (rally-hq worktree-isolation entry is single-consumer evidence; needs a second signal or fresh dogfood); C4 (amendment auto-classification) remains correctly deferred until volume materializes.
- Wave 29 — Skill-categories pattern (STAGE vs ROUTINE) promoted from two-example evidence (`/blueprint-handoff` + `/blueprint-amendment`). New routine skill `template/.claude/skills/blueprint/amendment.md` files a METHODOLOGY-AMENDMENTS entry via wave 27's 4-bucket decision tree; auto-initializes the amendments file from `template/methodology/amendments/METHODOLOGY-AMENDMENTS.template.md` if absent. Pattern doc `docs/patterns/skill-categories-pattern.md` promoted from `pattern-pending-validation` to `canonical: true`; names two skill categories (STAGE — invoked once per stage transition, ~6× per initiative, clear `I'm at Stage N` trigger from `blueprint.yml`; ROUTINE — invoked many times per initiative at unpredictable moments, no single state-field trigger), the three load-bearing differences (trigger discoverability, invocation count, cross-skill composition), and the two skill-discovery mechanisms that close the routine-skill discoverability gap (cross-skill invocation from existing pipeline skills; SessionStart hook hints per session shape). Authoring guidance for future routine skills: derive state mechanically → ask operator for judgment that cannot be derived → compose → write. The "what cannot be derived" line is load-bearing — routine skills that try to automate operator judgment produce wrong output. Existing 6-skill set (research / prototype / docs / validate / deploy / triage) reclassified as the STAGE category; no breaking change to existing skills. Inventory after wave 29: 6 STAGE + 2 ROUTINE = 8 total. Routine candidates explicitly deferred per their own promotion-evidence thresholds: `/blueprint-sweep` (await consumer report on two-layer recipe value), `/blueprint-dispatch` (await second parallel-dispatch failure mode), `/blueprint-archaeology-ready` (await first end-to-end Stage S-A deployment). Trigger: the 2026-05-27 operator meta-question *"are there skills that should ship with blueprint, like a skill that knows to create and maintain a handoff doc? think through this question across all of blueprint"* — revealed the existing skill set was incomplete by construction (modeled pipeline-stage advancement only, not convention-maintenance). Doc + skill additions; no reviewer, schema, or stamper impact. Freeze acknowledgment: all four locally-accessible consumers at rest; no consumer-side methodology edits in flight. No consumer-sync needed (additive; consumers adopt either or both routine skills at their own cadence; existing STAGE skills unchanged).
- Wave 28 — Multi-operator collaboration pattern promoted from three-consumer evidence across three distinct failure modes. New canonical doc `docs/patterns/multi-operator-collab-pattern.md` names the three sub-patterns (attribution-loss / cross-context confusion / parallel-work workaround) plus the adjacent peer-canonical-contamination recovery-path failure, documents the deferred architectural shape (inline operator annotations on portal sections → structured bubble-up to AMENDMENTS via wave 27's 4-bucket taxonomy), and lists three lightweight conventions consumers can adopt today (per-repo role declaration, worktree isolation per parallel agent, no-workaround success criterion). Evidence: rally-hq `0c074d5` commit-attribution loss + blueprint-redesign wrong-directory amendment + portal/CONVENTIONS.md peer-canonical-contamination + blog pilot success-criterion #3 explicitly naming parallel-work failure mode. Three consumers across three distinct sub-patterns; promotion threshold exceeded. **Doc-only wave — substrate (annotation surface + routing + identity layer) deferred to a `platform-feature` dogfood per wave 20 because the build expands Blueprint into auth/identity/persistence scope which is real-product-scope, not methodology-scope.** Three named wave-promotion criteria for the substrate build: (a) a wave-20 platform-feature dogfood names this as its candidate, OR (b) ≥1 coordination failure attributable to the missing substrate that the three lightweight conventions did NOT prevent, OR (c) ≥2 consumers begin maintaining bespoke annotation surfaces (issue trackers, side files, Slack threads). None met today. Closes inspiration-candidate C5 from `docs/_archive/2026-05-27-loom-inspiration-candidates.md`. Promoted via `docs/_archive/2026-05-27-extended-audit-findings.md`. No reviewer, schema, or stamper impact this wave. Freeze acknowledgment: all four locally-accessible consumers at rest; no consumer-side methodology edits in flight. No consumer-sync needed (lightweight conventions section formalizes practices already in use across all four consumers; existing CLAUDE.md role-declarations + global worktree-isolation rule remain authoritative). **State after waves 24-28**: all 5 inspiration candidates from `docs/_archive/2026-05-27-loom-inspiration-candidates.md` now closed (C1→wave 25, C2→wave 24, C3→wave 26, C4→wave 27, C5→wave 28). Extended audit also surfaced 7 NEW pattern candidates (N1-N7 in `docs/_archive/2026-05-27-extended-audit-findings.md`) that remain in watch-and-promote status with per-candidate promotion-bars; most are single-consumer evidence deliberately deferred to await cross-consumer signal.
- Wave 27 — Amendment-classification pattern promoted from extended-audit cross-consumer evidence. New canonical doc `docs/patterns/amendment-classification-pattern.md` documents a 4-bucket taxonomy (consumer-local / template / reviewer / methodology — *where* the fix lands, orthogonal to the amendments-convention's 3-scope axis of *what* promotion status the amendment carries) + decision tree for routing + manual→automated flip criteria. Evidence: rally-hq's 4 amendments (hand-bucketed by priority + impact-scope in METHODOLOGY-AMENDMENTS.md) + blueprint-redesign's 6 amendments (hand-bucketed by wave-promotion order, with the operator explicitly forecasting "10+ amendments next cycle" in `WAVE-2-BACKLOG.md:76` and naming triage convention as a methodology gap). Two consumers already at meaningful volume; operator-named promotion-bar threshold met. Doc-only wave — defers tool-build to second-consumer adoption of automated triage per the 2026-05-27 market-comparison "buy via GitHub Copilot SDK as a GitHub Action" verdict (matches Blueprint's per-developer distribution shape; closed-taxonomy SaaS options don't fit). Tool sketch included in the doc for the build-when-triggered path. Closes inspiration-candidate C4 from `docs/_archive/2026-05-27-loom-inspiration-candidates.md`. Promoted via `docs/_archive/2026-05-27-extended-audit-findings.md`. No reviewer, schema, or stamper impact. Freeze acknowledgment: all four locally-accessible consumers at rest (rally-hq, subs-initiative, blueprint-redesign, blog); no consumer-side methodology edits in flight. No consumer-sync needed (additive; the manual triage already in use continues to work; the doc formalizes what consumers already practice).

- Wave 30 (housekeeping) — wave log relocated from `CLAUDE.md` to `WAVE-LOG.md` to keep the agent charter scannable; `digest.mjs` reads it via `--source WAVE-LOG.md`. CLAUDE.md retains a pointer + current-state summary. Doc/structure-only; no template, reviewer, schema, or stamper impact; no consumer-sync (the shipped template convention still places the wave log in CLAUDE.md — a future wave can align the template if the relocation proves out).
- Wave 31 (`5e7ede7`) — **Pattern A portal → genuinely generic config-driven harness, plus the distribution substrate's first build slices.** Three arcs from the `blueprint-platform` productization dogfood (`wip/blueprint-platform`; scope ceiling A — methodology-native; charter + prescription + ADR-0003..0007 + Stage-1 canonical research). (1) **Portability + semver baseline**: SessionStart hook resolves `BLUEPRINT_HOME` via env → `blueprint.yml methodology_home` → local canonical → npm-installed package (was a single stale `wip/blueprint` literal); root `package.json` `@nino-chavez-labs/blueprint-cli` 0.1.0 + Changesets + CHANGELOG + dormant release Action establish the version primitive (`methodology_version` consumed by the hook with a drift banner); `stamp.mjs` stale generated-path leaks killed. (2) **Generic Pattern A harness** (load-bearing): `template/apps/portal` shipped as the methodology's own dogfood portal with data-coupling + narrative baked in — a fresh `--pattern=A` stamp could not `npm install` (phantom `@blueprint/gate-derive` import; no workspace root) nor render (loaders threw on absent state/board/scenarios substrate). Now: NEW `portal-config.ts` reads a `blueprint.yml portal:` block (decisions dir+pattern, optional source paths, computed `features` map); all 5 loaders degrade-to-empty universally; `@blueprint/gate-derive` vendored as a generic degrading package (EPICS=[]); `stamp.mjs writeWorkspaceRoot()` scaffolds the workspace-root package.json; `repo-root` marker METHODOLOGY.md→blueprint.yml; ~22 pages/components de-narrated against the frozen contract (10-agent parallel sweep). (3) **Stamper hardening**: `mechanicalCheck` scoped to stamped paths only (`apps/portal/`+`packages/`+root pkg) — closes the evidence-doc false-positive; `REPLACE_FOR_PROJECT_{NAME,REPO,TAGLINE}` + substrate `.pages.dev` URLs substituted; de-narration sentinels added as tripwires; banner text project-agnostic. **Verified**: fresh stamp → `npm install` → `astro check` (0 errors) → `astro build` (14 pages) clean at Tier-0 with zero sources; `blueprint-platform` re-stamped renders its real deliverables (charter excerpts / live ADR catalog / 14-step build-order ladder) with zero reference-project leak (screenshot-confirmed). Closes two `blueprint-platform` amendments (portal over-coupling to the dogfood substrate; stamper mechanical-check false-positives). Large stamper + portal-substrate impact; no schema/reviewer changes. **Freeze acknowledgment**: built on the `platform/substrate` worktree under explicit operator waiver ("Fix it" / North Star / campsite); `blueprint-platform` served as the verification re-stamp, not parallel-evolved; other consumers at rest. **Consumer-sync**: Pattern A consumers re-stamp to inherit the generic harness, then author a `blueprint.yml portal:` block pointing the IA routes at their deliverables; an unconfigured (`portal:`-less) consumer gets a clean Tier-0 catalog portal by default, so existing/older Pattern A stamps keep building (backward-compatible defaults). Pattern B unaffected.
- Wave 32 (`5ced121`) — **`@nino-chavez-labs/blueprint-cli` dispatcher + methodology-home resolver (build-order step 2).** Operationalizes ADR-0001 (dual-protocol distribution) / ADR-0007 (toolchain) from the `blueprint-platform` dogfood (`wip/blueprint-platform`, scope ceiling A). Thin, dependency-free ESM CLI: `bin/blueprint.mjs` routes `init` (delegates to the canonical `template/tools/blueprint-init/stamp.mjs` — the CLI is a stable front door, not a reimplementation) plus five not-yet-built subcommands (`review`/`upgrade`/`fleet`/`cost`/`doctor`) that resolve the methodology home and print an honest contract + their build-order step rather than crashing. NEW `bin/lib/blueprint-home.mjs` resolves the methodology source (env `$BLUEPRINT_HOME` → nearest `blueprint.yml` `methodology_home:` → the CLI's own npm package root → local dev paths), mirroring the SessionStart hook and adding the package-self probe so an `npm i -g` install resolves with zero config. `package.json` `bin: { blueprint }` wired; hand-rolled arg parsing (commander is the named escalation if the surface grows). No schema/reviewer/stamper-behavior impact (the stamper is unchanged; `init` shells it). **Verified**: `--version`/`--help`, `init` delegation, stub routing + exit codes (3 = not-yet, 2 = usage/unknown), unknown-command error, resolver resolution. **Freeze acknowledgment**: built on the `platform/cli` worktree off wave-31 main under the continued platform-build waiver; `blueprint-platform` consumes it via its build-order roadmap, not parallel-evolved; other consumers at rest. **Consumer-sync**: additive — once published, consumers get `npx @nino-chavez-labs/blueprint-cli init` as the zero-install scaffolder; no existing behavior changes (the five subcommands are honest stubs until steps 3/5/7/8/12 land). Remaining build-order: step 3 (first executable `.mjs` reviewer, ADR-0002/0006) → 12.
- Wave 33 (`66e2c51`) — **Excerpt rendering fix: `loadExcerpt` produces clean prose.** The Pattern A portal's `ExcerptCard` printed `loadExcerpt`'s raw markdown verbatim — table pipes, `**bold**`, and backticks rendered as literal syntax on the discover/inspect ExcerptCard surfaces (caught on the deployed blueprint-platform portal). `content.ts loadExcerpt` now skips markdown table rows/separators when building the excerpt body and strips inline emphasis/code markers + normalizes bullets, so a teaser reads as prose (the full table is one click away via the source link). Small generic-harness fix; no schema/reviewer/stamper impact. **Verified**: blueprint-platform re-stamped + redeployed — discover cards render clean (no raw pipes/markers). **Consumer-sync**: re-stamp to pick up; additive. Companion consumer-side guidance (not a methodology change): point `portal.excerpts` at PROSE sections, not tables — tables belong on Inspect.
- Wave 34 (`31ec653`) — **First executable `.mjs` reviewer — `portal-pattern-a-conformance` (build-order step 3, ADR-0002).** Proves the `review()` contract runs outside Claude Code (plain node / CLI / CI), closing the "15 `.md` reviewer specs, 0 `.mjs` executables" gap at the implementation layer. `portal-pattern-a-conformance-reviewer.mjs` implements all 8 conformance checks (IA routes, audience switcher, `@blueprint/ui`+design-tokens sourcing + no local shadows, package wiring, deliberation-venue, `REPLACE_FOR_PROJECT` banners, legacy invariants) and returns the ADR-0002 `ReviewResult` — `{status, findings[] with remediation, metadata}`. The CLI gained a real `blueprint review <name> --target=<dir> [--json]` (replaces the step-3 stub) that loads the `.mjs` by convention and exits 1 on BLOCKED. The paired `.md` was aligned (executable-pair note + audience-switcher check repointed at the nav component `PortalNav`, matching the generic harness). **Verified four ways**: real blueprint-platform portal → BLOCKED (correctly flags 12 `REPLACE_FOR_PROJECT` markers with remediation); clean portal → PASS; missing route → BLOCKED; `--json` honors `{severity,location,message,remediation,reference}` + `metadata`. Unblocks step 6 (the skip-justification gate reuses this loader) + step 11 (org-authored reviewers via the same `.mjs` channel). From the `blueprint-platform` dogfood. Additive (one `.mjs` pair; the other 14 specs remain `.md`-only until migrated); no schema impact. **Freeze acknowledgment**: built on `platform/reviewer` off wave-33 main under the continued platform-build waiver; other consumers at rest. **Consumer-sync**: additive — `blueprint review portal-pattern-a-conformance-reviewer` available once the CLI is published; existing reviewers unaffected.
- Wave 35 (`eb250e5`) — **Cost/effort dial config + resolver (build-order step 4, ADR-0003).** Closes charter req #3 ("cost-efficiency configurable like the effort level of Claude Code") at the config layer. NEW `cost:` block in `template/blueprint.yml`: a per-stage `{ effort, model_tier }` vector orthogonal to `execution.depth` (depth = how many docs; cost = how hard each stage thinks + which model runs it). `effort` uses Claude Code's enum **verbatim** (`low|medium|high|xhigh|max`); `model_tier` is `opus|sonnet|haiku|inherit`. The 7 stage anchors (research/design = opus high; prototype = sonnet high; fact_check = opus xhigh; documents = sonnet medium; deploy = sonnet low+`skip_justification: mechanical`; iterate = sonnet medium) are seeded from `docs/patterns/tiered-orchestration-pattern.md` and labeled **PROVISIONAL** — recalibrate from `.blueprint/telemetry.jsonl` after ~10 cycles, never ship telemetry-free numbers as settled. NEW `template/tools/lib/cost-dial.mjs` (dependency-free, no yaml dep, never throws): `parseCostBlock` / `resolveCost` (stage override → default → built-in) + `ANCHORS` + `underProcessed()` — the step-6 gate primitive that flags a stage resolved BELOW its anchor without a `skip_justification` (charter risk #3: under-processing must be a recorded conscious choice). `inherit`/unranked tiers never count as a provable downgrade. **Verified**: self-test (8 assertions PASS) + resolver run against the real shipped `cost:` block (all 7 stages parse; gate self-consistent — shipped values == anchors, nothing flags). Materialization into subagent frontmatter + the `blueprint cost` sweep are steps 5-6; inline-orchestrator stages stay advisory-only per ADR-0003. From the `blueprint-platform` dogfood. Additive (new schema block defaults applied only when present; new lib has no consumers yet); no reviewer/stamper-behavior impact. **Freeze acknowledgment**: built on `platform/cost` off wave-34 main under the continued platform-build waiver; other consumers at rest. **Consumer-sync**: additive — consumers add a `cost:` block to tune per-stage effort/model; a `cost:`-less consumer inherits the built-in `medium`/`inherit` default, so existing stamps are unaffected.
- Wave 36 (`d95dadd`) — **Cost-dial observability + enforcement: telemetry + `blueprint cost` sweep + skip-justification gate (build-order steps 5-6, ADR-0003).** Completes the ADR-0003 trilogy (config/telemetry/gate) on top of wave 35's resolver. **Step 5**: NEW `template/tools/lib/telemetry.mjs` — append-only `.blueprint/telemetry.jsonl` writer (best-effort; never breaks a stage run) + tolerant reader (skips garbled/blank lines) + `summarizeTelemetry` (per-stage count / median duration / pass-rate / by-model_tier); `duration_ms` is labeled a tier-weighted **TIME proxy, not dollars** (Claude Code doesn't expose per-turn tokens to a skill). The CLI's `cost` command promoted from stub to real: two views — (1) config (resolves the `cost:` block per stage + flags any below-anchor-unjustified stage the gate would BLOCK; exit 1 when flagged) and (2) telemetry sweep when the jsonl exists, else "anchors stay PROVISIONAL until ~10 cycles." `--json` emits both. Cost libs load from the methodology home (same decoupling as `review`). Self-test 12 assertions, idempotent. **Step 6**: NEW `cost-gate-reviewer` (`.mjs`+`.md`, second executable ADR-0002 reviewer) reusing `cost-dial.mjs` so gate and report can't disagree — no `cost:` block → PASS (advisory; preserves wave-35 backward-compat); stage below anchor with no `skip_justification` → BLOCK; below anchor WITH justification → WARN (visible, non-blocking). **Hardening** (caught during gate verification): `parseCostBlock` gained a `present` flag (distinguishes a deliberate cost block from built-in defaults so a `cost:`-less consumer is never gated) and `parseInlineMap` now splits on top-level commas only — a `skip_justification` containing a comma no longer truncates. **Verified four ways**: at-anchor → PASS; no block → PASS; downgraded fact_check unjustified → BLOCKED (exit 1); same downgrade justified → WARN (exit 0, full reason intact). From the `blueprint-platform` dogfood. Additive (one new lib, one new `.mjs`+`.md` reviewer pair, `cost` subcommand now real); no schema-shape or stamper-behavior change. **Freeze acknowledgment**: built on `platform/telemetry` off wave-35 main under the continued platform-build waiver; other consumers at rest. **Consumer-sync**: additive — `blueprint cost` + `blueprint review cost-gate-reviewer` available once the CLI is published; consumers wire telemetry by calling `appendTelemetry` from a stage hook (advisory until wired). Build-order: steps 0-6 done; step 7 (consumers.yml + `blueprint fleet`) next.
- Wave 37 (`1bfdc19`) — **Consumer registry (`consumers.yml`) + real `blueprint fleet` (build-order step 7, ADR-0005).** Opens Distribution & Versioning track A: a methodology-side view of fleet drift, the precondition for push-to-fleet (step 8 `upgrade`). NEW **`docs/decisions/0005-consumer-registry-and-fleet.md`** — a real methodology-source ADR (the prior "ADR-0005" was a phantom citation in the CLI stub; `docs/decisions/` stopped at 0004). It establishes: `consumers.yml` at the source root (methodology-side meta — "who depends on me", the inverse of a consumer's `blueprint.yml`); the **no-clone** constraint (fleet computes from the methodology's OWN git history alone — scope ceiling A); the **mirror-not-authoritative** model (the authoritative pin lives in each consumer's `blueprint.yml` where the SessionStart hook reads it; `consumers.yml` is a hand-maintained mirror with `synced_at` staleness, drift is drift-against-the-mirror); a **two-anchor version model selected by pin SHAPE** (semver pin → compare to `package.json` version; sha pin → compare to HEAD by git ancestry; **no cross-shape ordering**); six drift classes (`current`/`behind`/`ahead`/`on-deprecated`/`unpinned`/`unresolvable`); and an **actionable-only exit contract** (`unpinned` is informational exit-0, not drift — most of the live fleet is unpinned; a corrupt/empty-salvage registry forces non-clean to avoid a false-green). NEW **`consumers.yml`** at the methodology root, seeded with the real fleet (rally-hq/blog/blueprint-redesign unpinned, blueprint-platform sha-pinned `010945a`). NEW **`template/tools/lib/consumers-registry.mjs`** — dependency-free (a NEW list-of-maps grammar cost-dial.mjs doesn't parse), never-throws, injectable git probe (real one shells `git`, fake one drives the self-test), `parseConsumersRegistry`/`classifyConsumer`/`computeFleet`; equals-HEAD is checked **before** ancestry (the false-"behind 0" guard, since `git merge-base --is-ancestor HEAD HEAD` is true both directions); 33-assertion self-test. The CLI's `fleet` promoted from stub to real (`blueprint fleet [--json] [--strict]`, reads `consumers.yml` from the methodology home, no `--target`; exit 0 clean incl. unpinned / 1 drift / 2 input-error). **Designed + adversarially reviewed via two workflows** (4 readers → design → 3 design lenses → reconcile, then 3 impl lenses → independent verify): 13 design issues + 5 confirmed impl defects folded in (semver prerelease+build regex, never-throws on non-string input, null-HEAD masking, table column truncation, git-probe-failure warning). **Verified end-to-end**: real `git` computes blueprint-platform = **behind 23 commits** (matches `git rev-list --count 010945a..HEAD`); `--json`/`--strict`/input-error paths confirmed; all three libs' self-tests green. From the `blueprint-platform` dogfood. Additive (new root file, new lib, new ADR, `fleet` now real); no stamper/reviewer-behavior change. **Freeze acknowledgment**: built on `platform/fleet` off wave-36 main under the continued platform-build waiver; other consumers at rest. **Consumer-sync**: additive — `blueprint fleet` available once the CLI is published; consumers opt in by adding a row to `consumers.yml` (and, when they want a computed position, a `methodology_version` pin in their own `blueprint.yml`). Build-order: steps 0-7 done; step 8 (`blueprint upgrade`, semver-aware pull) next.
- Wave 38 (`3d3904b`) — **Real `blueprint upgrade` — consumer-local pin bump (build-order step 8, ADR-0005, the DOWN channel).** Completes the bidirectional update protocol's pull side: `upgrade` is `fleet` for N=1 — it runs INSIDE a consumer repo, reads THIS consumer's `methodology_version`, classifies it against current (reusing `classifyConsumer` verbatim — the version model is not reinvented), narrates the delta, and gated bumps the pin. NEW **`template/tools/lib/upgrade.mjs`** (dependency-free, never-throws): `narrateChangelogDelta` (slices `CHANGELOG.md` `## <semver>` sections for a semver pin; degrades to the methodology **commit log** for a sha pin — today's only real case, `010945a` predates the `0.1.0` baseline — explicitly labeled, never fabricating a section), `computeGate` (the apply gate), `bumpPin` (surgical column-0 write), `isDirty`/`isTracked`; 23-assertion self-test. **`breaking` is now a field on the shared `classifyConsumer`** (single source of truth `fleet` + `upgrade` read): `true` when a `behind` pin crosses a major boundary, `false` within-major, `null`=UNKNOWN when a sha pin predates `package.json`; `makeGitProbe` gains `commitSubjects`+`packageVersionAt`; `semverParts` exported; `fleet` inherits `breaking` free (39-assertion self-test). `readYamlScalar` promoted to an export (was private; avoids a third scalar-parser copy). The CLI's `upgrade` promoted from stub to real: **plan/apply split** (Terraform canonical) — DRY-RUN preview by default (classify + narrate + propose + gate, mutate nothing), `--apply` writes; flags `--target --apply --ack-untagged --require-pin --json`. **Safe-by-default apply** (it mutates a consumer repo): a major bump or an untagged-sha range (breaking-UNKNOWN) is **refused** unless `--ack-untagged`; a **dirty-tree guard** refuses `--apply` on uncommitted `blueprint.yml` (makes the `git checkout` revert real); the write is a single anchored column-0 `methodology_version` line via **function-replacement splice** (no `$`-backreference injection) that never touches `methodology_home`, staged to a temp file and **renamed into place only after the value round-trips** (a failed write leaves the original pristine — `blueprint.yml` is evidence). exit 0 current/applied/unpinned · 1 drift-not-applied · 2 no-yml/dirty/write-fail. **v1 is pin-bump only** — chrome re-stamp stays the separate `stamp.mjs --mode=restamp-chrome` step (the heaviest surface + the `--accept-overwrite=ALL` footgun; deferred per the over-engineering red line). ADR-0005 extended with an Upgrade (DOWN-channel) section. **Designed + adversarially reviewed** via a workflow (3 readers → design → 3 lenses → reconcile; 9 blocking/major folded — `breaking`-as-shared-field, sha-major inheritance, dirty-tree guard, anchored bumpPin, drop-the-restamp-engine) + a focused write-path safety review (backreference-injection + atomic-write hardening folded). **Verified**: dry-run vs blueprint-platform = behind 25 commits → refuse-untagged (exit 1); `--apply --ack-untagged` bumps a throwaway consumer to short HEAD + preserves the trailing comment + cleans the temp file; dirty-guard/`--json`/`--require-pin`/no-yml all confirmed; all four libs' self-tests green; `fleet` regression clean. From the `blueprint-platform` dogfood. Additive (one new lib, `breaking` field, two probe methods, one export, `upgrade` now real); no stamper/reviewer-behavior change. **Freeze acknowledgment**: built on `platform/upgrade` off wave-37 main under the continued platform-build waiver; other consumers at rest. **Consumer-sync**: additive — `blueprint upgrade` (dry-run) available once the CLI is published; `--apply` is the consumer-initiated, gated pull. Build-order: steps 0-8 done; step 9 (access + governance artifacts — CODEOWNERS + rulesets + CONTRIBUTING + amendment-RFC, one wave under a freeze waiver) next.
- Wave 39 (`ab4e9e2`) — **Access + governance artifacts (build-order step 9, ADR-0004).** Opens the Access & Governance track C with the principle **access = the git host's own identity + controls; Blueprint carries intent, the host enforces** (no Blueprint-owned auth — scope ceiling A). NEW **`.github/CODEOWNERS`** scopes promotion-sensitive paths (`METHODOLOGY.md`, `docs/`, `template/`, `*AMENDMENTS*`, governance/distribution config) to `@nino-chavez` — promotion authority becomes **mechanical** (closes charter risk #6: a methodology change now structurally needs an admin review). Known degrade documented (a self-approval no-op while the admin set is one person; it BINDS when a second admin joins — matches the charter's "ratified by the first non-Nino consumer" gate). NEW **`CONTRIBUTING.md`** encodes the **Rust-RFC-lite** UP channel mapped 1:1 onto the existing 4-bucket amendment taxonomy (`docs/patterns/amendment-classification-pattern.md`: consumer-local / template / reviewer / methodology) — substantial change → RFC first, bug-fix → plain PR. NEW **`.github/ISSUE_TEMPLATE/amendment-rfc.yml`** the amendment/RFC issue form (bucket + kind + surfacing-consumer + **required evidence** — no imagined cases), which the step-10 triage Action will consume. NEW **`docs/governance/main-ruleset.json`** the `main` repository ruleset (require PR + 1 approval + `require_code_owner_review` + linear history + squash/merge) as committed JSON, plus **`apply-ruleset.sh`** an idempotent `gh api` create-or-update script. NEW **`access.roles`** schema in `template/blueprint.yml` — 4 roles (admin/contributor/reviewer/stakeholder) → git-host team + GitHub built-in repo role; intent-only, empty teams inert until the repo is org-owned. **Canonical-pattern-first**: repository rulesets over classic branch protection (the successor — layerable, most-restrictive-wins; do NOT run both layers); Rust-RFC-lite over KEP/PEP (a single-maintainer repo has no SIGs). **Operator action required to BIND enforcement** (not done by the pipeline — it changes repo settings): run `docs/governance/apply-ruleset.sh` (needs `gh` admin) and remove any classic branch protection on `main`. **Verified**: ruleset JSON valid + `_comment` strips clean, apply-script bash-syntax-checks + is executable, issue form well-formed, the new `access:` block does not disturb the `cost-dial`/`readYamlScalar` parsers; all four lib self-tests green. From the `blueprint-platform` dogfood. Additive (5 new governance files + one schema block); no code/reviewer/stamper-behavior change. **Freeze acknowledgment**: built on `platform/governance` off wave-38 main under the continued platform-build waiver (ADR-0004 explicitly batches these as one wave); other consumers at rest. **Consumer-sync**: additive — consumers inherit the governance pattern by re-stamping once `template/` ships these files (a follow-up; v1 governs the methodology source itself, the live UP-channel target). Build-order: steps 0-9 done; step 10 (triage Action — the 4-bucket classifier on the amendment-RFC issues, completing the UP channel) next.
- Wave 40 (`42369ab`) — **Amendment/RFC triage classifier (build-order step 10, ADR-0004) — completes the UP channel.** An amendment/RFC issue filed via the wave-39 form is classified by the 4-bucket taxonomy and labeled, so the operator triages by `bucket:`/`kind:` instead of reading every issue. NEW **`template/tools/triage/classify.mjs`** — pure `classifyIssue({title, body})` → `{bucket, kind, source, labels}`. **Primary path reads the issue form's DECLARED `bucket`+`kind` dropdowns** (GitHub renders them as `### <label>` sections — the operator's choice, not a guess; `source: 'declared'`); a **keyword heuristic** is the free-form fallback (ordered reviewer → methodology → template → else consumer-local, since a reviewer lives inside `template/`), tagged `triage:needs-human` because a guess must be confirmed. Emits the label set: `bucket:<x>`, `kind:rfc`+`needs-rfc` (substantial) or `kind:bug-fix`+`pr-ok`, `stays-in-consumer` for consumer-local. Dependency-free, never-throws, + a CLI mode that reads a `$GITHUB_EVENT_PATH` issue event; **13-assertion self-test**. NEW **`template/tools/triage/amendment-triage.yml`** — the GitHub Action that runs the classifier and applies labels (**labels only** — never merges/closes/edits), shipped **DORMANT** (parked outside `.github/workflows/` so installing the methodology adds no surprise CI automation; activation is an explicit copy). NEW **`README.md`** — pieces + label set + activation steps. **Shipped in the dormant-template form per operator steer** (no active workflow on the methodology repo; consumers + operator activate deliberately). **Verified**: self-test green; workflow YAML well-formed (no tabs, keys present); CLI classifies a declared event (reviewer/substantial → `bucket:reviewer`+`needs-rfc`) and a free-form event (stamper/chrome keywords → `bucket:template`+`triage:needs-human`). From the `blueprint-platform` dogfood. Additive (3 new files under `template/tools/triage/`); no active automation, no code/reviewer/stamper-behavior change. **Freeze acknowledgment**: built on `platform/triage` off wave-39 main under the continued platform-build waiver; other consumers at rest. **Consumer-sync**: additive — consumers (and the operator) activate by copying `amendment-triage.yml` into `.github/workflows/` alongside `tools/triage/classify.mjs`. Build-order: steps 0-10 done; step 11 (native extensibility — convention-discovered org-authored `.mjs` reviewers via npm/git, ADR-0006) next.
- Wave 41 (`96f60db`) — **Org-authored reviewer discovery — native extensibility (build-order step 11, ADR-0006).** A department extends Blueprint with its OWN executable reviewers **without forking** — the difference between a methodology and a platform. The interface **is** ADR-0002's `review()`; there is no SDK (the signature is the SDK). NEW **`template/tools/lib/reviewer-registry.mjs`** — `discoverReviewers` across three convention sources: **canonical** (`<home>/template/.claude/agents/blueprint/reviewers/*.mjs`), **org-local** (`<target>/.blueprint/reviewers/*.mjs`), **org-npm** (installed packages declaring `keywords:['blueprint-reviewer']`, shipping `reviewers/*.mjs`). Precedence **canonical > org-local > org-npm**; a same-name collision is a **SHADOW** — the higher-precedence reviewer wins + runs, the shadowed one is recorded + WARNed and **never runs** (an org reviewer may *tighten* via its OWN gate, never *relax* a canonical one — the load-bearing safety rule). No central-registration array (it recreates the merge-conflict choke point a platform exists to remove). Lifts `state-derive`'s `walkCatalog` + dynamic-import loop near-verbatim; `validateReviewerModule` checks the contract **without invoking** (no validation-time side effects; arity is deliberately NOT gated — `fn.length` is 0 for a valid `(ctx = {})`/`(...args)` signature, so an arity check would false-reject legitimate reviewers). Dependency-free, never-throws, **15-assertion self-test**. The CLI's `review` now resolves a name **across canonical + org sources** via the registry (org reviewers are runnable; a shadowed name resolves to canonical with a WARN), and NEW **`blueprint review --list [--target=<dir>]`** enumerates discovered reviewers grouped by source + the shadowed set (binding stays auditable). NEW **`template/docs/methodology/org-reviewer-authoring.md`** — the contract, the two distribution conventions (in-repo + npm-keyword), the canonical-before-org/tighten-only/shadow rules, and the **v1 trust model** (org `.mjs` run with full Node privileges — "trust whoever you `npm install`"; WASM sandboxing rejected for v1, npm provenance/signing deferred to an ADR-0007 supply-chain follow-up before cross-department install is encouraged). **Adversarially reviewed** (precedence/shadow correctness, never-throws across malformed node_modules, path-safety — a crafted name is only a Map lookup key, never builds a path, validation, trust boundary): zero blocking/major; the one finding (the arity false-reject) was loosened. **Verified**: `--list` shows the 2 canonical `.mjs` reviewers; running a canonical reviewer regresses clean through the registry; an org-local reviewer discovers + runs (`[org-local]`); a name shadowing a canonical resolves to canonical with the WARN; all five lib self-tests green. From the `blueprint-platform` dogfood. Additive (one new lib, one doc, `review` resolution + `--list`); no stamper-behavior change; existing `blueprint review <canonical>` unchanged. **Freeze acknowledgment**: built on `platform/extensibility` off wave-40 main under the continued platform-build waiver; other consumers at rest. **Consumer-sync**: additive — a consumer drops org reviewers in `.blueprint/reviewers/` or `npm install`s a `blueprint-reviewer-*` package; `blueprint review --list` shows them. Build-order: steps 0-11 done; step 12 (`blueprint doctor` — conformance/health gating on runtime/browser verification, the false-green guard) next — then the build order is complete.
- Wave 42 (`225d3ab`) — **`blueprint doctor` — the conformance capstone (build-order step 12). The build order is COMPLETE.** doctor is the trust-at-scale capstone that gates on REAL runtime verification, not a curl-200 / files-exist false-green: it **actually loads** the `blueprint.yml`, **loads + validates EVERY discovered reviewer** (a broken `.mjs` is a FAIL, not a green — it executes the load), parses `consumers.yml`, and **RUNS the portal conformance reviewer**. Critically it is honest about its OWN boundary — it prints what it did NOT check (full `npm`/`astro build` + browser/Playwright rendering are the deploy gate's job) so a green is never read as more than it earned (the anti-false-green discipline turned on doctor itself). NEW **`template/tools/lib/doctor.mjs`** — `runDoctor({home, targetDir})` → `{checks, status, notChecked}`; read-only orchestration over the shipped libs (`cost-dial`/`consumers-registry`/`reviewer-registry`) + the conformance reviewer; never-throws; worst-of severity aggregation; **8-assertion self-test**. The CLI's `doctor` promoted from stub to real (`blueprint doctor [--target=<dir>] [--json]`; exit 0 healthy pass/warn, 1 on any FAIL, 2 on load error) — it was the **last stub, so `STUBS` is now empty and every command (init/review/cost/fleet/upgrade/doctor) is real.** **Verified**: vs the methodology home → WARN (no blueprint.yml) exit 0; vs the `blueprint-platform` consumer → all checks pass incl. `portal-conformance PASS (7/7 routes, actually run)` exit 0; a broken org reviewer → `reviewers-loadable FAIL` exit 1 (the load caught it); `--json` shape; all six lib self-tests green; unknown-cmd/version/other-commands regress clean. From the `blueprint-platform` dogfood. Additive (one new lib + `doctor` real); no stamper-behavior change. **Freeze acknowledgment**: built on `platform/doctor` off wave-41 main under the continued platform-build waiver; other consumers at rest. **Consumer-sync**: additive — `blueprint doctor` available once the CLI is published; a consumer runs it to gate its own conformance. **Build order COMPLETE (steps 0–13).** The `wip/blueprint-platform` productization is feature-complete at scope ceiling A: configurable cost (B), bidirectional distribution + versioning (A: registry/fleet/upgrade), access + governance (C: CODEOWNERS/ruleset/CONTRIBUTING/RFC/triage), extensibility (D: org reviewers), enablement (E: deployed portal) — every track at done or in-progress with its build steps shipped. Remaining work is operator-gated activation (run `docs/governance/apply-ruleset.sh`; `npm publish @nino-chavez-labs/blueprint-cli`) + post-publish consumer adoption, not new build-order steps.
- Wave 43 (`docs`) — **Rationale promotion: the productization "why" now lives in the methodology source, not only the dogfood.** Closes a context-loss risk: waves 31–42 landed the productization OUTPUT (code + template) in `tools/blueprint`, but the INPUT/rationale (the five productization ADRs, the recon synthesis, the canonical research, the charter/prescription) lived ONLY in `wip/blueprint-platform`, while the source code cited those ADRs by name (`cost-dial` → `ADR-0003-cost-effort-dial`, governance → `ADR-0004-native-access-governance`, …) — **dangling citations**, no pointer to where they resolve. Promoted into the source: the 5 ADRs to `docs/decisions/ADR-0003..0007` (collision-free — the source numbers `000X-`, the platform `ADR-000X-`; each gets a provenance banner pointing back to the dogfood + the mirrored research), and the input analysis to NEW **`docs/productization/`** — `recon-synthesis.md`, `canonical-research.md`, `charter.md`, `prescription.md`, and a **`README.md` that is the orientation artifact**: the goal, the six-requirement gap scorecard, the six tracks (A–F), the scope-ceiling-A constraint, and the **ADR → feature → wave map** (so the "why" and the "what" are one click apart). `CLAUDE.md` canonical-docs section points at it. The live dogfood (`github.com/nino-chavez/blueprint-platform`, a durable public repo + deployed portal) remains the running original; this promotion makes the methodology source **self-contained** so the rationale survives independent of the consumer. Doc-only; no code/template/schema change. Not a build-order step — a provenance/durability fix surfaced by the operator ("confirm input + output make it back, else we risk losing valuable context").
- Wave 44 (uncommitted) — **Best-practices-corpus hardening: reviewer over-engineering guard + a `/goal` exit gate for Stage 4.** Two changes from auditing Blueprint against Claude Code's published best-practices (code.claude.com/docs). **(1) Reviewer behavior-model guard** in `template/.claude/agents/blueprint/reviewers/README.md`: a BLOCKED verdict is now explicitly reserved for findings that violate a stated requirement / JTBD trace / invariant / factual accuracy — stylistic, speculative, and "could also" findings are NON-blocking (list-as-optional, PASS). Codifies Anthropic's own caveat ("a reviewer told to find gaps will manufacture them, even when the work is sound") plus the operator's standing over-engineering red line, so the gap-finding reviewers cannot accrete defensive scope / extra abstraction / impossible-case tests onto a sound deliverable. Ships via `template/` — every consumer inherits it on next reviewer read. **(2) NEW `docs/prompts/run-validate-to-goal.md`** — an opt-in operator paste-prompt that drives `/blueprint-validate` under a `/goal` exit condition binding the two existing done-signals (the `fact-check-loop-reviewer` `CONVERGENCE: converged` verdict and the self-attested Phase 6 report) so the stage cannot claim DONE against a loop that never went green or merely escalated. Read-only consumer of the orchestrator verdict — does NOT re-count iterations or add a second escalation path (**no double-gating**); treats `CONVERGENCE: escalated` as terminal; carries its own turn-ceiling so it cannot out-run the 5-iteration cap. The bind is **evidentiary, not mechanical** — the `/goal` evaluator judges the transcript (forces box→quoted-verdict pairs), not the on-disk file; the airtight version needs a command-type Stop hook in `template/.claude/hooks/` and is deferred to its own wave. **Soft-coupling**: the prompt quotes `fact-check-loop-reviewer.md`'s report tokens (`CONVERGENCE: converged`, `STAGE 4: COMPLETE`, `ITERATIONS:`, `TOTAL_FINDINGS_RESOLVED:`) verbatim — if that file's "How to report" protocol changes, the prompt's tokens must update in lockstep or the evaluator pattern-misses and returns a false not-met. **Design adversarially verified before landing** (dynamic workflow, 3 lenses — laziness-closure, over-engineering/double-gating, methodology-coherence; all three returned adopt-with-changes; the changes are folded in: evidentiary-not-mechanical framing, verbatim-report-block + box→quoted-verdict requirement, this wave entry). The workflow also killed the naive design — wiring `/goal` into `validate.md` is **inert** because `/goal` is operator-typed, not agent-settable. **Freeze acknowledgment**: blueprint-redesign + rally-hq at rest; blueprint-platform active but untouched by these edits. **Consumer-sync**: (1) is additive behavior guidance — no restamp needed; (2) is an operator doc, not stamped — a consumer that overrides its orchestrator report format updates the quoted tokens by hand. Not a build-order step — a best-practices hardening pass surfaced by auditing against the published Claude Code corpus.
- Wave 45 (uncommitted) — **Fold: `blueprint-platform` becomes the in-repo self-application (the reference portal).** Resolves the source-vs-consumer separation in favor of a self-hosting single repo — "the compiler that compiles itself." The strategic question ("is it time to fold blueprint-platform into blueprint?") was answered by an evidence workflow that **refuted the two arguments for keeping them apart**: (1) the public/private boundary is not real — the methodology is MIT, published publicly to npm, README audience is "teams adopting Blueprint" (the private GitHub repo is incidental workspace placement, not a designed boundary); (2) the source≠consumer boundary is a **DIRECTORY boundary, not a repo boundary** — `template/tools/blueprint-init/stamp.mjs` walks only the passed `src` and never reads the repo root, and `mechanicalCheck` is scoped to the stamped target, so a root `blueprint.yml`/`research/`/`decisions/`/`apps/portal/` **cannot pollute what an external consumer stamps**. The only invariant that genuinely wanted a hard boundary — the methodology freeze — is about concurrent *editing* (the 2026-05-25 four-way divergence), already enforced by the `worktree-guard` hook + sha pins, not by repo topology. And `blueprint-platform` was the **only active co-developer** (rally-hq/blog/blueprint-redesign all at rest), so "many consumers need a clean source" was moot. **What moved in** (from `wip/blueprint-platform`): `apps/portal/` (the live Pattern A reference implementation + demo + onboarding knowledge base), `packages/` (`@blueprint/ui|design-tokens|gate-derive`), `research/`, `decisions/00-charter.md` + `01-prescription.md` (the 5 ADRs already canon at `docs/decisions/ADR-0003..0007` per wave 43 — NOT duplicated), `tools/archaeology/` (the live event-sourced provenance substrate over the productization history), root `blueprint.yml` + `METHODOLOGY-AMENDMENTS.md` + `START-HERE.md` + `HANDOFF.md`, and `.github/workflows/deploy-portal.yml`. **Collision resolutions**: root `package.json` keeps the published `@nino-chavez-labs/blueprint-cli` identity + gains `workspaces: [apps/*, packages/*]` + portal dev/build/typecheck scripts (the `files` array is unchanged — `apps/`/`packages/` are NOT published, dev-only); `.gitignore` merged (node_modules/dist/.astro/.wrangler); `blueprint.yml` repointed (`repo_url` → `github.com/nino-chavez/blueprint`, `decisions.dir` → `docs/decisions` so the portal renders the canonical ADR store via `/^(?:ADR-)?(\d{4})-/`, `methodology_version: self`); `README.md` gains a "this repo is its own first consumer" section; `consumers.yml` drops the now-stale `blueprint-platform` external row. **The invariant rewrite is the core methodology change**: `CLAUDE.md` flips from "no `blueprint.yml`/`research/`/`decisions/` at root = you opened the wrong repo" (the pre-fold rule) to "the root is Blueprint-applied-to-itself; `template/` is the clean stampable substrate; **never let the self-application's churn leak into `template/`** — if you're editing `template/` to make the portal build, that's the leak, fix it at the root." **Cleanup**: the `wip/blueprint-platform` repo is archived with a tombstone redirect; the deployed portal (`blueprint-platform.pages.dev`) is repointed to deploy from the canonical repo (same Pages project, CF secrets set on `github.com/nino-chavez/blueprint`). Operator-authorized fold ("fold it. make it its own consumer with its own portal as a reference implementation"). Not a build-order step — a topology decision that makes the methodology self-hosting.
- Wave 46 (`936407c`+`b985dde`) — **Bespoke-with-ADR escape for portal archetypes that fit neither Pattern A nor B (candidate Pattern C: operator console).** First non-stakeholder-facing consumer — `ai-content-engine` (a daily content *operation*, not a product) — fit neither portal pattern and responded by **dropping the pattern and hand-rolling** a bespoke `prototype/` ops cockpit (Ops cockpit / System overview / Work-with-us) under a down-declared `tier:0`, re-introducing the exact drift the two-pattern naming exists to kill — through the Tier-0 escape hatch. The failure mode was NOT "force content into the wrong six verbs" (the predicted shape) but **pattern avoidance**: a Tier-1 deliverable (deployed multi-surface portal + live `state.json`-driven cockpit + CF-Pages config) wearing a Tier-0 label, with no `decisions/` ADR recording the divergence. Closes the hatch in `docs/portal-and-tier-ladder.md`: (a) a decision-tree lane — Pattern A now gated on "*as a stakeholder front-door to a product*"; operator-facing consoles route to bespoke-with-ADR; (b) a "When neither pattern fits the archetype" subsection; (c) a Tier-0 clarification — a portal's existence makes the tier ≥ 1, however lightweight (no down-declaring to dodge the gate); (d) an Open-questions item tracking candidate Pattern C. The rule gets **teeth** in `template/.claude/agents/blueprint/reviewers/README.md`: "running neither conformance reviewer at Tier 1+ is a violation" now carries the bespoke exception — neither runs *iff* a divergence ADR is present; for a bespoke portal it is the ADR's **absence** that violates the gate (documented gate condition — **no new reviewer code**; automate on the 2nd bespoke instance). `template/CLAUDE.md` selection line + root `README.md` doc-index updated. Dogfood evidence: `METHODOLOGY-AMENDMENTS.md` (2026-06-05). **Deliberately NOT canonized**: one instance names the gap; Pattern B was extracted from three consumers — a third frozen pattern waits for a second console instance (then: same intent-slots, verbs reweighted/relabeled cockpit/overview/convert, audience pills operator-first). **Freeze acknowledgment**: blueprint-redesign + rally-hq at rest; the matrix doc lives only at root `docs/` (not `template/`); the roster + `template/CLAUDE.md` edits are `template/` doc-only (additive behavior guidance — no restamp). **Consumer-sync**: none required (additive). Companion: the consumer-side divergence ADR shipped to `ai-content-engine` PR #1 (`972838c`). Not a build-order step — a missing-pattern-category fix surfaced by the first operator-facing consumer.
- Wave 47 (uncommitted) — **Executable reviewer fleet build-out — 12 of 15 reviewers now run outside Claude Code (was 2).** Closes the scope-audit's "2 of 16 executable" gap on the *mechanical* reviewers. Built 10 NEW dependency-free `.mjs` implementing the ADR-0002 `review({targetDir}) → {status, findings, metadata}` contract for every reviewer with a genuine deterministic check (file/structure existence, config/field presence, byte-comparison, cross-reference resolution, word counts): `design-principles`, `pilot-profile-lock`, `portal-chrome-canonical`, `portal-pattern-b-conformance`, `prescription-evidence`, `prescription-jtbd-traceability`, `prototype-forge-provenance`, `prototype-smoke-runner`, `research-completeness`, `terminology-linter`. Each follows the reference shape (cost-gate/portal-a): header gate-rule comment, `NAME` const, `result()` helper, never-throws (risky reads degrade to BLOCKED/WARN findings), + a guarded self-test (`node <file>.mjs` exercises inline fixtures and exits 1 on any failed assertion). **Honest scoping is the load-bearing discipline**: hybrid reviewers implement only the mechanical subset and emit an "agent-verified (see .md spec)" info finding for judgment criteria — they never let a judgment criterion drive BLOCKED, and they never fake an LLM check as deterministic. **3 reviewers deliberately left spec-only** (agent-judgment — a `.mjs` would manufacture the gate the wave-44 guard forbids): `doc-quality-auditor` (so-what placement / mental-math / logic-gaps = LLM editorial judgment), `fact-check-loop-reviewer` (orchestrator over LLM sub-reviewers `citation-checker`/`current-state-claim-verifier`/`codebase-claim-verifier`, which don't exist as `.mjs`), `research-reference-grader` (quality grading). The 2 prior references (`cost-gate`, `portal-pattern-a`) untouched (an agent's unsolicited portal-a rewrite was reverted). **Classified + built + adversarially verified via a workflow** (classify → build → verify pipeline; every built reviewer confirmed: self-test passes + loads-via-registry + runs-clean + does-NOT-fake-judgment). **Verified end-to-end**: `blueprint review --list` shows all 12 canonical; real runs produce legitimate findings (`pilot-profile-lock` caught a real pilot edit with no ADR; `research-completeness` caught missing brownfield legs); `blueprint doctor` → `reviewers-loadable: 12 reviewer(s) load + validate`. From the methodology source itself (the reviewers ARE `template/`). **Freeze acknowledgment**: blueprint-redesign (`cc4f62f`) + rally-hq (`1aa3c35`) + blog at rest; additive (10 new `.mjs` alongside the existing `.md` specs — consumers inherit executable reviewers on next stamp; the 3 judgment reviewers keep running agent-side via their `.md` unchanged). **Consumer-sync**: none required (additive); `blueprint review <name>` now runs these in CI/CLI for any consumer. Not a build-order step — completes the ADR-0002 executable-reviewer thesis surfaced by the scope audit.
- Wave 48 (uncommitted) — **Bespoke-with-ADR portal escape, automated in `doctor` (the wave-46 second-instance trigger fires).** Wave 46 named the escape for portals fitting neither Pattern A nor B and left enforcement as a *documented* gate condition, to automate "on the 2nd bespoke instance." That instance arrived: the fold's product-site rebuild (wave 45) made the methodology's OWN `apps/portal/` bespoke — it dropped Pattern A's audience switcher + 6-verb lane router for a dev-tool homepage (hero + install command + quickstart + `/learn`) — so `blueprint doctor` correctly BLOCKED it on Pattern A conformance: the methodology failing its own gate. Resolved by automating the escape, NOT by reverting the product site. **`template/tools/lib/doctor.mjs`**: reads `portal_pattern` from `blueprint.yml`; when `bespoke`, runs NEITHER conformance reviewer and instead checks for a divergence ADR in `decisions/` via a filename convention (`findDivergenceAdr` — `/portal.*(bespoke|divergen)/i`) — present → `portal-conformance: pass` ("bespoke portal — divergence recorded in <ADR>; Pattern A/B conformance not applicable"); absent → `fail` (the ADR's *absence* is the violation, per wave 46). Both paths verified: the self-app (bespoke + ADR) → `doctor` PASS exit 0; a `/tmp` fixture (bespoke, no ADR) → `portal-conformance` FAIL exit 1 (the gate has teeth). **Declared the self-app bespoke**: `blueprint.yml` `portal_pattern: A → bespoke` + NEW **`decisions/02-portal-bespoke-product-site.md`** (why the product homepage fits neither pattern; the instance-diverges / template-stays-clean boundary; why this does NOT mint Pattern C — a product homepage is a *different* archetype from the ops console, so a third frozen pattern still waits for a 2nd instance of the SAME archetype). Docs flipped from "no automated reviewer enforces this yet" → "enforced in `doctor` as of wave 48": the reviewer roster `README.md` + `docs/portal-and-tier-ladder.md`. No reviewer code changed — this is `doctor` orchestration (which reviewer RUNS), matching wave 46's "neither reviewer runs" intent; the 12 reviewers from wave 47 are untouched. **Freeze acknowledgment**: blueprint-redesign + rally-hq + blog at rest; the `doctor` change is additive (consumers with `portal_pattern: A|B` see no change — only `bespoke` declarers hit the new branch). **Consumer-sync**: a consumer that hand-rolled a bespoke portal can now declare `portal_pattern: bespoke` + add a divergence ADR and `blueprint doctor` passes it. Not a build-order step — closes the wave-46 second-instance trigger, surfaced by the self-app's own `doctor` during the wave-47 reviewer build.

- Wave 49 (`e9f5f04`+`dc153f1`+`ba842dc`) — **Blueprint + hive team onboarding/adoption kit — the methodology answers "a real team adopts this," not just "the author dogfoods it."** Surfaced by an operator engagement: the partner SA (T.) adopting Blueprint for a client commerce-platform build — a *team*, so the hive coordination substrate is in scope, and "onboarding + adoption for Blueprint + hive for a real project team" is the unit of work. The competitive-positioning pass (`research/02-competitive-positioning.md`) had already named the matching gaps as the honest weaknesses: full hive = exactly one consumer, not productized (`bootstrap.sh` a placeholder that exits 1), multi-operator built-but-never-run, identity = a spoofable shared bearer. This wave closes the *adoption* surface of those. **Three docs (the crawl→walk→run framing, zero-infra first):** **`docs/governance/team-roles-and-conventions.md`** — the three lightweight conventions that handle most multi-operator failure with NO server (per-repo role declaration, worktree isolation, no-workaround success criterion), the territory-bound-DRI / cadence-operator-synthesis / spec-via-proposal operating model proven on subs-initiative, and the **litmus** for when contention is real enough to escalate from conventions to the full substrate (5+ parallel streams ∧ ≥2 concurrent sessions ∧ overlapping files ∧ tight timeline — observed, not scheduled). **`docs/governance/hive-identity-gap.md`** — honest disclosure that the substrate authenticates with a single shared bearer (anyone holding it can `register_session(human_name='<any-name>')` — the decision log is spoofable + unauditable), with the per-engagement decision: option (a) trust-within-a-known-team + a risk-register entry for ≤3 trusted/co-located, harden to per-session JWT (option c) *before* a 2nd team or external contributor joins; option (b) per-operator Worker rejected (destroys the shared board); carries its "why not canonical" sentence (scope-ceiling A defers per-principal identity as product-scope — a justification that holds for the methodology but NOT automatically for a contractually-binding engagement). **`template/tools/hive/ONBOARDING.md`** — productized from the subs-initiative kit (de-placeholdered; the three first-week trip-ups — territory-bound authority, spec-changes-via-proposal, reaped-session-recovery — pulled to the top). **The keystone — `template/tools/hive/bootstrap.mjs` + `blueprint hive setup`:** the manual 18-step `BOOTSTRAP.md` (which shipped as a `bootstrap.sh` placeholder that exits 1) collapses into one command that provisions a fresh Hive (CF D1 + Worker + Pages dashboard) from a vendored ai-hive kit. Dependency-free (`node:` builtins only), matching the six `tools/lib/` libs' shape: a **pure declarative PLANNER** (`buildPlan` — fully offline-testable, 18 ordered steps) split from a **side-effecting EXECUTOR**; never throws (every failure → `{ok:false}`, the CLI owns the exit code); `--self-test` (25 assertions). **terraform-plan discipline** (same as `upgrade.mjs`): dry-run PLAN by default — prints every exact command + file patch, touching no live billable CF infra — `--execute` applies (honors the operator's destructive-action-confirms rule + keeps the planner verifiable on a degraded network). **Idempotent** (D1/Pages reuse-if-exists; patches re-match) so a mid-run failure is safe to fix + re-run. **Integrate-not-absorb** (the ai-hive charter seam): operates on `--hive-dir`, vendors nothing. Bearer via `crypto.randomBytes` (parity with `openssl rand -hex 24`); secrets piped via stdin, never argv. **Three steps stay manual by nature** and live in the productized **`template/tools/hive/BOOTSTRAP.md`**, not the script (`wrangler login` OAuth — *checked* as a precondition; CF API-token creation — browser scopes; `hive_create_project` — in-Claude-Code MCP). `bin/blueprint.mjs` wires `hive setup` (loads bootstrap from the methodology home, same decoupling as review/cost/fleet/doctor; secrets prefer flags, fall back to env — the `op`-inject path). **Verified**: 25-assertion self-test passes; the plan renders correctly against the **real subs-initiative `.hive` kit** (the reference hive worker/d1/dashboard, account pinned); all 7 lib self-tests + `node --check` green. **Freeze acknowledgment**: blueprint-redesign (`cc4f62f`) + rally-hq (`1aa3c35`) + blog at rest; the engagement consumers are operator-authorized + at rest, so the `template/` edits are clear of the freeze. **Consumer-sync**: additive — a team taking on a coordinated build vendors an ai-hive kit and runs `blueprint hive setup`; the three docs are methodology-side references. The full hive RBAC/identity hardening (option c) + a multi-operator chaos test stay deferred until a 2nd team or external contributor is real (the identity-gap trigger). Not a build-order step — the adoption surface for the Blueprint + hive *system*, surfaced by the first real team engagement.

The blueprint-redesign consumer initiative is at rest as of `dogfood-v1` (commit `cc4f62f` on `dogfood/self-redesign`). Rally-hq consumer at rest as of `1aa3c35` (amendments file post peer-review revises). Methodology freeze lifted; methodology can now advance in parallel with future consumer initiatives. Consumer-sync command for existing Pattern B portals to pull waves 8-16: first run `--mode=audit-chrome --pattern=B --target=<portal-root>` to classify divergences, then `--mode=restamp-chrome --pattern=B --target=<portal-root> --accept-overwrite=<LAG-files-from-audit>` to pull forward safely.

- Wave 50 (`b503ca0`) — **Coherence + doc-currency gates (the Glass wave): active defrag, automated doc-reference rot detection, and the entropy-wall adoption litmus.** Prompted by an external comparable: Ramp's Glass team (3-person core, vibe-coded to 700 internal users) independently rediscovered the methodology's first principle — agent entropy is a missing-capability problem solved by mechanical gates + durable docs, "the engineering discipline doesn't go away just because the AI is writing the code" — and their two strongest fixes named two real Blueprint gaps: detection-only conformance (no active repair) and a doc-accuracy gate that existed only as a manual sweep (the 2026-06 184-doc audit). Captured as `research/03-comparable-glass-ramp.md` (per promote-rationale: the analysis is a committed artifact, with the sourcing caveat — internal tool, captive distribution; engineering lessons transfer, adoption numbers don't). **NEW `defrag-reviewer`** (.mjs + .md, wave-47 hybrid discipline): mechanical fragmentation census — duplicate exported symbols, near-identical component stems (Button/ButtonNew/BaseButton), identical normalized function bodies (the reimplemented-utility class), orphan modules via a relative+`@/`-alias import graph (skipped entirely when no graph exists — absence of edges is not evidence) — with consolidate-or-keep emitted as agent-verified (deliberate fork vs drift is judgment); the .md spec judges candidates into CONSOLIDATE/KEEP/RENAME/DELETE proposal plans. **Never BLOCKs by design** — a cadence pass (per wave / pre-release), not a merge gate; blocking on drift punishes in-flight work (README behavior-model exception recorded). template/-boundary groups are partitioned, never merged (boilerplate-vs-instance duplication is the charter working). Noise classes burned down against the live repo: storybook CSF exports, Radix compound sub-part names (Root/Trigger/Content), framework route handlers. Dogfood found real rot: ArchaeologyChat + DependencyGraph duplicated portal-vs-source, 10 genuinely orphaned portal components from the product-site rebuild (left for a defrag pass to dispose). **NEW `doc-currency-reviewer`** (.mjs + .md): broken internal markdown links → BLOCK (deterministic); `blueprint <cmd>` mentions at command position verified against the bin → WARN; unresolved backtick path citations → **agent-verified INFO, never a gate** — the dogfood proved prescriptive-consumer-path vs rotten-reference is semantic (ADR-0004 prescribes `research/personas/` mid-prose), so the mechanical half only shrinks the judgment list (parent-dir-exists gate, ≥2 segments, blockquote provenance-note lines skipped, date-prefixed/`_archive`/pattern/case-study docs excluded by register, `<!-- doc-currency: external -->` opt-out). The inverse direction (feature shipped, doc missing) deliberately stays with Stage-4 validate + doc-quality-auditor — a mechanical version would manufacture noise into a gate. **Wired into `doctor`** as check 7 (runs the reviewer; BLOCKED→fail) — the self-app now gates on its own doc accuracy every run instead of per-manual-audit. Dogfood fixes: `portal-and-tier-ladder.md` cited `template/docs/context/voice-template.md` (real location `docs/context/voice-template.md`); `atlassian-sdlc-export.md` (documents the subs-initiative RAG corpus) carries the external marker. **`docs/variant-selection.md` gains the entropy-wall litmus** — the four signals (outward growth, seam bugs, unreviewable PRs, docs lagging the agent's reference needs) as the midstream/brownfield adoption trigger for teams arriving post-vibe-coding; positions Tier 0 as pre-empting the wall and names the two new reviewers as the standing repair. Deferred with reasons: Dojo-style git-invisible contribution UI (second-instance rule; wave 49 covers the live engagement), memory-synthesis cron (SessionStart injection serves it), self-healing integrations (runtime product, not methodology). Verified: 6 lib self-tests + both new reviewer self-tests green; `blueprint review --list` → 14; `doctor` → 7 checks, exit 0. Freeze acknowledgment: all external consumers at rest (blueprint-redesign `cc4f62f`, rally-hq `1aa3c35`, blog, subs-initiative). Consumer-sync: additive — consumers inherit both reviewers via registry discovery on next methodology pull; no restamp required.

- Wave 51 (`132eb59`) — **Mom Test validation bridge: the generated→real demand-evidence discipline.** Operator-directed analysis of Fitzpatrick's *The Mom Test* against Blueprint's generated assets named a structural exposure: the fact-check loop gates *technical* claims hard (everything traces to code/citations), but *demand* claims in generated strategy/market docs are agent-hypotheses by construction — "buyers will want X" is a guess wearing evidence's clothes, exactly Fitzpatrick's false-positive trap (compliments / hypothetical fluff / wishlists), and an agent-accelerated research stage is MORE exposed because the hypotheses arrive polished. The fix is NOT mom-testing the prose (cargo cult — the test governs evidence collection from humans, not how documents read); it is making the generated→real bridge explicit. **NEW `template/docs/methodology/mom-test-validation-pattern.md`** (canonical): the three evidence classes every demand claim carries (repo-grounded / stakeholder-given / agent-hypothesis — class 3 is legitimate; presenting it as 1/2 is the violation), the **validation-script artifact shape** (riskiest assumptions + evidence class, the three scary questions first, per-assumption past-specific/their-life/non-leading question plans with the disconfirming answer spelled out, commitment asks, conversation log), and the **commitment currencies** (money > reputation > time > none). **`/blueprint-docs`** now emits `docs/content/validation-script.md` as a fifth package doc whenever the package contains demand claims + two new quality checks (load-bearing hypotheses must appear in the script's assumptions table; no "would you…" phrasing — a package may ship all-hypothesis, the gate is that the script NAMES them, not that they are validated). **`/blueprint-triage`** gains the commitment-weight axis: every item records what the stakeholder GAVE (`gave: money|reputation|time|none`); scope-adds with `gave: none` default to deferred (wishlist), the same ask backed by money/reputation recommends scoped-in; kudos never counts toward demand validation regardless of volume; items touching scripted assumptions append to the script's log — the demand-evidence record. Ride-alongs: `fact-check-loop-reviewer` spec gains the `hypothetical-demand-claim-checker` sub-reviewer (unanchored future-tense demand claim + unlisted in the script → BLOCK; listed → PASS, a hypothesis named as a hypothesis is honest); `research-completeness-reviewer` spec gains `JTBD_INVENTED_DESIRE` (warning — a JTBD `job` with no observable basis in `research/` must surface in the validation script, not hide in a persona file as fact). Spec-side only this wave per the wave-47 discipline — the new criteria are semantic judgment; mechanical halves wait for a real initiative to run them (second-instance rule). Skills are symlinked from the template, so the local install tracks live. Also in this push: the wave-50 defrag disposal pass (`2258d01`) — first end-to-end run of the defrag workflow over its own findings (7 orphaned portal components deleted, 4 KEEP dispositions with evidence, `layouts/` added to the reviewer's entry-like dirs after DocLayout false-positived via md `layout:` frontmatter). Freeze acknowledgment: all external consumers at rest. Consumer-sync: additive — consumers pick up the pattern doc + skill text on next methodology pull; no restamp.

- Wave 52 — **DoD verification ladder: presence is not function.** Surfaced by subs-initiative (2026-06-10): `state-derive` — which the template ships to every consumer — got enshrined as "the implementation-state authority," but ALL its check primitives are static (file/grep/schema/commit-message); none execute code or assert a test passed, so `COMPLIANT` = "expected artifacts exist," never "works / passes its AC / runs E2E." Proof-by-precedent: a widget injector emitting `<subs-initiative-widget>` and a PDP renderer reading `[data-bcs-widget]` were both COMPLIANT — broken feature, green register. The conflation was codified in the methodology source itself: the state-join pattern labeled the join "what's actually shipped," METHODOLOGY.md called the overlay a check "against reality," and the `scenario-coverage` category claimed an AC was "exercised end-to-end" when its checks only prove the test file exists. **NEW `template/docs/methodology/dod-verification-ladder-pattern.md`** (canonical): five gates per AC — G1 spec written → G2 prototype exists → G3 artifacts present (state-derive's ceiling) → G4 scenario exists AND passes → G5 green against the live deployment, with `blocked-pending-external` as a first-class state distinct from unbuilt; rules: monotone claims, one re-derivable register, derive-on-main (never hand-commit), gate-honest language on every rendering surface, and the register SURFACES the G4 coverage gap rather than assuming it. Honesty relabels landed across `template/tools/state-derive/README.md` + `types.ts` (presence-oracle scope note, COMPLIANT caveat, scenario-coverage exists≠passes), `docs/patterns/traceability-state-join-pattern.md` (implementationStatus = presence/G3), and `METHODOLOGY.md` (state overlay checks presence, not reality). Spec-side only this wave per the proof-first discipline: instance 1 (subs-initiative) is designing the mechanical half now (behavioral primitive reading CI test-results evidence vs sibling behavior-derive, per-AC↔scenario registry fields, blocked-external modeling, catalog drift resistance) — its ratified design upstreams via METHODOLOGY-AMENDMENTS in the promotion wave. Freeze acknowledgment: subs-initiative is mid-flight on its own pinned methodology copy, not mid-migration — no waiver needed; other external consumers at rest. Consumer-sync: additive doc pull; no restamp.

- Wave 53 — **Mom Test ask-outcome discipline: the zombie-lead rule.** Surfaced by the self-application's first buyer-persona feedback (`feedback/2026-06-10-eng-lead-thread.md`): an eng lead who hand-rolls Blueprint's exact stage/gate/reviewer shape affirmed the team-alignment problem unprompted, complimented the work — and took neither of two explicit commitment asks (tokens, repo access). The wave-51 pattern weighted what stakeholders GAVE but its Log shape had nowhere to record an ask **made and not taken**, so the load-bearing negative datum of the exchange would have evaporated and the log would read warmer than reality. **`template/docs/methodology/mom-test-validation-pattern.md`** gains § "Ask outcomes — advancement or rejection, both are data" (taken / not taken / no-ask-made; an untaken ask is first-class disconfirming evidence; kudos + open ask = the compliment trap at full strength) and an `Ask made → outcome` column in the Log shape. **`/blueprint-triage`** weighting rules gain the matching record-the-outcome rule. Direct promotion on first instance is justified because this is Fitzpatrick's own advancement/zombie-lead rule — source-grounded restatement, not n=1 invention. Self-app dogfoods the new shape immediately (`docs/content/validation-script.md` Log re-columned; NEW A6 personal-incumbent-displacement assumption + scary question 4 + A6 conversation plan from the same exchange). Two sibling gaps stay second-instance gated in `METHODOLOGY-AMENDMENTS.md` (2026-06-10): a `market-signal` triage category (evidence about the stakeholder's own practice had to be shoehorned into `opinion`) and an assumption-archetype checklist for the validation-script generator (A6 had to be discovered from feedback instead of prompted at generation time). Freeze acknowledgment: subs-initiative mid-flight on its pinned copy, not mid-migration — no waiver needed; other external consumers at rest. Consumer-sync: additive — pattern doc + triage skill text on next methodology pull; no restamp.

- Wave 54 — **docs/ reorg: typed subdirs, full clean tree.** Operator-triggered after the live feedback artifacts proved hard to find and the GitHub tree showed `docs/` as a 41-file flat dump. The fix is structural, not an index patch: `docs/` top level now holds ONLY the two SessionStart-hook-injected canonicals (`variant-selection.md`, `portal-and-tier-ladder.md`) + `README.md`; everything else descended into typed subdirs — **`docs/patterns/`** (20 pattern docs), **`docs/context/`** (bc-* platform packs, browser-legibility, voice-template, voice-b2b-addendum), **`docs/case-studies/`** (3 case studies + design-system-audit + the traceability sweep), **`docs/governance/`** (+ team-roles-and-conventions, hive-identity-gap), **`docs/_archive/`** (+ the four dated 2026-05 artifacts, atlassian-sdlc-export). All references rewritten repo-wide including `template/` (`$BLUEPRINT_HOME/docs/...` citations now point at the new paths); relative links inside moved files re-depthed; `doc-currency` green (60 docs, 0 broken links). Discoverability fixes for the trigger: `docs/README.md` gains a "Self-application working artifacts" section mapping `feedback/`, the validation script, `docs/content/`, and `METHODOLOGY-AMENDMENTS.md`; the charter's self-application list now names `feedback/`. Root hygiene: `.DS_Store` untracked + ignored. **BREAKING for stamped consumers**: already-stamped CLAUDE.md copies cite old `$BLUEPRINT_HOME/docs/<file>` paths (bc-* context packs, hive/cloudflare/archaeology/owner-spec patterns, voice-template, browser-legibility) — those references are stale until the consumer re-stamps or re-pastes `docs/prompts/pick-up-blueprint-updates.md`. Freeze acknowledgment: operator-directed reorg with explicit scope approval; subs-initiative mid-flight on its pinned copy, not mid-migration; other external consumers at rest. Consumer-sync: run the pick-up-updates prompt (refreshes the stamped CLAUDE.md reference table) — content unchanged, paths only.

- Wave 55 — **enforcement wiring + privacy-safe capture (presence ≠ function, applied to the gates themselves).** Operator challenge ("is Blueprint failing its own reviews?") audited the session's failures: every one — PII pushed public, the docs/ dump, 5-wave-stale claims, a 6-wave changeset lapse — was operator-caught, not gate-caught, and the gates that DO cover a class (doctor/doc-currency) were invocation-gated: **no CI ran them**, so a doctor-failing state could land on main and deploy. Two direct fixes: **NEW `.github/workflows/doctor.yml`** (doctor's 7 checks on every push/PR to main; dependency-free checkout + node, no install) and the **`/blueprint-triage` anonymize-by-default capture** (verbatim originals → gitignored `feedback/raw/`; committed captures carry role + initial, paraphrase private-DM quotes, drop personal disclosures; private repos treated as public-later — the skill previously *instructed* the verbatim copy that caused the 2026-06-09/10 PII leak and its history rewrite). NEW anti-pattern: committing verbatim identity. Deferred to `METHODOLOGY-AMENDMENTS.md` (2026-06-10, enforcement-gaps entry): the mechanical changeset-presence CI check (ADR-0007 enforcement) and the stateful-claim lint (instance 1 of the claim-rot class; second-instance gated). Freeze acknowledgment: subs-initiative mid-flight on its pinned copy, not mid-migration; other external consumers at rest. Consumer-sync: additive — skill text on next pull; the doctor workflow is source-repo-only (consumers gain it when stamped CI ships, a separate candidate).

- Wave 56 — **public-repo sanitization + reader-path review + neutral template source identity.** Operator-directed, root-cause framed: the reader-path docs (what a stranger reads to answer "what is this / how do I use it") have NO usage-feedback loop — functional docs self-heal via the amendments cycle, comprehension docs just teach the wrong thing — and the self-app had never run Stage 4 against its own reader surface. Three workstreams. **(1) Reader-path systematic review** (sanitize + adjudicate every claim): METHODOLOGY.md repositioned — Blueprint is the full loop (research → BRD/PRD-class docs → planning → prototype → fact-check → build handoff), not "a jig"; Forge Signal rename (was Forge Signal) everywhere in prose (`signal_forge:` config key kept); README drift fixed (duplicate pre-CLI quickstart deleted, `portal_pattern: bespoke` corrected, private-repo notes on companion tools); START-HERE fixed (hive command row added, internal channel ref removed); the **reader-path manifest** declared in `docs/README.md` with a review cadence — the encoding so this drift class can't recur silently. **(2) Employer/initiative de-naming** (public repo names no real client/employer work): the platform context packs (B2B edition / buyer portal / marketplace) UNBUNDLED to private per-engagement supply; all initiative names → neutral descriptors (subscriptions/promotions initiative), people → role + initial, demo URLs de-linked, employer env-var/infra examples genericized, `consumers.yml` carries neutral slugs with identities in gitignored `consumers.local.yml`. **(3) Neutral template source identity**: the stampable substrate's reference strings are now `blueprint-example`/`@blueprint-example/`/`--bpx-`/`An example product initiative`; `stamp.mjs` substitution table + mechanical check re-keyed; URL-blanking rules retired (the tree carries blanks); SUBSTRATE_TRIPWIRES slimmed to narrative sentinels. Functional keeps (public npm/tech identifiers, not initiative names): `@bigcommerce/big-design*` deps inside the legacy `template/prototype/` shell + the `bigdesign` design_system enum + `signal_forge:` key. Verified: stamp end-to-end test green (mechanical check PASS, parameterization fires), all 6 lib self-tests, doctor 7/7, portal build, doc-currency 0 broken links. Freeze acknowledgment: subs-initiative mid-flight on its pinned copy, not mid-migration; other external consumers at rest. Consumer-sync: BREAKING for restamp — `restamp-chrome` keys on the NEW neutral strings (changeset carries the migration note); prose/doc changes are additive pulls.

- Wave 57 — **market-signal triage category + assumption archetypes (two second-instance promotions) — and the ground-truth-scope gap named.** Driven by the eng-team thread (June 7–9, `feedback/2026-06-09-eng-team-thread.md`): the first stakeholders to review a generated ARTIFACT (not the pitch) against their own domain knowledge. Three outcomes. **(1) Second-instance rule fired twice:** `/blueprint-triage` gains the **`market-signal`** category (evidence about the stakeholder's own practice — R.'s four-skill loop was instance 1, V.'s AI-mobbing practice + 3-cycle model instance 2; both had been shoehorned into `opinion`) plus an explicit **`logged`** state; `mom-test-validation-pattern.md` gains § **Assumption archetypes** (incumbent-displacement, solo-vs-team credibility, ground-truth scope) so the script generator prompts for predictable assumption shapes instead of discovering them from feedback. **(2) NEW gap, instance 1 (candidate):** **ground-truth scope** — the promotions initiative's architecture was verified GREEN by fact-check against a repo that implements only a slice of the domain ("the main promotion logic still lives in [the monolith]"); the loop has no check that the analyzed codebase is the right ground truth. Candidate: a Stage-1 mandatory scope question + a Stage-4 `GROUND_TRUTH_SCOPE` criterion; the live test is **(3) the inbound advancement** — the eng manager PROPOSED a second-attempt experiment (full repo scope + team review time), unprompted: the strongest positive signal class in the validation log to date (a commitment offered, not extracted). Validation script: NEW A7 (solo-operator prod credibility, challenged — role-gated sign-offs per stage is the buyer's model), A6 partial counter-datum, two Log rows, the ACCEPT-the-offer commitment ask. Freeze acknowledgment: subs-initiative mid-flight on its pinned copy, not mid-migration; other external consumers at rest. Consumer-sync: additive — skill text + pattern doc on next pull; no restamp.

- Wave 58 — **START-HERE folded into README + the first set-level reader-path pass.** The operator's deletion-test question ("why do we need both?") exposed what wave 56's per-file review couldn't see; the manifest gained the two set-level questions (deletion test per doc; claim ownership per shared claim) and this wave applies them. **The fold**: START-HERE.md deleted; its unique 20 lines (team-evaluation path, Blueprint-vs-Hive, charter pointers, pilot invite) became README § "Evaluating Blueprint for your team?"; the on-ramp is now portal → README → variant-selection; references repointed across CLAUDE.md, docs/README, the portal /learn page, and `blueprint.yml`'s discover excerpt (verified resolving in the built page). **Claim-ownership fixes**: the five-check document-quality audit had THREE competing statements (voice-template: five checks; METHODOLOGY + README: four) — voice-template is the owner, both losers now sketch-and-point; README's Voice Modes table (a restatement of voice-template's own table) trimmed to a pointer; README's "How It Works" now names METHODOLOGY.md as the owning doc and its Project Structure tree was corrected to what `init --pattern=A` actually stamps (it showed the legacy prototype/ shape — a whole architecture behind the stamper). Freeze acknowledgment: subs-initiative mid-flight on its pinned copy, not mid-migration; other external consumers at rest. Consumer-sync: additive doc pull; consumers with stamped references to START-HERE.md don't exist (it was never stamped).

- Wave 59 — **stateful-claim lint: the counts/versions/latest gate (third-instance promotion).** The claim-rot class crossed its own threshold — "forty-nine waves captured in CLAUDE.md" (06-10 sweep), HANDOFF's "Latest wave (49)", and the charter's "at rest as of wave 54" + "0.1.0 on npm" (06-11) — every instance operator-caught. NEW reviewer pair `stateful-claim-lint-reviewer.{md,mjs}` (ADR-0002: dependency-free, never-throws, 9-assertion self-test): five checks against located sources of truth — wave-currency claims vs WAVE-LOG max (BLOCK), consumer counts vs consumers.yml, fleet counts vs the reviewers dir, doctor-check counts vs doctor.mjs, version pins vs package.json (WARN) — each SKIPPING where its source is absent, so consumer repos run it safely. Honest-gate calibration on the live repo: the first run produced 27 findings, 5 true; the shipped patterns (registry-flavored lines for consumer counts, 50-char doctor proximity, ADR/deprecation/fence exclusions) produce exactly the 5 true positives, all fixed this wave. **Wired as `doctor` check 8** → CI gates on it per wave 55's enforcement wiring. **The claim-ownership consequence (operator question: "why is wave log content embedded in CLAUDE.md?"):** the charter's hand-maintained current-state paragraph — a cache of WAVE-LOG's tail that rotted every wave and got hand-bumped 6 times today alone — is GONE; CLAUDE.md and HANDOFF now point at WAVE-LOG's last entry as the single source of wave state. Fleet truth: 18 specs, 15 executable; doctor 8 checks (all lint-guarded numbers now). Freeze acknowledgment: subs-initiative mid-flight on its pinned copy, not mid-migration; other external consumers at rest. Consumer-sync: additive — new reviewer arrives on next methodology pull; doctor check 8 skips gracefully where sources are absent.

- Wave 60 — **terminology-linter scan-set extension: `.md` pages, root README, operator-doc exemptions (amendment promotion, scan-set half).** Driven by the 2026-06-10 jargon audit of the self-application ("what is Pattern A? without context it means nothing") — two manual copy passes (`a1dbd4a`, `7257539`) de-jargoned surfaces the gate never read. Promoting the amendment surfaced a correction: the executable already walked `apps/portal/src` + `docs/content` (the spec lagged it); the real holes were the file-type filter dropping `.md` entirely (learn pages + content deliverables walked, then discarded) and root `README.md` absent from the candidate set. This wave: `.md` joins the scan set with a Markdown text extractor (frontmatter, code fences, inline code, link URLs are not copy); root README scanned as the public entry point; operator-facing basenames exempt inside scanned subtrees (`DESIGN.md`, `CLAUDE.md`, `STATE.md`, subtree `README.md`s, …); acronym allow-list gains universally-understood dev terms (API/CLI/SDK/AI/UI/UX/YAML/…) so `.md` scanning doesn't WARN-spam — ADR/BRD/PRD deliberately stay flagged (the define-on-first-use class). Spec + executable + self-tests updated in lockstep (36 assertions, 8 new). The surface-taxonomy half (reader-facing bans vs define-on-first-use rule classes) stays second-instance gated per `METHODOLOGY-AMENDMENTS.md` 2026-06-10. Freeze acknowledgment: subs-initiative mid-flight on its pinned copy, not mid-migration; other external consumers at rest (fleet verified this wave: 12 consumers, 0 behind). Consumer-sync: additive — updated reviewer arrives on next methodology pull; the broader scan may surface new findings on the first Stage 5→6 run (review them; fix copy or extend the glossary).
- Wave 61 — **stamper defaults: `--name` is the only required flag; target auto-created; defaults echoed.** Driven by the 2026-06-10 website audit's adversarial pass: every `init` command printed on the product site errored when pasted, because the stamper demanded all 8 flags and a pre-existing target — while the category convention (create-astro, create-next-app) is scaffold-with-defaults. This wave: `template/tools/blueprint-init/stamp.mjs` derives display-name (title-cased slug), tagline, repo-url placeholder, `--variant=greenfield --tier=1 --pattern=A`, and `--target=./<name>` (created when missing); every applied default is echoed on a `defaulted:` line so a scaffold never carries a value the operator didn't see; bare `init` fails with a working one-liner instead of a flag dump. Explicit flags override everything — full-flag invocations are byte-identical to before (verified: greenfield + brownfield regression stamps, mechanical check PASS). Contract docs updated in lockstep (`blueprint-init/README.md`, `template/CLAUDE.md`); the product site, root README, and demo fixtures now print the minimal runnable form. Freeze acknowledgment: external consumers at rest (fleet verified this session: 12 consumers, 0 behind; subs-initiative on its pinned copy, not mid-migration). Consumer-sync: additive + backward-compatible — existing full-flag scripts unaffected; the new minimal form arrives on next methodology pull.

- Wave 62 — **DoD verification ladder: the mechanical half (instance-1 promotion).** Wave 52 shipped the spec side (five gates + honesty rules) and explicitly deferred four design decisions + the mechanical realization to instance 1 — which built it and ran the ladder at scale across a real backlog. This promotion fills the stub. **`template/docs/methodology/dod-verification-ladder-pattern.md` gains both halves:** the **four resolved decisions** — a `scenario_passes` check primitive INSIDE state-derive reading a CI-produced `_scenario-results.json` (fail-safe: absent/stale/no-run/only-skipped → UNKNOWN-never-COMPLIANT; any failed → NON-COMPLIANT; the `as_of_commit == HEAD` staleness guard load-bearing); the load-bearing **correction** that G4 is an AC-keyed `behavior-gate` cap set, NOT retrofitted scenario-coverage caps (AC↔scenario is many-to-many, so keying scenario-centric caps by primary AC false-reds a passing scenario sharing an AC with a failing one); `blocked_external` as a catalog field with `review_when` expiry; drift resistance via schema-checks-not-`file_exists` + catalog-currency lint + orphan-tag lint — plus **the operational half**: derive-time-but-never-commit results production (avoids the derive-on-main rebase race by construction), render-from-the-ONE-register (a second unverifiable rollup datastore was demoted), the self-logging operating model (surface→log→**prioritize [the one operator knob]**→dispatch→verify), and **the verification discipline — 11 failure modes** the orchestrator must guard when per-AC authoring is parallelized across agents (code-inspection-predictions-unreliable, assert-the-bug-false-green, CI-green≠pass, phantom-reference, crash-as-RED, event-lag, FK-seed, **fake-coverage-via-presence-assertion** [the G3-wearing-a-G4-label anti-pattern this ladder exists to kill — assert a STATE CHANGE, not a seeded row], **false-gap-from-not-reading-source** [grep to PROVE absence before any honest-RED], secret-scanner-on-fixtures, mock/spy-state-bleed). Three strategic patterns: **the gate is the work-finder** (surfacing what's missing IS the path to feature-complete; G4 yield ≠ presence-count; a RED closes by fixing-the-scenario OR building-the-feature), **present-but-broken is the keystone payoff** (G3-COMPLIANT features that never worked — a privacy export that crashed on every record, an erasure path that crashed on an invalid constraint value — the proof presence ≠ function), and **some REDs are spec conflicts to escalate, not code** (BRD-vs-ADR / BRD-vs-schema → spec amendment or operator/legal `[Decision]`). The check-primitive **code** lift (the `scenario_passes` check + `behavior-gate` category + net-new normalizer into `template/tools/`) is the sequenced next wave, lifted with the template's self-test bar against this now-complete spec. Domain-neutral throughout (no consumer-initiative specifics). **Freeze: operator-waived** — the operator explicitly directed this promotion mid-consumer-session (instance 1 still mid-flight on its pinned copy); the override is logged here, not silent. Consumer-sync: additive — the pattern doc arrives on next methodology pull; no restamp.

- Wave 63 — **DoD verification ladder: the engine code-lift (the behavioral primitive ships).** Wave 62 promoted the mechanical-half *methodology* + named the code-lift as sequenced; this ships it. **`template/tools/state-derive` gains the `scenario_passes` check** (`checks/scenario.ts`) wired into `runCheck`, the `ScenarioRun`/`ScenarioResults` contract + the `scenario_passes` `Check` variant + an `unknown?` flag on `CheckResult` in `types.ts`, and the **`unknown`→MANUAL_REVIEW fail-safe folded into `aggregateStatus`** — backward-compatible by construction: presence checks never set `unknown`, so `determinate === results` and the original static-derivation logic is preserved exactly (verified). The fail-safe is the load-bearing contract: artifact absent/unparseable/stale(`as_of_commit != HEAD`)/no-run/only-skipped → UNKNOWN→MANUAL_REVIEW (never COMPLIANT); any failed → NON-COMPLIANT; ≥1 passed + 0 failed → COMPLIANT; and the monotone rule (a passing presence check + an unknown G4 → MANUAL_REVIEW, can't claim done). **NEW `template/tools/scenario-results/`** — the net-new normalizer: parses Jest/Vitest + Playwright JSON reporters into the `_scenario-results.json` artifact, one run per AC (the `§story.ac` prose convention, `acs:`/`@ac:` tags preferred when present, no-AC scenarios keyed by slug), deterministic ordering, `as_of` stamped to the run commit. **It never executes tests — it parses recorded evidence**, produced ephemerally in the derive-on-main job and never committed (the no-state-files gate avoids the rebase race by construction). Both tools ship a self-test per the template bar: `checks/scenario.test.ts` (7 cases incl. the staleness guard + the monotone rule) and `scenario-results/index.test.ts` (7 cases — AC extraction, both reporter parsers, deterministic sort); all 14 green, tsc clean. Domain-neutral throughout (cwd-relative source paths, generic example fixtures, no consumer-initiative refs). The `behavior-gate` catalog category + AC-keyed cap helper stay consumer-authored (caps are per-initiative); the pattern doc § Engine lift documents the consumer wiring. The instance-1 working artifacts (`bc-subscriptions` `tools/state-derive` + `tools/scenario-results`) were the reference implementation. **Freeze: operator-waived** (continuation of the wave-62 directive — finish the engine lift). Consumer-sync: additive — the new check + normalizer arrive on next methodology pull; `scenario_passes` is opt-in (a consumer gains G4 by authoring `behavior-gate` caps + wiring the normalizer into CI), so existing static derivations are unaffected.
- Wave 65 — **portal chrome: route-aware `pageHref` (manifest `routes` map).** Driven by the rally-hq public-receipt audit (2026-06-11): meta-only product destinations — a documented Pattern B feature (catalog entries whose surface lives in production, not in the prototype) — broke every chrome surface that builds hrefs from page ids: `proto-nav.js`'s jump-to select and flow nav emitted `./pages/<id>.html` phantom links that soft-404'd to the front door (and with a 404.html present, hard-404). This wave: `_meta/index.json` may declare a `routes` map (`id → absolute or portal-relative URL`); `pageHref` prefers it and falls back to the old construction — fully backward-compatible (no `routes` key → byte-identical behavior). rally-hq is the reference consumer: three production destinations (`help-center`, `api-docs`, `account`) declared, chrome re-synced byte-identical. Related finding filed (NOT this wave): `restamp-chrome` has no scoped mode, so the audit's "restamp safely" hint is unexecutable when LAG and CUSTOMIZATION coexist — see rally-hq `METHODOLOGY-AMENDMENTS.md` 2026-06-11; promotion awaits the convention's second instance or the next tooling wave. Freeze acknowledgment: operator-directed consumer session (rally-hq); external consumers at rest (fleet: 12 consumers, 0 behind). Consumer-sync: additive chrome — arrives on next `restamp-chrome` or manual chrome copy; consumers without meta-only destinations are unaffected.

- Wave 64 — **DoD-ladder operational hardening: catalog-hygiene rule + the `[skip ci]`-starves-dashboards gotcha (two instance-1 amendments).** Both surfaced cleaning up a consumer's portal: a "look under the hood" page understated built features (G3 presence caps reading MANUAL_REVIEW for shipped+G4-verified work) and showed a stale ADR count. Verified both apply to the template, not just the instance, before promoting. **(1) `template/docs/methodology/dod-verification-ladder-pattern.md` § Catalog hygiene** — `manual_review` is NOT a behavior gate: a G3 presence cap carrying `manual_review:true` (often a "requires-browser/runtime" note) to flag "presence isn't the whole story" conflates presence with behavior — whether it works is G4, whether it's live is G5; holding the presence cap at MANUAL_REVIEW makes it double as a behavior tracker and understates built features on every register-consuming surface. Plus the **verify-by-derive** cleanup discipline: don't bulk-flip flags to "go green" (inverse of trust-but-verify) — remove the flag, RUN THE DERIVE, keep the removal only if it resolves COMPLIANT (false flag); revert if it goes NON-COMPLIANT/PARTIAL (real path-drift behind the flag — a false RED is worse than an honest MANUAL_REVIEW). The derive is the oracle distinguishing false-flag from real-drift, never the agent's say-so (instance-1 audit: 29 of 43 false flags cleaned, 14 path-drift left flagged). **(2) `docs/patterns/traceability-state-join-pattern.md` § Operationalizing** — the `[skip ci]` rederive commit (mandatory to avoid self-retrigger + push races) ALSO suppresses the push-triggered dashboard deploys whose path filters include the freshly-committed `_state.json`/`_board.json` → the dashboard reads the new state but never rebuilds, lagging every release while looking authoritative. Fix: the rederive re-triggers the deploys via `workflow_dispatch` (which `[skip ci]` doesn't gate — it's an API call, not a push event), gated on "state changed", `actions: write`, best-effort; corollary — give the rederive a `workflow_dispatch` trigger so the chain is forceable on demand. Both apply to the template, verified: blueprint ships `state-derive` with `manual_review` (types.ts) + a `deploy-portal.yml` reading committed derived files. The bc-subs-local dual-DoD reconciliation (a 10-gate Epic-DoD vs the 5-gate ladder) was assessed and NOT promoted — blueprint has no 10-gate model, so it's a consumer-specific concern. Domain-neutral throughout. **Freeze: operator-waived** (continuation of the 62/63 DoD-ladder promotion arc). Consumer-sync: additive — the two pattern-doc sections arrive on next methodology pull; no restamp, no code change (the rederive-workflow snippet is copy-in guidance, since the workflow itself is consumer-authored).
- Wave 67 — **portal chrome: cognitive-audit round — neutrality sweep, compare-mode layout, drawer close semantics, plain labels, audience-note fix.** Driven by the rally-hq cognitive walkthrough (2026-06-11). Five chrome fixes, all canonical: (1) the front-door audience-note claimed "Engineer leads with Inspect + Operate" — no Inspect card exists in the PATHS array; corrected to the priority objects' real order (Operate + Validate). Every Pattern A/B consumer inherited this phantom term. (2) Neutrality sweep: `proto-nav.js`'s current-state fallback said "No equivalent surface in shipped **Rally HQ**" — a competitor consumer would render Rally HQ's name; now manifest-derived (`shippedProductLabel()`) with a neutral fallback. Same sweep across `_portal-shell.js`, `proto-annotate.js`, `chat-widget.js`, template `index.html` PATHS copy, and the `prototype/index.html` "volleyball identity" comment. Back-compat identifiers (`demo_tournament`, `--tournament-accent`, `applyTournamentAccent`) deliberately kept with a do-not-rename note — renaming breaks consumer manifests. (3) Compare mode clipped at 1440px (1fr columns bottomed out at content min-width; in-flow chrome auto-placed into grid cells): fixed in the canonical `shared.css` baseline — `minmax(0,1fr)` columns, full-span chrome rows, viewport-fit panels with internal scroll, sticky badges — with the same rule-level patch applied to rally-hq's consumer-owned `shared.css`. (4) Drawer × is now strictly close-its-own-drawer (`closePanel()` + stopPropagation) — toggle semantics let a desynced click reopen the other drawer. (5) The "production chrome" toggle is labeled "real product navbar" (storage keys/classes unchanged). rally-hq synced byte-identical on all chrome files and additionally migrated its docs viewer to the manifest-driven canonical (`docs.tiers` + `footer` authored into `_meta/index.json`; the hardcoded 20-slug sidebar was the leak the template's own comment documents). Known residual: `template/portal/functions/api/chat.js` still carries rally-hq strings — next chrome pass. Freeze acknowledgment: operator-directed consumer session; external consumers at rest. Consumer-sync: chrome restamp (or manual copy) picks up all five fixes; the docs-viewer migration needs consumer-authored `docs.tiers` data per the template comment.

- Wave 66 — **Test discipline: the test is a gated co-deliverable of the build, not a downstream audit.** Surfaced by an operator interrogation of a consumer ("we can't say a feature is built if it hasn't been tested — shouldn't the methodology *drive* writing the test when the code is written, not wait for a tester?"). Closes a structural gap: the DoD ladder is entirely **detective** (checks state after the fact), and nothing binds **test-creation to code-creation** — the common operating model literally runs building and test-authoring as two separate passes (implementation lands code/G3; a later drain authors scenarios/G4), modelling tests as downstream of building (the "wait for a tester" anti-pattern). **NEW `template/docs/methodology/test-discipline-pattern.md`** (canonical): **(1) the principle — source over order:** a behavioral test must assert the SPEC's *required* behavior, not the code's *implemented* behavior; naive same-agent TDD fails for AI-built code (one agent writing test-then-code from its own interpretation goes green on a misread — the assert-the-bug false-pass), so the rule is **spec-first, test-as-contract**, not dogmatic red-green (strict red-green is also unreliable when the agent can't run tests locally — the load-bearing property is *sourced-from-spec + executed-in-CI*). **(2) the preventive build-stage contract:** the behavioral test is a first-class **gated co-deliverable** of the code (same unit of work, sourced from the AC, committed alongside, executed at the gate); "code present without its test authored *and* running" is a **build-stage gate failure**, not a three-waves-later discovery; plus the **two-role build** for high-stakes ACs (the agent that writes the spec-test ≠ the agent that makes it pass — the independence that catches present-but-broken). **(3) configurable rigor — opt-out, tier-bound, declared:** default ON (rigorous), dialed *down* explicitly (if rigor were opt-in the path of least resistance skips it); bound to `--tier` (T3 strict+two-role+coverage-gate / T2 co-deliverable+executed / T1 non-trivial-logic-only). **(4) the detective backstop:** a per-AC × per-test-type coverage matrix (functional/integration/E2E/BDD → not-defined / defined-but-failing / green), derivable only once tests carry the `acs:`/`@ac:` tag (so tagging is the prerequisite; untagged-test is itself a finding). Also adds a new **honesty rule to `dod-verification-ladder-pattern.md`**: **`G4`-green ≠ fully tested** — G4 is *at least one* passing scenario, NOT the full pyramid; reading it as "fully tested" is the presence-read-as-function authority-bleed recursed one gate up. Domain-neutral. **Freeze: operator-waived** (the operator co-designed this in-session). Consumer-sync: additive — the new pattern doc + the ladder honesty-rule arrive on next methodology pull; the build-stage gate + coverage-matrix are consumer-authored against the contract (no template code this wave — the matrix derive is a sequenced follow-up like the wave-62→63 split).
- Wave 68 — **portal chrome: navigation paradigm — the slice rail retires; breadcrumb switchers + flow stepper + command palette.** Operator decision after the rally-hq cognitive audit: the prototype pages are full-bleed product mockups — the mock IS the content — and the persistent rail spent ~210px of permanent width on slice-scoped wayfinding while the actual need (move across ALL prototype screens contextually) was served only by a footer select. The portal has exactly two traversal modes and each now gets its right convention: sequential = the flow stepper (`?flow=`, now M clickable step dots); random access = the breadcrumb segments as manifest-driven switcher menus (slice segment → all slices; page segment → current slice's pages + 'Walk:' flow entries) — global from every page, zero new real estate, the path you're on IS the menu you open. Plus a Cmd/Ctrl+K command palette over pages/slices/docs (manifest-driven, dependency-free). Slice context (label + summary) moved into the strategy drawer; the footer 'Jump to' select retired. Hardening from the verify pass: menus are position:fixed and JS-placed from the button rect, because consumer-owned top-bar CSS (rally-hq's ellipsis overflow:hidden) clipped absolute menus invisible; a ≤640px media query gives the crumb its own scrollable row and 40px targets (palette hint hidden on touch); ALL chrome script ?v= tokens bumped alongside the stylesheet token — the JS changed materially and stale-cached proto-nav against new CSS is the same failure class the compare fix hit. Upstream docs amended in lockstep: `proto-nav.OWNER-SPEC.md` (navigation model + rationale + retired pieces), portal `CONVENTIONS.md` v4 (shell stack rewritten), portal README; `functions/api/chat.js` + its OWNER-SPEC neutralized (wave-67's named residual closed — comments and fallback title now stamper-substitutable). rally-hq synced byte-identical (proto-nav, docs viewer) with the same shared.css rules patched rule-for-rule into its consumer-owned design system; deployed and verified (menus hit-testable at 1440, mobile usable at 390, compare panels 704px each full-width, flow stepper clickable, palette filters and navigates). Freeze acknowledgment: operator-directed consumer session; external consumers at rest. Consumer-sync: chrome restamp picks up the new nav; consumers with customized top-bar CSS should re-check their crumb rules (the fixed-position menus tolerate them by design).

- Wave 69 — **portal chrome: the canvas rule + mock frame primitive (CONVENTIONS v5).** Operator question after the nav wave: "why does the style change from screen to screen? first light now dark mode" — the rally-hq scoring mock themed the whole page body dark (`body { background: var(--arena-900) }` + a bespoke hand-rolled phone bezel), so the portal flipped light/dark page to page and per-surface theming intent read as sloppiness. The rule that resolves it: **the canvas belongs to the portal; the theme belongs to the mock.** New optional page-meta field `mock_frame` — `"desktop"` (default) | `"phone"` | `"none"` — read by `proto-nav.js`'s new `buildMockFrames()`: desktop surfaces render inside a browser-window frame (portal-owned light bar: three dots + the production route from `currentState.route`/`surface`); handheld surfaces inside a centered ≤420px bezel; document-style pages (comparisons, study content) and bespoke in-view device presentations opt out with `none`. The frame is the one sanctioned chrome touch of the page body — it wraps the product UI, never alters it. Mechanics that matter: frame clip is `overflow: clip`, not `hidden` (no scroll container → `position: sticky` inside mocks keeps working against the viewport — verified on rally-hq's tournament tabs); split mode's panel rules deliberately out-specify the frame container styles, so compare layout is untouched and the frame bar rides inside each panel; `init()` now falls back to a direct meta fetch for pages with a meta file but no manifest listing (never for `index`/`docs`). New anti-pattern: page-level `body { background: ... }` theming. rally-hq applied as the reference sweep: all 25 metas declare `mock_frame` explicitly (scoring=phone; vs-*/sketch/positioning docs=none; spectator-follow + captain=none — bespoke in-view phone presentations a single bezel can't wrap; 16 product surfaces=desktop), scoring.html dropped its bespoke bezel + body-dark + duplicate back-link, three more pages dropped `body { background }`, chrome synced byte-identical (proto-nav.js + OWNER-SPEC — the OWNER-SPEC was a stale pre-wave-68 consumer fork, now canonical), the same frame CSS patched rule-for-rule into rally-hq's consumer-owned shared.css, and ALL `?v=` tokens bumped to `20260611-frame` in both trees. Freeze acknowledgment: operator-directed consumer session; external consumers at rest. Consumer-sync: chrome restamp picks up the primitive; existing consumers get the default desktop frame on every compare page — pages that shouldn't be framed declare `"mock_frame": "none"` (additive, no breaking schema change).

- Wave 70 — **portal chrome: docs viewer reading ergonomics — on-this-page rail, heading anchors, scroll-spy, prev/next pager, 70ch measure, visible search.** Operator question wearing the technical-writer/document-UX hat: "is our docs layout conventional and correct and optimal?" Verdict: conventional and above-average on epistemics (tiered sidebar with rationale blurbs, DATE/METHOD/HONESTY-CAVEAT metadata header, consistent doc schema) but below convention on reading ergonomics. Six canonical fixes in `template/portal/docs/index.html` (the docs viewer is canonical chrome — never edited per-consumer): **(1)** the prose measure dropped 75ch→70ch, solidly inside the 66–75ch readable band; **(2)** an **on-this-page ToC rail** (third grid column ≥1100px, sticky like the doc-nav) built per-doc from the rendered h2/h3 — explicitly `display:none` below 1100px so the third grid child doesn't auto-place into an implicit row under the content (verified at 960px); **(3)** every h2/h3 gets a slugified, de-duplicated **anchor id** (was zero — long strategic docs couldn't be deep-linked or scanned positionally) with `scroll-margin-top` clearing the fixed bar; **(4)** an **IntersectionObserver scroll-spy** (`-80px / -70%` rootMargin) marks the heading nearest the viewport top active in the rail; **(5)** a **prev/next pager** at the foot of the content column, built from the flattened tier order, surfacing the reading path the tiers imply; **(6)** a **visible search trigger** in the sidebar — the Cmd/Ctrl+K palette proto-nav already wires globally was undiscoverable on docs (no breadcrumb to host the `⌘K` hint), so `proto-nav.js` now exposes `window.openBlueprintPalette()` and the docs sidebar surfaces a "Jump anywhere ⌘K" button that routes back through the single palette. ToC + pager rebuild on every doc switch and clear on error; a rail of <2 headings renders nothing. proto-nav.js changed materially (the palette export) so ALL `?v=` tokens bumped `20260611-frame`→`20260611-docs` in both trees, and proto-nav + its OWNER-SPEC + the docs viewer synced byte-identical to rally-hq. Verified locally on real consumer docs: research-synthesis (24 headings, 24 ids, scroll-spy tracks, pager prev/next resolve), validation-plan (17 headings), rail hidden at 960px, search opens the palette, measure capped at 70ch. Freeze acknowledgment: operator-directed consumer session; external consumers at rest. Consumer-sync: chrome restamp picks up the new viewer — additive, no manifest schema change (the ToC + pager auto-derive from rendered markdown + the existing `docs.tiers` order).
- Wave 71 — **portal chrome: docs hero metadata length discipline + strategic-doc authoring conventions (CONVENTIONS v6).** Follow-up to wave 70 from the same operator hat (frontend-designer + technical-writer audit of the rally-hq docs portal): the hero metadata block rendered every `**Label:**` value as a token pill (`border-radius:999px`, `line-height:1`, no `max-width` on the value), so a paragraph-length `**Status:**` (rally-hq's cx-strategy) or an 80–150-word `**Method:**` (every teardown) stretched the pill full-width and wrapped prose inside a rounded blob — the hero-metadata overflow defect. Two compounding gaps, one visible break: (1) `buildHeroChips` had no degradation for long values; (2) the hero-metadata convention itself was undocumented, so authors had no rule to keep `Status` short. Fixes in `template/portal/docs/index.html` (canonical chrome — never edited per-consumer): **(1)** `buildHeroChips` now routes by value length — ≤52 chars stays a pill (scannable token), longer renders as a `.doc-hero-row` definition row (label + wrapping block); a paragraph never lands in a pill again, and the label is now escaped (was raw); **(2)** the on-this-page rail clamps entries to two lines (`-webkit-line-clamp`) as a backstop against engineer-length headings overflowing the ~200px rail. `template/portal/CONVENTIONS.md` v6 adds a "Strategic-doc authoring" section documenting the hero-metadata convention, the field-length discipline (`Status` = state token, never prose; the ≤52ch pill/row split), the callout-vocabulary table, and the TOC-friendly-heading rule — the discipline that was missing. No `?v=` token bump: only the docs viewer's own inline CSS/JS changed (proto-nav.js + shared.css untouched), so the cross-asset cache stamps stay honest. Surfaced + first-applied by the rally-hq consumer: `cx-strategy.md` Status→token, reconciliation prose→blockquote note, surface headings→concept-first (routes demoted to italic sub-line); restamped byte-identical (PR nino-chavez/rally-hq#139) + amendment filed. Verified across the heaviest docs (cx-strategy, volleyballlife-teardown 150-word Method, research-synthesis, design-principles): pills for short values, definition rows for long, zero horizontal overflow. Freeze acknowledgment: operator-directed consumer session; external consumers at rest. Consumer-sync: chrome restamp picks up the length-aware renderer — additive, no manifest schema change.

Each wave includes a freeze-rule acknowledgment in the commit message and a recommended consumer-sync command.
- Wave 72 — **Portal type rename: "Pattern A/B" → "Initiative Portal / Review Portal" (decisions/03-portal-type-naming.md).** The labels "Pattern A" and "Pattern B" were opaque methodology catalog positions — a reader without prior context could not infer the portal's purpose from its name. Surfaced during mrr-automation initiative scaffolding (2026-06-12). Renamed across all live methodology surfaces: **Pattern A → Initiative Portal** (Astro + @blueprint/ui multi-audience front-door), **Pattern B → Review Portal** (static HTML + drawers brownfield audit/redesign shell). Field rename: `portal_pattern: A|B` → `portal_type: initiative|review|bespoke`. CLI flag: `--pattern=A` (deprecated alias, warns) → `--portal-type=initiative`. Reviewer files renamed: `portal-pattern-a-conformance-reviewer.{md,mjs}` → `portal-initiative-conformance-reviewer.{md,mjs}`; `portal-pattern-b-conformance-reviewer.{md,mjs}` → `portal-review-conformance-reviewer.{md,mjs}`. Doctor backward-compat shim: reads `portal_type:` first, falls back to `portal_pattern:` with a warn + migration instruction (maps legacy `A`→`initiative`, `B`→`review`). Terminology-linter: "Pattern A" and "Pattern B" added as deprecated labels — BLOCK finding if either appears in user-facing copy. **Live surfaces updated** (canonical doc, stamper, doctor, reviewer files, template CLAUDE.md, template blueprint.yml, mrr-automation consumer). **History not modified** (wave-log entries 1–71, existing decisions/*.md, CHANGELOG.md, docs/_archive/ retain "Pattern A/B" as recorded at time of writing). ADR: `decisions/03-portal-type-naming.md`. Freeze acknowledgment: all consumers at rest (0 mid-migration per `blueprint fleet`); mrr-automation consumer sync included in this wave. Consumer-sync: `blueprint doctor` emits a warn + one-line migration instruction for any consumer still on `portal_pattern:`; migrate by replacing field + value per ADR-03.
- Wave 73 — **Agent output discipline: the return value is the conclusion, not the corpus.** Surfaced while assessing a token-compression tool (Headroom, `extraheadroom.com`) against Blueprint's own practice (2026-06-26). The tool compresses verbose tool output before it reaches the model; Blueprint already does the high-leverage version at the orchestration layer (a dispatched agent reads a corpus and returns a finding), but the discipline was only encoded in `dispatch.md` "Reporting expectations" — never in `research.md`, the widest read fan-out in the pipeline. **NEW `template/docs/methodology/agent-output-discipline-pattern.md`** (canonical): the gap (an agent that dumps the corpus it read back to the orchestrator makes it re-pay to read what the agent already understood — delegation's cost paid twice), the principle (return the synthesized conclusion + citations, never the bytes; orchestrator keeps the finding + a pointer), the four-part rule (synthesis-first / every load-bearing claim cited / bounded size / pointers over payloads), and the tier dial (Tier 3 strict synthesis-only → Tier 1 conclusion-shaped but un-gated). **`/blueprint-research`** gains an "Output discipline" section pointing at the pattern. **`/blueprint-dispatch`** "Reporting expectations" (field 7) extended: report the synthesized result plus pointers, never the corpus. **`METHODOLOGY.md`** First Principle prose list gains the failure→encoding entry. Docs-only; no schema, reviewer, or stamper change. Freeze acknowledgment: all external consumers at rest (verify with `blueprint fleet`); self-application pinned to `methodology_version: self`. Consumer-sync: re-stamp to inherit the new pattern doc + updated skills; no migration required (additive, no field changes).
- Wave 74 — **Backlog drain: 16 open consumer amendments promoted in one wave (operator override of the second-instance gate).** Operator directive (2026-06-26) to clear the full pending-amendment backlog rather than wait for second-instance confirmation per `methodology-amendments-convention.md`. The override is deliberate and logged here for honesty: several of these are single-consumer findings promoted on operator authority, NOT because a second consumer independently hit them. Scope was triaged first — chapterzero's amendment file is empty (no work) and its proof-obligation item already shipped (via #25), so the real set was 16 across three consumers. **photography (4):** Review-Portal front-door audience-switcher gated behind a manifest flag (default off — the Review Portal is a drawer contract, not an audience switcher); cited-spec-evidence chrome styled + `proto-nav.js` skips the chip when no `citations` data; reviewers + SessionStart hook resolve the initiative root via a shared `template/.claude/agents/lib/initiative-root.mjs` (walk up for `blueprint.yml`) so subdir layouts satisfy both reviewer families; `stamp.mjs --mode=stamp --pattern=B` (Review Portal initial stamp) implemented. **bc-subscriptions (5):** NEW `three-pass-research-discipline-pattern.md` (Pass 3 = re-test option candidates against the eight architectural principles), `peer-vs-modifier-test-pattern.md` (failure-mode enumeration decides doc topology), `back-door-native-anti-pattern.md` (domain-named platform asks reframe to mechanisms) — all three wired as `design-principles-reviewer` Stage 2→3 gate checks 7–9; `cited-url-lint` lifted to `template/tools/cited-url-lint/` + `citation-correctness-pattern.md` + anti-circular-audit guard in `fact-check-loop-reviewer`; global methodology rules distributed via `template/docs/methodology/global-rules/{audit-discipline,decision-bias}.md` + install snippet + non-fatal SessionStart verification. **rally-hq (7):** Stage-1 Sibling-Project Scan (NEW `research-sibling-scanner` reviewer + METHODOLOGY.md Stage 1); parallel-dispatch overlap rule documented as mandatory (existing `parallel-dispatch-check/check.sh` already had the logic — documented, not duplicated); reference-quality grading (existing `research-reference-grader` verified + wired into Stage 1); scoped restamp-chrome (`--accept-overwrite=<files>` is now SCOPE — overwrites accepted files, emits `SKIPPED` for un-accepted diverged, no whole-run refusal); NEW `roadmap-registry-sync-reviewer` (.md + .mjs, 9-assertion self-test) + `stateful_pairs` schema field + `doctor` check 9 (mirrors wave-59 stateful-claim-lint). **Two shipped as FLAG-GATED PROPOSALS (Status: proposed, opt-in, default off — operator design calls, open decisions noted for review):** Foundation: Design System + IA capability stage (`foundation.enabled`, new `S-B` optional stage mirroring archaeology `S-A`, `foundation-stage-reviewer`); portal-chrome two-profile model (`prototype.chrome_profile: methodology-themed|consumer-themed`, default methodology-themed so existing consumers stay byte-identical; consumer-themed lets brand-owning consumers own `shared.css` and `@layer`-import a canonical `canonical-primitives.css` the reviewer byte-checks instead). Verification: `typecheck` green; `doctor reviewers-loadable` 17/17; new reviewer self-tests pass; `registry-sync` PASS. Pre-existing `doc-currency` BLOCK (1 broken link in `ground-truth-over-proxy.md` + 30 INFO path-citations) is baseline, untouched by this wave. **Freeze: operator-directed; external consumers at rest.** Consumer-sync: re-stamp to inherit; all new fields are optional with backward-compatible defaults (no migration required).
- Wave 75 — **Release gates on doctor + template-boundary gate mechanized + doc-currency baseline cleared.** Surfaced by a four-persona audit (adopter / contributor / investor / acquirer, 2026-07-02) whose contributor lens found the same shape wave 55 fixed, one layer up: detection existed, enforcement wiring didn't. Three fixes, no `template/` changes. **(1) Release path now gates on doctor**: `release.yml` runs `node bin/blueprint.mjs doctor` before the changesets publish step — `doctor.yml` has gated every push since wave 55, but as a separate workflow nothing tied its result to the publish path, so a failing main could still ship to npm. The gate is dependency-free and runs before `npm install`. **(2) Template-boundary rule mechanized**: NEW `.github/workflows/template-boundary.yml` — the charter's "a change to `template/` is a methodology change" was prose-only (no hook, no CI check); now a PR touching `template/**` without a `WAVE-LOG.md` change fails with the leak warning. Binds when PR flow is real; solo-phase direct pushes bypass it, same maturity path as CODEOWNERS. **(3) Doc-currency baseline (logged as untouched in wave 74) cleared**: the `ground-truth-over-proxy.md` broken link fixed at both occurrences (the proof-obligation pattern lives at `template/docs/methodology/`, not `docs/patterns/` — the file's third link already pointed there correctly); ADR-0005 fold-rot citation in `docs/decisions/0005-consumer-registry-and-fleet.md` updated to the promoted sibling path; stateful-claims mismatches cleared (CLAUDE.md: 13 consumers / 22 reviewer specs (17 executable) / 9 doctor checks); `doctor.yml`'s header comment de-numbered (the cached count rotted twice — 7 in the comment, 8 in the charter, 9 in code). Verification: `blueprint doctor` overall PASS exit 0 (doc-currency broken-links=0, missing-citations 30→29 — the remainder are prescriptive consumer-path INFO items by design; stateful-claims mismatches=0); doctor self-test PASS 8/8 (previously failed its "real home is healthy (pass/warn)" assertion). Freeze acknowledgment: external consumers at rest per `blueprint fleet` (3 behind, 0 mid-migration). Consumer-sync: none — source-repo workflows + root docs only; nothing stamped changes.
- Wave 76 — **Hive integration contract extracted: substrate-agnostic MCP tool contract, not a host.** Salvaged from the `platform/hive-contract` branch (Track F slice, remote tip `1f3de2e`) — the branch itself was unmergeable: its `template/blueprint.yml` predated wave 56 sanitization (would reintroduce employer-named content), reverted the wave-72 portal-type rename, and deleted wave-74 fields (`foundation:`, `stateful_pairs:`, `chrome_profile:`); only the contract was extracted, the branch retired (tip archived locally). **NEW `template/docs/methodology/hive-integration-contract.md`**: Blueprint integrates with a Hive coordination companion against its **MCP tool surface** (the ~25 `hive_*` tools proven in subs-initiative — session lifecycle, projects/repos, tasks, locks, blackboard, status), not against a host — multiple Hive incarnations exist (CF Worker + D1 proven; Vercel + Supabase built; ai-hive RFC) and all speak the same tools, so the contract binds to the constant and leaves the host an org decision. Boundary table codifies integrate-not-absorb: Blueprint never absorbs persistence/identity/coordination (the named SaaS-creep failure mode); access control stays the git host's job. **`template/blueprint.yml` hive block**: NEW `mcp_endpoint` field — the only required wiring for any conforming Hive; existing CF-specific fields (`source`, `mcp_server`, `dashboard`, `cf_account`, `d1_database`) re-scoped as reference-impl-only. Additive — `hive setup` (bootstrap.mjs) reads no `blueprint.yml` fields, self-test 25/25. **`template/CLAUDE.md`** optional-capabilities row for `hive.enabled` now points at the contract + the coordination pattern. Verification: `blueprint doctor` overall PASS exit 0; hive bootstrap self-test PASS. Freeze acknowledgment: external consumers at rest per `blueprint fleet` (0 mid-migration). Consumer-sync: re-stamp to inherit the contract doc + updated hive block; `mcp_endpoint` is optional with a backward-compatible default (no migration required).
- Wave 77 — **Jurisdiction closes as a class: drift lints declare their scan sets, doctor diffs them against the tree, terminology gains an enforcement point, doc-currency reaches the consumer-shipped docs.** Promotes the 2026-07-02 jurisdiction-audit amendment same-day (operator override of the second-instance gate: the detection-without-jurisdiction class fired 4× in one day — stateful-claims/portal, terminology/docs-index, terminology/no-enforcement-point, doc-currency/template-docs). Four changes: **(1) `jurisdiction` exports** on the three corpus scanners (`doc-currency-reviewer`, `stateful-claim-lint-reviewer`, `terminology-linter`) — each declares roots/rootFiles/extensions/excludes as its honest-scope statement. **(2) NEW doctor check `lint-jurisdiction`** (check 11): enumerates the tree's prose surfaces (`.md`/`.astro`/`.html`; record dirs and non-root operator-doc basenames exempt), diffs against the union of declared jurisdictions, WARNs with the uncovered list — informational by design, so a new surface or scanner re-raises the coverage question automatically instead of each gap being operator-found. A `template/` path counts as covered when its STAMPED location is covered (the substrate is linted consumer-side after stamping, and doctor now enforces it there). Current home state: 19 genuinely uncovered surfaces named (demos shell, tool-shell, package previews, `template/design_principles.md`, …). **(3) `terminology` wired as doctor check 10** — the linter's wave-72 deprecated-label BLOCKs had sat live on the deployed portal for three weeks because it was invocation-only (the wave-55 class, third instance). Enablers shipped with it: the linter reads `product_type` + `terminology.glossary` + `b2b_edition.enabled` from `blueprint.yml` via a dependency-free line-scan when callers pass `blueprintYml: null` (before this, `isDevTool` never fired from doctor/CLI paths and dev-tool repos got jargon BLOCKs on their own vocabulary); NEW `terminology.glossary: "none"` declares a no-glossary repo (jargon handled by copy rewrites / define-on-first-use — the operator rule) and retires the missing-glossary WARN; both fields documented in `template/blueprint.yml`, declared in the self-application's root `blueprint.yml`. **(4) doc-currency walks `template/docs/**`** — the consumer-shipped methodology docs, the one surface where rot multiplies by fleet size; the extension immediately caught 3 broken links (archaeology READMEs linking a consumer-side `../decisions/` that does not exist source-side — de-linked to honest path citations per the un-linkable-source convention; `architect-challenge-pattern.md` linking a portal route that no longer exists). Also fixed: a missed wave-72 label in `try.astro`. Verification: linter self-test PASS, doctor self-test PASS 8/8, `doctor reviewers-loadable` 17/17, `blueprint doctor` overall WARN exit 0 (WARNs are the two new checks reporting honestly: acronym heuristics + the 19 uncovered surfaces; all fail-class checks green). CLAUDE.md check count updated 9→11 (caught by stateful-claims mid-wave — the gate working on its own change). Freeze acknowledgment: external consumers at rest per `blueprint fleet` (0 mid-migration). Consumer-sync: re-stamp to inherit; both new `blueprint.yml` fields are optional with backward-compatible defaults (empty = prior behavior; no migration required). Consumers with jargon-heavy dev-tool copy should declare `product_type` before their next doctor run — the terminology check is new in their CI.
- Wave 78 — **Variant-fit hint at stamp time: greenfield-by-default stops being silent.** Promotes the 2026-06-25 amendment's deferred template half (2nd instance: mrr-automation birthed the research variant by hitting this trap; chapterzero reproduced it — an investor-deck read stamped greenfield ran the full product pipeline and surfaced the mis-fit at Stage 5, portal scaffold mostly throwaway). `stamp.mjs` now echoes a variant-fit HINT whenever `variant` is defaulted rather than explicitly passed: brief/deck/dataset work with no product and no codebase belongs on `--variant=research` (portal-optional, provenance-only, deliverable = decision memo), with a pointer to the pre-stamp fit check that has led `docs/variant-selection.md` since 2026-06-25. Silent when any `--variant` is explicit — the hint targets the default path only, because both instances entered through the default, never through a wrong explicit choice. Verification: dry-run stamp shows the hint on the defaulted path and zero HINT lines with `--variant=research`. Freeze acknowledgment: external consumers at rest per `blueprint fleet` (0 mid-migration). Consumer-sync: none required — stamp-time UX only; nothing in existing stamps changes.
- Wave 79 — **Template scripts for blueprint strategies: serve.sh + manifest-driven prep-deploy.sh.** Fills gap found by session mining: `serve.sh` hand-written in 2 initiatives (rally-hq, rally-hq-audit-2026-07 — identical implementations), `prep-deploy.sh` in 3 (rally-hq [hardcoded doc list + drift check + audit harness banner], blog [hardcoded SOURCES array], rally-hq-audit-2026-07 [hardcoded doc list]). Divergence pattern: initiative-specific extensions (rally-hq drift checks, audit banners) stay local; canonical versions are initiative-agnostic. **NEW `template/blueprint/serve.sh`**: simple static server serving `./prototype/` on configurable port (default 8765), kills prior process on port for safe re-run. **NEW `template/blueprint/scripts/prep-deploy.sh`**: manifest-driven via `_meta/index.json` (Wave 6 pattern), uses Node.js to parse manifest + extract source:id pairs + copy docs to `_docs/` for portal deployment. Path resolution: `$(dirname "${BASH_SOURCE[0]}")` walks up to BLUEPRINT_DIR/INITIATIVE_DIR, parameterized by script location (works for `blueprint/scripts/` layout; portal-only at `portal/scripts/` can adapt the same logic). Both scripts follow the path hierarchy where real initiatives keep them. Verification: `bash -n` syntax check on both scripts PASS; test manifest-driven prep-deploy (created `_meta/index.json`, ran script, confirmed Node parse + doc locate + copy to `_docs/`) PASS; worktree-guard clean. Freeze acknowledgment: external consumers at rest per `blueprint fleet` (0 mid-migration). Consumer-sync: optional — consumers with hand-written prep-deploy can adopt canonical via copy/reference; new consumers copying `template/blueprint/` inherit both scripts automatically; no breaking changes (existing hand-written versions continue to work).
- Wave 80 — **Deterministic core / agentic shell: `blueprint stage status|advance` promotes stage sequencing from prose to an executable model (ADR-0008).** Provenance: a David Khourshid talk ("Goodbye slop; welcome determinism", AG Grid 2026-06) named, in sharper vocabulary, the architecture Blueprint already half-had. The self-audit finding: Blueprint's stage *sequencing* was **prose control flow** — the agent reads METHODOLOGY.md and decides which stage it's in / whether the gate passed — the talk's exact named failure mode. Triage was already a real state machine and variant-selection already selects between four; the spine was the gap. **Grounded in evidence, not theory**: a derivability probe run against this repo found **11/14 stage gates machine-derivable, 3/14 needing assertion** — and the 3 non-derivable ones (sensor drives the app, claims re-derived, URL reachable) are exactly the "agentic shell" edges. **NEW `template/tools/lib/stage-model.mjs`** (dependency-free, 11-assertion self-test): a declarative stage model (data + an 8-kind CHECK_KINDS registry, no closures) selected via `stage_model:` in blueprint.yml (built-in `greenfield` default; JSON-path override; unknown variant names fall back with an honest note — midstream/brownfield/research ship in rollout step d). Each gate carries a `derivable` flag = the deterministic-core / shell boundary. Linear-spine cursor (highest N with *every* stage ≤ N passing — rejects "furthest independent all-pass"). **NEW `blueprint stage` subcommand**: `status` renders position + the derivable/assertion split; `advance` is a terraform-plan-style dry-run preview of the next transition's entry-guard (state recording + `--execute` deferred to a later slice, stubbed honestly). ADR-0008 accepted (operator go 2026-07-07); rollout (a)+(b)+(c1) shipped, (c2 advance-execute)/(d variants)/(e STATE.md retirement + doctor model-conformance check) PENDING. Live dogfood finding: the Stage-2 `principles-doc` gate false-negatives on this repo — a naive presence-scan miss that *validates* the ADR's "gate authoring is real work" cost with a running example, not an assertion. Verification: lib self-test PASS; `blueprint stage status` + `stage advance` (dry-run, correctly BLOCKED at Stage 2, exit 1) PASS; unknown-model fallback PASS; `doctor` unaffected (WARN→exit 0); frontmatter-lint clean. Freeze acknowledgment: fleet audited before touching `template/` — 13 consumers, 3 behind / 10 unpinned, **0 mid-migration** (behind ≠ migrating), so the freeze is not tripped. Consumer-sync: additive — `stage` is a new read-only command + a new lib; consumers inherit it on next methodology pull; no schema break (`stage_model:` is optional, defaults to greenfield).
- Wave 81 — **`stage advance` gets teeth: entry-guard + `--execute` state recording (ADR-0008 rollout step c2).** Wave 80 shipped `advance` as a stubbed dry-run; this makes it gate real transitions. **The inversion completed**: `advance` computes the next stage's entry-guard and refuses the transition unless every *derivable* gate passes AND every *non-derivable* (agentic-shell) gate has a recorded assertion — the "program requires the guard to pass, then records" shape, vs. "the LLM decides it's done." **Assertion model**: `--assert-<gate>="evidence"` records an operator/reviewer confirmation for a shell gate (sensor-drove-the-app, claims-verified, url-reachable); a recorded assertion satisfies a non-derivable gate but **never** a derivable one the disk contradicts (`deriveStageStatus` folds assertions only for `derivable:false` gates — you cannot assert your way past a mechanical check). **State**: `--execute` persists `{cursor, advancedTo, assertions, history}` to `.blueprint/stage-state.json` (ADR-0008 decision #3 — the machine-read state file that replaces the drift-prone hand-maintained STATE.md for the one thing a program should own); dry-run by default (terraform-plan idiom), `now` injected for deterministic testing. `stage status` reads the state file and folds recorded assertions (shell gates show `asserted: <evidence>`) + surfaces the recorded cursor. Verification: lib self-test PASS (added: assertion satisfies a non-derivable gate; assertion does NOT override a derivable gate; recordAdvance dry-run blocks on this repo's derivable gap and writes no file); live fixture PASS (blocked→`--assert`+`--execute`→state file written→`status` folds it, cursor advanced); real-repo `advance` correctly BLOCKED on the derivable `principles-doc` gate with "cannot assert" messaging, human + `--json` exit 1; `doctor` WARN→exit 0; real-repo status unchanged (11/14). Freeze acknowledgment: fleet re-audited — 0 mid-migration, freeze not tripped. Consumer-sync: additive — `advance` extends the existing `stage` command; `.blueprint/stage-state.json` is created only on `--execute` (operator choice whether to commit it, like STATE.md); no schema break. **REVIEW FIX (same session, pre-push):** the first cut of `advance` was *unsatisfiable on the shipped greenfield model* — `stagePass` checked only derivable gates, so the frontier was always a derivable-blocked stage and shell-gate assertions were inert dead code (the only "success" was a degenerate all-manual custom stage, which then wedged the cursor + duplicated history). Four findings fixed: (1)+(2) completeness now folds ALL gates (non-derivable satisfied by a recorded assertion), split into `artifactCursor` (disk reach, derivable-only) vs `cursor` (confirmed, all gates); `advance` targets the *fixed frontier* computed from already-recorded assertions and records incrementally (no goalpost drift, no wedge); (3) `readStageState` distinguishes absent from corrupt and `--execute` refuses to overwrite an unparseable state file (was silently discarding recorded assertions); (4) `--assert-<gate>` ids validated against the model's shell gates + evidence-less bare flags rejected. The greenfield success path (assert Stage-0 `sensor-wired` → cursor advances) is now exercised in the self-test — the path the broken cut could never reach. A second review pass caught two residual input-validation gaps in the same class, both fixed: empty/whitespace assertion evidence (`--assert-x=`) slipped the `=== 'true'` bare-flag check and recorded an evidence-less confirmation (now rejects `!evidence.trim()`); and a valid-JSON-but-non-object state file (`[]`, `null`, `"…"`) escaped the corrupt guard and got clobbered (now a `isValidStateShape` check flags it, refusing overwrite). Both reproduced before fixing; `isValidStateShape` is self-tested.
- Wave 82 — **Stage models for the other three variants: `stage status|advance` now works for midstream/brownfield/research (ADR-0008 rollout step d).** Wave 80/81 shipped the `stage` machine but only the greenfield model; the other three fell back to greenfield with a note. Now all four ship, encoded faithfully from `docs/variant-selection.md` (§ Stage shapes per variant + § Required sub-deliverables) rather than reasoned from first principles: **midstream** (Targeted Diagnose → Prescription → Design Principles → Prototype-as-Patch → Fact-Check → Documents → Deploy+Iterate; Prescription precedes Design Principles per the canonical rationale), **brownfield** (Diagnose → Prescription → Design Brief → *optional* Prototype → Fact-Check → Documents → Deploy+Iterate; the numbered `01-diagnose`/`02-prescription`/`03-design-brief` artifacts are the deliverables), **research** (Inputs Intake → Personas/JTBD → Research → Synthesis/Decisions → Fact-Check → Decision Memo → Deliver → Iterate; **no Stage-0 sensor** — there is no app; Fact-Check is cross-asset reconciliation, non-derivable). **`loadStageModel` now follows the declared `blueprint.yml` `variant:`** when no explicit `stage_model:` is set — the field already exists and means exactly this, so consumers declare the shape once, not twice. Two mechanism additions: a **`any-exists`** check kind (pass if any listed path is a file or any listed dir is populated — the "sub-deliverable dir present" / "portal OR prototype shell" gate shape), and an **`optional` gate flag** (brownfield's optional prototype + research's iterate: absent/partial reads as pass so an optional stage never wedges the confirmed cursor, while a present artifact still shows a real pass). Verification: self-test PASS (now validates all 4 models — every gate references a known kind, carries a boolean derivable flag, unique ids per stage; 9 check kinds); live fixtures for all three variants derive correctly and auto-select from `variant:`; brownfield optional-prototype fixture builds *through* Stage 4 without wedging; greenfield status/advance unchanged; doctor WARN→exit 0. Freeze acknowledgment: additive change (three new built-in models + one check kind + one gate flag); fleet re-audited, 0 mid-migration. Consumer-sync: additive — a consumer that declares `variant: midstream|brownfield|research` now gets its real pipeline from `stage status` on next pull instead of the greenfield fallback; greenfield consumers unaffected; no schema break. **REVIEW FIX (pre-push):** the review caught that research Stage 2 and brownfield Stage 1 gated on a non-recursive top-level `dir-md-min` while the canonical layout keeps those legs in *subdirectories* — so a correctly-structured initiative wedged the frontier (derivable gate, un-assertable). Both now gate on the leg subdirs with **one `any-exists` gate per leg** (AND semantics — all legs required, matching midstream Stage 1's shape and the canonical "≥3 legs" / "all five populated"; a single collapsed gate would confirm the stage on one leg, and since these are derivable gates feeding `advance --execute`, the program — not a downstream reviewer — must own that guard per ADR-0008). Also: `artifactCursor` capped at the last derivable-bearing stage because research's all-non-derivable Fact-Check/Deliver stages had `artifactPass=false` — zero-derivable stages are now vacuously artifact-passing (mirrors the empty-gate `complete` rule; display-only, `advance` was unaffected). Two minors: stale `loadStageModel` comment refreshed; midstream's `DESIGN` pattern anchored (`^design\.md$`) so it no longer matches `redesign-notes.md`. All reproduced before fixing; verified via subdir-layout fixtures for all three variants.
- Wave 83 — **ADR-0008 rollout closes: STATE.md retirement + a `doctor` stage-model conformance check (step e).** The last rollout step, and it makes the stage machine own the two things it should. **STATE.md**: `template/STATE.md`'s tiered-usage table gains a **"Stage / pipeline position"** row — skip-condition "Always (the stage machine derives it)", use-instead `blueprint stage status`. Never hand-maintain "we're at Stage N" in STATE.md; the machine derives it from artifacts-on-disk + recorded assertions, and a hand-copied cursor drifts (the exact multi-role-file anti-pattern STATE.md's own header warns against). **`doctor` stage-model check (the 12th)**: the declared `variant:` / `stage_model:` must resolve to a REAL model and `deriveStageStatus` must run without throwing (which proves every gate references a known check kind). A misdeclared `variant: midstreem` that silently falls back to greenfield is now a first-class **WARN** with the fallback note — not an invisible wrong-pipeline. This is ADR-0008's premise applied to the config itself: the program owns the guard, so the false-green surfaces mechanically instead of at Stage 5 when the wrong deliverables show up. Count-claim ripple handled: the two "`doctor` … 11 checks" claims (CLAUDE.md standing-state, ADR-0008 context) bumped to 12; `stateful-claims` doctor check reconciles (mismatches=0). Verification: doctor self-test PASS (9 assertions); `doctor` shows the stage-model check + 12 total; misdeclared-variant fixture → WARN as designed; `stateful-claims` PASS; frontmatter-lint clean; doctor overall WARN→exit 0. **ADR-0008 rollout (a)–(e) is now complete across waves 80–83.** Freeze acknowledgment: additive (one new doctor check + one STATE.md table row); fleet re-audited, 0 mid-migration. Consumer-sync: additive — the stage-model check arrives on next methodology pull and SKIPs cleanly where no blueprint.yml exists; STATE.md guidance is doc-only.
- Wave 84 — **Stage-model calibration against the real consumer fleet: layout-tolerant gates + a coverage metric (the gates passed 0/7 before).** A controlled experiment — run `blueprint stage status` against the seven local consumers (blog/mrr/quantifai/ai-content-engine greenfield, rally-hq/atelier midstream, blueprint-redesign brownfield) — found the wave-80–82 gates derived a sensible cursor for **0 of 7**: every mature initiative (live platforms with prototypes + ADRs + deploy configs) read as "stuck at Stage 0." The self-tests + synthetic fixtures had been **circular** (fixtures shaped to match the gates). This is the wave-59 honest-gate-calibration pattern applied to the stage machine, which had shipped without it. Four mismatch classes, all fixed: **(A) greenfield subdir bug** — greenfield Stage 1 counted top-level `research/*.md` while real greenfield consumers keep legs in subdirs (the exact bug the wave-82 review fixed for research/brownfield but I'd left in greenfield "because the self-app is flat" — the self-app is the atypical one); **(B) `blueprint/` nesting** — rally-hq/blog keep all blueprint work under `blueprint/`, so every root-relative gate saw nothing (NEW `underRoots` helper searches root + `blueprint/`); **(C) `pilot_profile` absent** — pinned mature initiatives to Stage −1 on a missing yml field (now `optional`, deferred to `pilot-profile-lock-reviewer`); **(D) leg-name divergence** — *no real consumer uses the canonical leg names* (`architecture/`, `problem-space/`…), so the strict per-leg-AND gates the wave-82 review insisted on passed 0/7. **This wave deliberately reverses that wave-82 decision on calibration evidence**: NEW layout-tolerant **`research-legs`** kind counts legs as subdirs-or-substantive-files under any name (README + configurable `exclude` skipped); the reviewer (`research-completeness-reviewer`) still owns "the RIGHT legs + primary-source grounding." Also fixed greenfield Stage 2 `principles-doc` to search `prototype/` (canonical `prototype/DESIGN.md`). **Deeper finding — the linear-spine cursor mismodels reality:** real initiatives complete stages NON-CONTIGUOUSLY (research + decisions + deploy done, Stage-2/4 skipped — exactly `case-study-subs-skipped-stages-2-4.md`), so a contiguous spine under-reports. NEW `stagesComplete` coverage metric reported alongside the spine (e.g. rally-hq spine=Stage 1 but coverage=[1,4,6]; the self-app itself is [1,3,5,7]). Result: 0/7 → all 7 now show real non-contiguous coverage (2–3 stages each). Verification: self-test PASS (10 check kinds, 12 gates); doctor WARN→exit 0; self-app unchanged in shape; full re-calibration table captured. Freeze acknowledgment: additive kinds + tolerant params; fleet re-audited, 0 mid-migration. Consumer-sync: additive — tolerant gates + coverage arrive on next pull; strictly MORE consumers derive a sensible position than before; no schema break. **REVIEW FIX (post-push):** the review caught three real gaps, all reproduced then fixed. (1) `research-legs` counted EMPTY leg subdirs — the research stamper creates `research/problem-space/` etc. empty, so Stage 2 read pass at stamp time with zero research (a false green feeding `advance --execute`); a leg subdir must now be POPULATED (>=1 non-hidden entry). (2) The `underRoots` first-existing-wins resolver let an empty root `research/` stub SHADOW real work under `blueprint/research/` — replaced with per-candidate evaluation running each gate against BOTH roots and taking the best result (content wins over existence; this also improved the calibration — rally-hq now finds its `blueprint/`-nested work, spine Stage 1->2). (3) The new `stagesComplete` coverage metric counted optional-only stages (an empty brownfield reported `[4]` via the optional prototype); a stage now counts only if complete AND has >=1 non-vacuous pass. Plus the behavior self-tests the prior (circular) tests lacked: real fs fixtures assert empty-vs-populated legs, no-shadow multi-root resolution, and vacuous-coverage exclusion.
- Wave 85 — **Pattern B consumer-hardening: nine defects from the ai-enablement consumer, one root cause, one encoded fix (the stamp-then-gate smoke test).** The ai-enablement initiative (nino-chavez/nino-chavez-site, brownfield → Tier 2 shipped 2026-07-09) was the first consumer to run the Pattern B initial stamp end-to-end since amendment 1 (2026-06-27) implemented it — and logged nine defects in one day, all invisible until a consumer paid for them, all sharing one root: **the Pattern B path had never been executed against its own gates.** Fixed this wave, each with the consumer's amendment as the audit trail: **(1)** `stamp.mjs` Pattern B branch crashed on a `log` TDZ ReferenceError (the outer Pattern A `const log` in its dead zone) — every fresh Pattern B stamp failed; block-scoped log. **(2)** The imposition layer (`.claude` reviewers + `tools/lib` + `run-reviewers.mjs`) was Pattern A-only (the 2026-06-16 install-gap fix never reached the review branch) — Pattern B consumers got a portal with no stage gates; now copied before `stampPatternB`. **(3)** copyTree applied substitutions (incl. the wave-11 data-theme swap) to chrome-manifest files, so `docs/index.html` drifted ~1 line from canonical on every stamp and `portal-chrome-canonical-reviewer` BLOCKed an untouched consumer; the Profile A manifest is now re-copied byte-identical after substitution. **(4)** `canonical-primitives.css` (the Profile B import) was stamped at the portal root where the conformance reviewer correctly rejects it as an orphan under default Profile A; no longer stamped — Profile B consumers pull it via `restamp-chrome --accept-overwrite`. **(5)** `wrangler.toml` rode the binary copy path (`.toml` ∉ TEXT_EXTS), preserving a literal `PROJECT_SLUG-blueprint` that failed every consumer's first `wrangler pages deploy`; `.toml` joins TEXT_EXTS and `PROJECT_SLUG` joins the substitution table (a bare `PROJECT_NAME` rule was deliberately NOT added — it would clobber Layout.astro's JS identifier; the Pattern B shells now use the covered `REPLACE_FOR_PROJECT_NAME`, incl. `_meta/index.json` name/tagline, which previously leaked `PROJECT_NAME` into the stamped brand bar). **(6)** `mechanicalCheck` scoped only `apps/portal` + `packages` — VACUOUS for Pattern B, "PASS" shipped source strings untested; now scans `portal/` + `blueprint/portal/`, and immediately caught two `cx-strategy` doc-slug leaks in canonical chrome comments (reworded). **(7+8)** the front door (`index.html`) baked the source project's hero ("One front door, five paths in."), stat tiles, and a five-path grid with foreign doc slugs into HTML, AND referenced an undeclared `MANIFEST` — the ReferenceError killed the script (frozen tiles, empty grid, dead footer; the consumer's operator met "five paths" on a deployed portal and asked what they were). Now fully manifest-driven via a new `front_door:` block (label/h1/lede/tiles/paths; derived pages/slices/docs counts; neutral fallbacks; same contract as `docs.tiers[]` + `footer:` — CONVENTIONS.md v7 documents the schema). **(9)** `proto-nav.js` has emitted footer/lifecycle/phone-frame classes since v2/v5 with no canonical CSS — every consumer rendered an unstyled footer; token-driven rules added to `shared.css` so the `[data-theme]` registries apply. **The encoding (First Principle):** `template/tools/blueprint-init/smoke.mjs` — stamps Pattern B into a temp dir and asserts: exit 0, imposition layer present, `portal-chrome-canonical-reviewer` PASS **from the stamped tree**, `portal-review-conformance-reviewer` not BLOCKED, wrangler deployable, Pattern A dry-run sane. "The stamper's output passes the stamper's gates" is now a mechanical property, not a hope. Verified: smoke green; fresh-stamp front door boot + screenshot shows correct brand/hero/tiles; CONVENTIONS.md bumped v6→v7; deploy gotchas documented (secrets bind on the NEXT Pages deployment — set secret, then deploy). Freeze acknowledgment: no external consumer mid-migration (ai-enablement completed + shipped before this wave; fleet otherwise at rest). Consumer-sync: Pattern B consumers restamp chrome to pick up the footer/lifecycle rules + reworded comments (audit-chrome will classify as LAG); pre-wave-85 stamps should hand-fix `wrangler.toml`'s `PROJECT_SLUG` or re-stamp; `front_door:` is additive (absent block → neutral fallbacks). Registry: `nino-chavez/nino-chavez-site` entry corrected (Pattern B, 0.5.0, Tier 2, synced 2026-07-09). Source amendments: METHODOLOGY-AMENDMENTS.md in the consumer repo (5 entries, 2026-07-08/09).
- Wave 86 — **External-audit response: the gates that existed get consistently wired (9 fixes, 2 external review rounds, conditionally approved plan v3).** An external audit (Codex, 2026-07-11) landed one diagnosis this repo had to accept on its own terms: *mechanical gates must be consistently wired, not merely present* — doctor skipped Pattern B trees, CI ran doctor only while the portal typecheck sat red on main, the stage machine's pilot gate was vacuously satisfiable, and ADR-0008 claimed a rollout "complete" whose dispatch clause never shipped. Every audit claim was re-verified against source before acting (three of eight were documented calibration decisions, not defects; two were WORSE than reported). The fixes: **(1) portal typecheck red** — `states.astro:20` union-narrowing fix (`'optional' in g`); `astro check` green. **(2) CI three-gate split** — `doctor.yml` grows `core-tests` (bare-node: 7 lib self-tests + pilot reviewer self-test + smoke, single source `npm run test:core`), `template-health` (REAL Pattern A stamp → install → typecheck → build of the stamped fixture), `portal-health` (self-app portal; NOT a release gate — apps/portal is not in the npm `files` array); `release.yml` publish now `needs: [core-tests, template-health]` because the npm artifact ships `template/`. Template-health path executed locally before first CI run (the wave-85 lesson applied to CI itself). **(3) doctor Pattern B blind spot** — check 6b: a chrome-marked `portal/` or `blueprint/portal/` tree runs BOTH `portal-review-conformance-reviewer` + `portal-chrome-canonical-reviewer` from the methodology home (bespoke keeps the divergence-ADR gate); smoke asserts both reviewers *executed* on the stamped tree so a skipped check can never read green. **(4) pilot gate substance** — the audit found presence-only checking; verification found worse: `init` never wrote the block at all, so "absent→legacy" would have grandfathered every fresh scaffold (external reviewer caught my v1 fix shape). Fix per plan v2/v3: `writeBlueprintYml` stamps `pilot_profile_policy: required` + the empty 7-field block (non-research only); new `pilot-profile` check kind — policy=required + unfilled/missing → BLOCKS advance (citation must resolve to a real file); POLICY-field absence (not profile absence) is the legacy exception, so the wave-84 calibration holds; `pilot-profile-lock-reviewer` reads the same policy (legacy: WARN not BLOCK) so the deterministic core and the reviewer cannot disagree on the same tree — the audit's original "control systems disagree" finding closed at the root. Integration-tested against actual `init` output in smoke (fresh stamp blocks → populated profile passes). **(5) chat proxy partial abuse mitigation** — `/api/chat` was an open proxy on the consumer's OpenRouter key once enabled: same-origin check (documented browser-only — headers spoofable outside browsers), caps before parse/upstream (64KB raw body, 30 msgs, 4000 chars/msg, 48000 total), `max_tokens` server-fixed at 1024 (was 2048, violating the OWNER-SPEC's own >1500 danger zone); OWNER-SPEC gains § Security posture: EXPERIMENTAL, default-off, OpenRouter key spend cap is the REQUIRED backstop; real access control (Turnstile + stateful rate limit) → wave-87 ADR. Explicitly NOT access control, per external review. **(6) chat widget honesty** — subtitle/greeting/suggestions derive from `_meta/index.json` via pure `deriveChatMeta` (zero docs → "Docs not published yet", never "Claude has read the docs"); killed THREE baked-in leaks: the false read-attestation, the source project's greeting doc-list ("CX strategy…" — prose residue that evades slug tripwires), and the source quick prompts ("viral loop", "Phase 3"); optional `chat.suggestions` manifest block (CONVENTIONS.md v8, zero/custom/absent cases smoke-tested). **(7) ADR-0008 addendum** — decision #2's dispatch promise vs the shipped validate-and-record `advance`, named explicitly: verifying-reviewer dispatch SCHEDULED (wave-87 ADR: machine-readable transition→reviewer mapping, per-reviewer WARN semantics, .md-only reviewer behavior, input-glob content fingerprints + reviewer-file hash for assertion freshness); producing-skill dispatch DECLINED (harness coupling vs the ADR's own scope discipline); stale "Read-only today" comment in `bin/blueprint.mjs` fixed. **(8) prose reconciliation** — METHODOLOGY.md: the stage spine is a *reporting frontier*, not an execution order (docs/prototype simultaneity stands); self-app `blueprint.yml`: the real WHY for `stage_model: greenfield` under `variant: brownfield` (artifacts are greenfield-shaped; the brownfield model's numbered-artifact gates would wedge). **(9) contract copy** — README headline gains the steering clause (agent executes / operator steers / gates verify — the one overclaiming sentence); `/learn/getting-started` gains "What Blueprint is — and isn't" (three-role contract); `init` prints a variant-aware next-steps epilogue after successful non-dry-run stamps (stamped consumers never see the hosted /learn). Declined from the audit, with cited grounds: progressive-complexity reduction (premature for the declared pilot audience) and SaaS auth/tenancy (charter scope ceiling) — hosted chat stays default-off + experimental per (5). Wave 87 queued behind TWO ADRs (external review's split): reviewer-wired `advance`, and Pattern B readiness states (`shell`/`content-ready`/`stakeholder-ready` — preview deploys WARN, stakeholder deploys BLOCK, enforced in doctor + the deploy path, not just CONVENTIONS). Verification: `npm run test:core` green end-to-end (7 lib self-tests incl. new pilot-profile policy states; reviewer self-test incl. policy-aware §1; smoke 9 steps incl. doctor-executed-both-reviewers, real Pattern A stamp, pilot-gate integration, deriveChatMeta cases); template-health locally green (stamp→install→typecheck→build); `astro check` 0 errors; doctor on self-app unchanged (bespoke branch, no double-report); workflows YAML-validated. Freeze acknowledgment: fleet re-audited 2026-07-11 — 3 behind / 0 mid-migration, freeze not tripped. Consumer-sync: doctor 6b + stage-model kind are additive on next pull (legacy trees derive identically — policy-field absence preserves every wave-84 calibrated cursor); `pilot_profile_policy` reaches only NEWLY stamped initiatives; chrome files (`chat-widget.js`, `functions/api/chat.js`, CONVENTIONS.md) are LAG for existing Pattern B consumers via `--mode=audit-chrome` → `restamp-chrome`; pre-wave-86 deployed chats should set an OpenRouter key spend cap immediately.
- Wave 87 — **ADR-0009 ships: reviewer-wired `advance` (ratified same-day with ADR-0010; rollout a–d complete).** Operator ratified both wave-86 ADR drafts ("ratify both", 2026-07-11); this wave implements ADR-0009 end-to-end for the calibrated gate. **The mechanism**: stage-model gates gain an optional `reviewer: { name, onWarn }` mapping (machine-readable, in the model data — the `template/CLAUDE.md` prose table is now explicitly a rendering, per ADR-0009 #1); at the frontier, `recordAdvance` (now async; `bin` awaits + passes `home`) verifies every mapped gate: a recorded PASS is REUSED only while the reviewer file's sha256 AND the fingerprint of its declared `inputs` globs both match (`fingerprintInputs` — minimal dialect: `dir/**` recursive or exact path; sorted relpath+content-sha lines; no `inputs` export → never reused, always reruns — safe default); anything else runs the reviewer live (read-only, so dry-run runs them too and shows what `--execute` would record). PASS → recorded to the new `stage-state.json` `reviews:` field (additive; `isValidStateShape` extended); WARN → per-gate `onWarn` policy (`pass` = advisory + recorded, `block`/`ask` = refused — no global WARN rule); BLOCKED/THREW/INVALID → transition refused with findings; **mapped-but-UNRESOLVED reviewer → hard stop** (a silently skipped reviewer is the wave-55 false green; self-tested). WARN results are recorded for audit but never reused — advisory findings must re-surface. **Mapped this wave**: `pilot-profile` → `pilot-profile-lock-reviewer` (`onWarn: pass` — legacy WARN must not block) in the three product-variant models; the reviewer exports `inputs: ['blueprint.yml', 'research/**', 'decisions/**']`. **Deliberately staged**: research-legs/principles-doc/portal mappings expand per-gate only after fleet calibration — wave 84 proved strict gates shipped uncalibrated pass 0/7 real consumers; the ADR's Status section records this. Verification: stage-model selftest green (fingerprint determinism/sensitivity/scoping, reviews state-shape, mapped-reviewer-ran assertion, UNRESOLVED-refuses assertion); smoke 8b end-to-end on the REAL stamped tree + REAL reviewer (dry-run runs reviewer → `--execute` records fingerprint → rewound frontier reuses fresh PASS without rerun → mutated `pain_point` forces rerun); live CLI dry-run on the self-app renders `✓ pilot-profile reviewer pilot-profile-lock-reviewer: PASS`; test:core green; doctor unchanged. Freeze acknowledgment: fleet re-audited 2026-07-11 — 3 behind / 0 mid-migration, not tripped. Consumer-sync: additive — `reviews:` appears in stage-state only when a mapped gate advances; consumers pull the mapping + async advance together (bin+lib ship as one); un-stamped initiatives resolve reviewers from the methodology home, stamped ones from their imposition layer (canonical wins). **REVIEW FIX (same session, pre-push):** the post-commit review caught three freshness-guarantee gaps, all fixed + self-tested: (1) declared inputs under-covered the verdict — `inputs` now supports a FUNCTION form `(root)=>paths` so the pilot reviewer fingerprints its dynamic `walkthrough_citation` target (+ `blueprint/decisions/**`, the alternate ADR layout it scans); absolute citation paths hash under their raw label instead of being join-mangled out of the hash; the §7 git-recency check is recorded in ADR-0009 § Known limits as inherently outside content fingerprints. (2) Zero-match globs returned the constant empty-digest — a typo'd glob would stay "fresh" forever; now null → always rerun. (3) The confirmed-cursor jump could pass stages whose mapped reviewers never ran (latent until the mapping expansion); `advance` now walks every stage from the frontier through the new cursor and verifies each mapped gate.
- Wave 88 — **ADR-0010 ships: page readiness states + intent-gated deploys + chat default-off (rollout a–e; turnstile serving deferred to d2).** Second half of the same-day double ratification. **(a) Readiness states**: page metas (`_meta/<page-id>.json`) declare `readiness: shell | content-ready | stakeholder-ready`; the stamper's example page ships `shell`; ABSENT field = legacy-undeclared (pre-ADR-0010 consumers must not wedge — the pilot-gate policy-discriminator pattern reapplied). **(b) Verified promotion**: `portal-review-conformance-reviewer` check 4c — a page declared `stakeholder-ready` BLOCKs if its HTML still contains placeholder tripwires ("Replace this hero"/"Replace this body"/`REPLACE_FOR_`) or its title/summary/strategy.decision are empty; a hand-edited state that fails verification is a finding, not a state. **(c) Intent-scoped deploy gate**: `prep-deploy.sh --intent=preview|stakeholder` (default preview) — preview + shell → WARN and proceed; stakeholder + ANY shell → exit 1 listing the pages; content-ready and legacy pages WARN-listed at stakeholder intent. New doctor check (13th, `portal-readiness`): the census reported on every run for Pattern B trees (count-claim ripple handled: CLAUDE.md/AGENTS.md standing-state + ADR-0008 context bumped 12→13). **(d) chat.access modes**: manifest `chat.access: off | open-capped | turnstile`, default OFF — the widget does not mount AND `/api/chat` 403s without the manifest opt-in, so a bound key alone can no longer serve an endpoint the deployment never enabled; `open-capped` = the wave-86 posture, and a stakeholder-intent deploy additionally requires `spend_cap_attested: <date>` in `chat.OWNER-SPEC.md` (template ships `none`); `turnstile` is schema-recognized but its widget half is NOT shipped — the widget won't mount it, the function rejects it, stakeholder deploys refuse it — deferred to a d2 follow-up rather than shipping an unverifiable half-integration (CONVENTIONS v9 documents all of this). **(e) smoke step 10** proves the loop on the real stamped tree: shell census → preview proceeds → stakeholder BLOCKs → hand-promotion with placeholders intact → conformance BLOCK → real content → clears → stakeholder deploy passes; `chatAccessMode` default-off cases. **Also in this commit — wave-87 post-commit review, three freshness fixes** (see the wave-87 entry's REVIEW FIX note): function-form `inputs` fingerprinting the dynamic citation target, zero-match-glob → null, and the cursor-jump reviewer walk. Verification: smoke 10 steps green; stage-model + reviewer selftests green; doctor on self-app 13 checks, stateful-claims mismatches=0; test:core green. Freeze acknowledgment: fleet unchanged since the same-day wave-87 audit — 3 behind / 0 mid-migration, not tripped. Consumer-sync: readiness + chat.access are additive schema (absent = legacy WARN / chat off); **BEHAVIOR CHANGE for deployed Pattern B chats**: restamping chrome picks up default-off — consumers with live chats (nino-chavez-site) must add `"chat": { "access": "open-capped" }` to `_meta/index.json` and attest the spend cap to keep chat serving; prep-deploy is chrome, restamp per usual audit-chrome → restamp-chrome flow. **REVIEW FIX (same session, pre-push):** the post-commit review found four gaps, all reproduced then fixed + smoke-covered (step 10b): (1) the spend-cap attestation gate was defeated by the template's own default line — a greedy capture read `none    # set to <YYYY-MM-DD>…` as an attestation; the gate now strips inline comments (and smoke covers the open-capped path it previously never executed). (2) An invalid readiness value (a promotion typo) downgraded to legacy-undeclared WARN while the conformance reviewer BLOCKs the same tree — the control-systems-disagree class again; invalid is now its own census bucket, BLOCKing stakeholder deploys in prep-deploy and reported distinctly by doctor. (3) A stakeholder-ready meta whose page file is missing skipped the placeholder check entirely ('verified' degraded to declared); now BLOCKs. (4) The chat access gate self-fetched the manifest with plain fetch — behind Cloudflare Access that gets the challenge page and 403s an opted-in chat; it now uses the env.ASSETS binding with plain-fetch fallback, same as loadContext. Fix (4) is verified by pattern-match to loadContext's established ASSETS idiom, NOT smoke-covered (the binding doesn't exist in bare-node smoke) — fixes 1–3 are the smoke-covered ones. Plus a shell-quoting bug in fix (2)'s first cut (single quotes inside the single-quoted node body broke every stamped prep-deploy invocation) — caught by smoke before commit, which is the point of running the gates before the gates run you.
- Wave 89 — **decisions/05 ships: the actor-output contract (candidate B) replaces the 6-verb portal IA; step 1 of the build order lands (`actor-output.mjs`).** The full rederivation ran as research-first: fleet evidence inventory (7 verdicts — the verbs organized the builder's content, not any reader's job; the switcher was dead weight solo and a clearance hazard with counterparties; the originating consumer had itself deleted the contract the fleet gate still enforced) → three candidate structures compared across six consumers → candidate B ratified after an **executed falsification pass** (`research/portal-ia-rederivation/00–06`): four specimen manifests authored from real consumer facts, five real negative fixtures, an experimental validator proving all eight rule families mechanically checkable, three rounds of external adversarial review (Codex) incorporated verify-before-concede, Codex independently reran the suite. Operator ratified all four calls: candidate B; `actors:` terminology (people/agents who read, operate, decide, configure, receive); measured ceremony floor (~15–20 intrinsic / 70–91 fully declared); four-consumer first wave (film-room + blueprint-self active, se-docs-frontdoor configuration-first case, bc-subscriptions read-only compatibility). **Shipped this wave**: `template/tools/lib/actor-output.mjs` (7th dependency-free lib; wired into `test:core`) — YAML-subset parser (comments are load-bearing in manifests), the 8 rule families (refs, lifecycle+serving, recipient-safe-as-proven-state incl. as_of/destination, structured receipts, proof-grade/actor-kind compatibility, typed preconditions, path resolution incl. de-named `local:` indirection, legacy/new routing), and the three-state verdict PASS/PENDING/BLOCKED — a stage transition never treats PENDING as green; gate mode BLOCKS on unresolvable roots (skipped validation can't produce green); `human_validation: passed` requires an observed-human pass RECEIPT (structured record), never a sniffed string; legacy `validated_by:` is a BLOCK with a migration message; `viewers:` accepted as deprecated alias with warn. Verified: selftest 19 assertions green; run against the four research specimens → all PENDING with honest reasons (planned outputs, FAILING walk) and the five fixtures → BLOCKED on their named rules; full `test:core` green. **Remaining build order (steps 2–6, next waves)**: gate orchestration that actually invokes legacy reviewers on the legacy route; evidentiary recipient safety (selector-level allowlists, executed leakage lint, recorded issuance); account projection + intrinsic outputs; stamper emits intrinsic-only manifests and the 6-verb boilerplate retires; `docs/portal-and-tier-ladder.md` amendment. Count-claim ripple: "six dependency-free libs" → seven (CLAUDE.md + AGENTS.md). Freeze acknowledgment: `blueprint fleet` recorded in the ADR — 14 consumers, 0 mid-migration, not tripped. Consumer-sync: purely additive this wave (a new lib + selftest; no reviewer, stamper, or schema behavior changes until step 2+) — no consumer action required; migration is dual-validation by design (legacy `portal_type` keeps existing reviewers + warn indefinitely until per-consumer migration). **STEP-3 CONTINUATION (same session, pre-push) — the routing shim lands in doctor + first-wave adoption executes.** New doctor check (14th, `actor-output-routing`): an `actor-output.yml` at the consumer root routes validation to the new contract (manifest verdict surfaces as pass/warn/fail — PENDING is a WARN, never green) and the legacy portal-conformance checks report a VISIBLE skip; legacy `portal_type` alone keeps the existing portal reviewers as the gate (doctor already RUNS them — the legacy-route-invokes-reviewers criterion); both declared without `migration: actor-output` in blueprint.yml is a FAIL (the ADR's hard error, with the explicit migration mode as the sanctioned dual state). First-wave adoption executed: blueprint-self (manifest live at root, `manifest:check|gate` npm scripts, portal reshaped — new pipeline-gates logo replacing the bc-blueprint gear, stale `portal_pattern: A` quickstart copy fixed, reader-job footer labels, HarnessNote de-weighting banner on /states + /graph), film-room + se-docs-frontdoor (manifests live, migration mode declared; se-docs' deflection-baseline typed precondition correctly refuses go-live pre-baseline), bc-subscriptions read-only by design. All three gates: PENDING with honest reasons, 0 errors. **FALSE-GREEN FIX**: doctor's self-test block matched only `--self-test` while test:core invoked `--selftest` — the suite green-lit a doctor self-test that NEVER RAN; both spellings now accepted (11 assertions, actually executing), plus an entry-module guard on actor-output.mjs so a parent's --selftest doesn't re-trigger its self-test on import. Count-claim ripple: doctor 13→14 (CLAUDE.md, AGENTS.md, ADR-0008 context); stateful-claims reconciled to the pre-existing consumer-count mismatch only (other thread's uncommitted consumers.yml). Verification: doctor self-test 11 assertions green; full test:core green; doctor on self-app WARN→exit 0 with the route engaged; doctor on film-room shows route + visible skip (its terminology BLOCK is pre-existing, its own thread). **STEP-5 CONTINUATION (same session, pre-push) — account projection + intrinsic derived outputs ship (`account-derive.mjs`, 8th lib).** One derivation core: `deriveProjection` (the stable machine projection — account map resolved against disk with entry counts, actors/outputs summary, decisions index by title, git as_of + recent subjects, and the manifest's LIVE verdict embedded), rendered two ways — the versioned agent boot packet (`derived/boot-packet.json`, schema `actor-output-boot/1`) and the maintainer recovery brief (`derived/recovery-brief.md`, leads with the verdict and lists pendings as the what's-next; stamps itself derived/never-hand-edit). Two-pass derivation on purpose: a fresh clone's first run would otherwise embed a stale self-referential BLOCKED about its own not-yet-written artifact. Consumers get `npm run derive`; film-room's recovery-brief + boot-packet flipped planned→ready with real artifacts — its gate dropped 3 pendings → 1 (only the club's observed-human receipt remains); self-app recovery-brief now the derived artifact (WAVE-LOG + digest stay the substrate); se-docs wired. Count-claim ripple: seven→eight libs (CLAUDE.md, AGENTS.md). Verification: account-derive selftest 8 assertions green (in test:core); all three adopters derive PENDING briefs with honest reasons; full test:core green. **STEP-6 CONTINUATION (same session, pre-push) — every new stamp starts on the actor-output contract.** `stamp.mjs` now writes the intrinsic `actor-output.yml` (maintainer + next-agent, ~18 functional lines — the ratified ceremony floor) on BOTH stamp paths, ensures `decisions/`, and materializes `derived/boot-packet.json` + `derived/recovery-brief.md` via account-derive at stamp time, so a fresh stamp's gate verdict is an EARNED PASS (outcomes served by real artifacts), not a promise; stamped blueprint.yml carries `migration: actor-output` so the still-stamped portal_type chrome is the sanctioned dual state. Smoke gained step 7b (fresh stamp gates PASS in gate mode + migration key present) and immediately caught two real bugs: (1) the stamped proof signal had a top-level comma inside a flow map — rejected by the subset parser exactly as designed; (2) doctor's routing skip was TOO BROAD — it skipped the Pattern B chrome reviewers, but those are RENDERER conformance (mechanical receipts for the surviving review-context output type), not the retired IA contract; the skip now covers only the Initiative Portal IA gate (check 6), and the wave-86 reviewers-actually-ran tripwires pass again. `docs/portal-and-tier-ladder.md` opens with the supersession banner (retired: 6-verb IA + switcher as contract; survives: review-context output type, tier ladder, shells-as-harness); portal-initiative-conformance-reviewer (.md + .mjs) carry DEPRECATED-as-gate headers (legacy-only, do not extend). Verification: smoke green incl. 7b; doctor self-test 11 assertions; full test:core green. Build order 1/2/3/5/6 of decisions/05 are now COMPLETE; step 4 (evidentiary recipient safety — selector-level allowlists, executed leakage lint generalizing the GSI sanitizer, recorded issuance attestation) is the remaining wave work. **STEP-4 CONTINUATION (same session, pre-push) — recipient safety becomes evidentiary (`recipient-safety.mjs`, 9th lib); decisions/05 build order COMPLETE.** R3 checks the declarations; this lib EXECUTES the proof at issuance, generalizing the GSI sanitize.py mechanism: (1) `recipient-safety.yml` policy maps each forbidden CLASSIFICATION the manifest names to concrete patterns — an unmapped classification FAILS the lint (an unexecutable denylist proves nothing); (2) executed leakage lint scans the package hard-fail-BEFORE-issuance with file/line/class hits; (3) `attest` records a structured issuance receipt (sha256 per file, as_of, destination, issuer, timestamp → derived/issuance/) and REFUSES a dirty package or a missing --by (human issuance is a recorded act); (4) `verify` re-hashes the package against the attestation — post-attestation edits and added files fail. Honest boundary documented: selector-level EXTRACTION from mixed-clearance sources stays consumer tooling; the generic layer proves the issued bytes. Film-room wired (`lint:package`/`attest:package`) with a real policy for its four classes — its two REAL club-facing reports lint clean (2 files scanned, 0 hits). Parser fix en route: subset-parser map keys now allow '/' (classification names like `research/:`). Verification: recipient-safety selftest 11 assertions (in test:core); actor-output selftest still green; full test:core green. With steps 1–6 all landed, decisions/05's build order is complete in-wave; remaining wave-89 closure is operator-side: the P4 cold-open walk (blueprint-self's last PENDING), the club's delivery acceptance receipt (film-room's last PENDING), and commits across the three repos.
- Wave 90 — **decisions/05 build-order step 7 ships: manifest-derived portal views (`portal-derive.mjs`, tenth lib) + dual-mode template shell.** Freeze check: `blueprint fleet` — 14 consumers, 4 behind, 0 mid-migration; no waiver needed. Promoted from two same-day hand-reshapes under the contract (blueprint-self; film-room — its `METHODOLOGY-AMENDMENTS.md` 2026-07-20 entry is the ≥2-consumer evidence): a portal page exists only when a declared output serves a declared actor outcome; nav labels name the reader's job; the interim proof is an EXECUTED walk. Mechanics: outputs declare `views: [{path, label, presents?}]`; `portal-derive.mjs` validates (presents must resolve to an output id or account key; duplicate paths error), emits `derived/portal-views.json` (schema `portal-views/1`, as_of commit; stale file removed on no-op), and `--check <dist>` walks the built dist — front door + every view must exist, carry ≥500 bytes, and show no empty-state marker. Template shell dual-mode: `portal-config.ts` loads the derived file (degrade-to-null, never crash); `PortalNav` renders reader-job entries with the audience switcher off when views exist, legacy 7-verb spine otherwise; index renders view cards in place of the verb lanes. Smoke 7c: intrinsic stamps derive zero views (clean no-op — no browsable surface for maintainer + next-agent). Validated three ways: 12-assertion selftest in `test:core`; film-room live case (4 views derived from its manifest, executed walk green on its deployed dist — `npm run walk:portal` wired next to `lint:portal`); fresh-stamp e2e in scratchpad (build green with views file, nav flipped, walk correctly FAILED the tier-0 `/try` config shell while passing the populated `/strategy` — the gate discriminates). Also this wave: film-room portal reshaped + redeployed as the second contract instance (provenance-portal output: recipient-safe, club-identity denylist class, executed leakage lint + simulated-walk receipt; 9 pages → 5, reader-job nav). Physical deletion of the remaining verb boilerplate from `template/apps/portal` stays deferred — the dual-mode shell renders the derived IA without it, and legacy consumers still stamp green.
- Wave 91 — **blueprint-self compliance: the red-team audit's V1–V6 closed on the self-application.** A four-lens persona/red-team pass (skeptical adopter, team onboarder, enterprise intake, self-contract red team) + a director-walk of film-room's scope aid drove the day. Self manifest gains the two undeclared observed actors: `adopter-operator` (npm CLI + learn docs + README quickstart were orphans of the contract's own flagship surface — V1) and `external-reviewer` (Codex, four review rounds; AGENTS.md declared as its `reviewer-mirror` output — V3). New outputs: readme-front-door, cli-package, learn-docs, reviewer-mirror; `product-site` gains `views:` (page-level accountability via step-7 machinery — V5) and the executed walk runs green on the built dist (6 targets). HarnessNote's citation corrected (cited decisions/05 for the Class-H rule that lives in the SSP pattern — V6, the film-room defect class in our own shipped code). Front-door `/discover` CTA re-targeted to /learn (wrong-reader leakage, flagged independently by two lenses). Stale prose reconciled: README's "one portal that serves everyone" and METHODOLOGY's "deployed URL is the primary deliverable" now state the actor-output reality. Gate: PENDING-1 (casual-visitor walk standing RED — operator-side P4).
- Wave 92 — **decisions/06 ships: the steering loop becomes contract.** Ratified same-day ("ratify both") with decisions/07. Six job families (orient / learn-use / steer / audit / receive / operate) adopted as the output interrogation taxonomy with an explicit anti-regression clause — never a navigation contract (that would rebuild the retired 6-verb portal with better vocabulary). Three loop output types: orientation-brief, steering-packet (validator-enforced `asks:`/`authority:`/`capture:`), disposition-record. The projection-tension rule from the director-walk finding: an output serving a decide-*/steer-* outcome with forbidden classes MUST declare `omissions:` — a silently-withheld axis corrupts the decision it serves (ERROR). Validator hardening from the same audit: serving outputs require `artifact:` (live-surface exempt; the rule immediately caught blueprint-self's own boot-packet + harness-views — fixed), citation-existence for unambiguous refs (`*.md` paths + ADR-NNNN; bare "decisions/NN" skipped after smoke caught the false BLOCK on fresh stamps citing the methodology's records). actor-output.mjs selftest 19→28 assertions. New consumer doc: `template/docs/methodology/steering-loop-convention.md`.
- Wave 93 — **decisions/07 ships: Stage 8 (Handoff), actor-gated.** The enterprise-intake lens found "hands off to build" had no stage, no gate, no artifact — the pipeline ended at a demo URL. Stage 8 added to all four variant models with the `handoff-manifest` gate kind: n/a-pass without a receiving actor (kind: team or receive-*/build-intake outcome — a solo tool never pays the ceremony), absent when a receiving actor is declared with no handoff-manifest output, pass when one serves. `team` actor kind accepted, held to observed-human targets. The assembling derivation (handoff-derive) is deliberately trigger-gated on the first consumer that declares a receiving actor — building the generator before any initiative has a receiving org is ceremony without a reader. New consumer doc: `template/docs/methodology/handoff-manifest-convention.md`; METHODOLOGY pipeline + Stage-6 deliverable prose scoped honestly. stage-model: 9 stages, selftest extended (handoff gate 4 assertions). NOT yet released to npm — 0.7.0 shipped earlier today; waves 91–93 ride the next bump.
- Wave 94 — **Scaffold-contamination follow-up to wave 17: the template's placeholder portal logo was bc-subscriptions' brand.** `template/apps/portal/public/project-logo.png` shipped byte-identical to bc-subscriptions' `subs-logo.png` (the recurring-cycle "S") — it leaked into the scaffold 2026-05-25, four days after bc-subscriptions authored the mark, so every consumer stamped without `--logo` inherited a real product's brand as its default (confirmed byte-identical across film-room, se-docs-frontdoor, quantifai-next, fleet-observability, archive/blueprint-platform). Wave 17 swept the subs `WORKER_URL` + strategy skeletons but never checked the binary asset. Fix: replaced the placeholder with the genuine Blueprint mark (the blueprint-grid + node-path the methodology's own `apps/portal` uses), so an un-customized stamp reads as a Blueprint portal awaiting its brand, not as Subscriptions. The `stamp.mjs` `replaceLogo` `--logo` override is unchanged — consumers still supply their own. Already-stamped consumers keep the inherited "S" until they re-stamp or set `--logo` (each a separate brand decision; not swept here). Asset-only; no reviewer/schema/stamper-logic change. Surfaced by the film-room consumer (its portal still showed the "S").
- Wave 95 — **Plain-language & Diátaxis pass codified as a reusable audit + revision procedure.** New consumer doc `template/docs/methodology/plain-language-pass.md`: a self-contained procedure (written to be inlined into an executing agent, not pointed at) that makes any surface's reader-facing copy as plain as its reader needs and organizes each page to one Diátaxis purpose. **Step 1 is reader-calibration** — lay / practitioner / developer bars — the split that stops it shredding load-bearing domain vocabulary on developer-facing docs (a subscriber portal and an integration handoff do not share a bar). It kills *incidental* jargon (internal codenames, methodology vocabulary) but KEEPS precise domain terms, defined on first use, never in a glossary (honors the standing no-glossary rule). Hard constraints: change only prose; rewrite SOURCE not generated output (re-run the generator to verify); preserve interpolations/escapes/contract examples; invent nothing. Distilled from the film-room portal rewrite (2026-07-21 — five pages: home=orientation, console=reference, running-it=how-to, under-the-hood/decisions=explanation) and validated same-day on bc-subscriptions: a four-site run (internal portal, merchant guides, vendor RFP package, developer/handoff/spec canon) where the reader-calibration split proved load-bearing — merchant guides got full plain-language while the PCI contract, vendor estimates, and ADR-cited handoff invariants were held verbatim. Now a two-consumer pattern. Positioned as the REMEDIATION the `terminology-linter` gate lacked: the linter DETECTS internal-jargon leakage and BLOCKS; this pass rewrites plain and reshapes by purpose. `terminology-linter.md` gains a remediation pointer to it (and steers its own glossary-reflex to inline first-use definitions). Doc + pointer only; no schema/stamper/reviewer-logic change.
- Wave 96 — **Reader-facing readiness becomes enforceable (`reader-contract.json` + `encounter-audit.mjs`).** The missing wave-log entry for commit `a5a8577` is restored here so the log again matches the code's own wave references. New stamps declare the surface, reader job, copy sources, required states, and encounter mode; the encounter audit checks rendered copy against the declared source map and refuses source/generated drift or unsupported ready-state claims. The reviewer, doctor route, stamper, and stamp smoke are wired end to end; a missing built surface is an honest WARN on a fresh scaffold, not a false PASS. The same change tightened the wave-95 plain-language procedure around source ownership and installed the reviewer spec in the consumer template. Verification at landing: encounter-audit selftest, doctor coverage, and fresh-stamp reader-contract smoke green. Consumer-sync: additive; existing consumers gain it on restamp and remain unchanged until they declare a reader contract.
- Wave 97 — **Review and disposition becomes a first-class, renderer-independent capability after the Atelier convergence review.** The comparison used two Atelier generations (archived dashboard initiative `6e35dbb`; current public repo `d1aec33114abe330422b25e9c3addd10f83f45ba`) and found the same boundary as Film Room + Adaptive Commerce Content: Blueprint owns exact candidate, reader/ask/authority, untrusted submissions, dispositions, automation authority, and return receipts; a substrate may own identity/presence/annotations/transport/queues; each initiative owns its presentation. New `review-loop.mjs` (13th dependency-free lib) validates `blueprint-review-loop/1`, `blueprint-review-submission/1`, and `blueprint-review-disposition/1`; `blueprint feedback` surfaces PASS/PENDING/BLOCKED and `--gate` refuses unissued, open, unreturned, candidate-laundered, adapter-mismatched, or authority-laundered loops. Doctor validates only declared contracts; Stage feedback delegates to the same validator and requires at least one closed submission (directory presence cannot green-light it); legacy Markdown feedback remains supported. Guidance + three JSON examples ship in `template/docs/methodology/review-disposition-loop.md`; `/blueprint-triage` may classify/propose autonomously only when the contract grants it, while apply remains human/delegation-bound. Three-history replay: Atelier PENDING (adapter exists, issuance/closure unproven), Film Room PASS (one exact mediated founder finding dispositioned and returned), Adaptive PENDING (artifact issued, no self-service adapter/team response). Freeze acknowledgment: `blueprint fleet` reported 15 consumers, 5 behind, 10 unpinned, 0 mid-migration; template freeze not tripped. Consumer-sync: additive/opt-in, no restamp required; author `review-contract.json` when issuing a human/team steering output. Verification: review-loop selftest, three-history replay, stage-model semantic-gate tests, CLI help/default/gate behavior, stamp smoke (including stamped review-loop), doctor, and full `test:core`.
- Wave 98 — **SE Docs retrospective findings become bounded public-correctness maintenance, without promoting retrospective steering claims.** A central top-level YAML scalar parser now handles inline comments and quoted values consistently across the CLI reviewer runner, executable reviewers, and doctor, so `variant: research # comment` cannot silently route as greenfield. Actor-output R6/R7 now preserves an exact absent precondition as PENDING without the raw citation scanner also reporting a missing-file ERROR. Persona-fit review now checks semantic, reader-language-compatible capability traceability rather than requiring the literal phrase “what each persona can do.” The canonical and stamped methodology-amendment entry shapes are reconciled, and research-variant init documentation now matches runtime output and the research portal/tier restrictions. These are generic fixes derived from the completed SE Docs retrospective; they add no SE Docs-specific variant or semantics and make no prospective-validation claim. Variant-transition migration and steering-layer research remain separate candidates. Freeze acknowledgment: `blueprint fleet` reported 15 consumers, 5 behind, 10 unpinned, 0 mid-migration; template freeze not tripped. Consumer-sync: after an authorized release, preview `blueprint upgrade --target=<consumer>` before changing a pin and apply only with operator authorization; stamped reviewer, runner, and documentation files require the consumer’s normal authorized full methodology receive/restamp path, not a Chrome-only restamp. No consumer was mutated in this wave. Verification: YAML-scalar selftest (8 cases), persona-fit positive and negative selftests, actor-output regressions, doctor (17 checks), stamp smoke, immutable SE Docs exact-state replay at `95f6191`, diff checks, and full `npm run test:core`; specification-only transition and steering gates are not claimed executed.
- Wave 99 — **`doc-quality-auditor` gains its `.mjs`: check 5 ("methodology statement for derived data") stops being judgment-only.** ADR-0002 says the executable implements the spec; check 5 has carried CRITICAL severity since the reviewer was authored and its mechanical half was never built, leaving the highest-stakes claim class in a stakeholder document — a derived figure — graded only by the same class of system that produced it. No ADR and no new methodology: this finishes a half-made encoding, which the first principle already demands; the second-instance rule governs *new* checks. Encoded against a documented failure, not a hypothetical: the origin initiative (`docs/case-studies/case-study-pp-cx.md:91`) shipped an external "88%" that was actually 92% **and** a percentage breakdown over an admittedly-incomplete denominator ("if 84.7% of cases are uncategorized, how do you know 59% are invoice inquiries?") — both caught by an external AI review, neither by a gate. Two checks: `derivation-methodology` (BLOCK — ≥2 percentage figures + a line pairing a percentage with an incompleteness marker + no declared derivation) and `figure-attribution` (WARN — a risky-shaped figure `N%` / "N of M" / `$N` / `Nx` in a block with no source, link, footnote, or not-a-claim marker). **The derivation predicate is structural, never lexical** — the load-bearing decision: `case-study-pp-cx.md:93` contains "The methodology answer … resolved the logic gap" in ambient narrative, so a lexical match would pass the exact document it must catch; a declaration is a deliberate `<!-- derivation: <source> — <how computed> -->` comment or a `## Methodology` heading / `**Methodology:**` label. Candidates rejected: a per-figure provenance sidecar with inline tokens (the Kepler atomic-provenance shape — authoring tax exceeds the error class, and Blueprint deliverables rarely have machine-readable sources to re-derive against) and a broad every-figure-must-be-attributed lint (the "27 findings before guards, 5 true positives after" false-positive failure `stateful-claim-lint-reviewer.md` documents). Jurisdiction is the false-positive control: deliverables only (`docs/content/**.md`, `decision-memo*.md`, the root trio's markdown members), as a UNION of existing paths rather than a variant-gated selection — a misdeclared `variant:` would silently narrow the scan set. `02-prescription.yml` excluded (YAML blocks/fences differ); bare `N/M` not treated as a figure (collides with dates/versions); `~`/"approx" do NOT exempt (an approximation is still a world-claim) while `illustrative`/`hypothetical` do. Scope precision: only C2's *shape* is caught. C1 is not detected — a transcription error is invisible to any check that does not re-derive the value; what `figure-attribution` does for C1 is make it **auditable**, by forcing the source to be named so a human or the `evidence-audit` skill can re-check it. This wave binds declarations, not values. **Gate mapping wired in the same wave, once calibration cleared its stated precondition** (it was scoped as a follow-up on the assumption calibration would lag; it didn't). All four variant models' Documents-stage gates carry `reviewer: { name: 'doc-quality-auditor', onWarn: 'pass' }` — greenfield S5 `decisions`, midstream S6 `strategy-docs`, brownfield S6 `package-docs`, research S5 `decision-memo`. BLOCKED refuses the transition and exits 1; WARN proceeds and is recorded but never cached (advisory findings should re-surface); a PASS is reused only while the reviewer hash AND the fingerprint of the new `inputs` export both match. Verified end-to-end on a fixture initiative walked to the mapped gate: WARN-shaped deliverable advanced with "onWarn=pass — advisory, proceeding"; BLOCK-shaped deliverable refused with the reviewer's message surfaced, exit 1. Dry-runs against the two real consumers were read-only and confirmed clean `git status`, but neither exercised the mapping (film-room is pipeline-complete; docracles blocks earlier at Stage 4's non-derivable `claims-verified`) — hence the fixture, and hence saying so rather than letting two green consumer runs imply coverage they didn't provide. The `jurisdiction` export lands so doctor check 12 picks it up; no new doctor check is added. Freeze acknowledgment: `blueprint fleet` reported 16 consumers, 5 behind, 10 unpinned, 1 current, 0 mid-migration; template freeze not tripped. Consumer-sync: additive; existing consumers gain it on their normal authorized receive/restamp path — no consumer was mutated in this wave. **Calibration, stated honestly:** this repo's three in-jurisdiction deliverables are qualitative (`docs=3, figures=0`), so the self-application provides near-zero *passive* signal. Three things stand in: a 21-assertion selftest; a discrimination run against the **genuine** pp-cx prose (`case-study-pp-cx.md:85–95` copied to a temp deliverable → BLOCKED, 4 figures, and the ambient "The methodology answer … resolved the logic gap" sentence sitting one line below the trigger is exactly what a lexical predicate would have accepted); and an isolation probe that caught a real defect the fixtures structurally could not — every passing fixture also carried a link, so the weak `ATTRIBUTION` branches were never exercised alone, and bare `per` ("42% per quarter") silently exempted any block. Those branches (`per`, `from the`, `for example`, `e.g.`, `sample`) were removed and pinned by five isolation assertions; the `.md`'s honest scope now states the accepted set and the block-vs-figure granularity ceiling (a table is one block, so one cited cell exempts every row). **Fleet calibration ran before the wave closed** (it was initially deferred; deferring it would have shipped a gate calibrated only against its own fixtures — the circular-fixture failure wave 84 caught). Two consumer surfaces with real deliverables (film-room 14 docs, docracles 3 docs; 34 figures) produced 21 warnings on the first run. Reading every one found four false-positive sources, all fixed: bare arXiv/DOI identifiers and backtick'd internal artifact paths were unrecognized attribution (the second is Blueprint's *most common* citation form — "~70% of auto-clips need manual cleanup (`analogous-creator-clipping.md`)"); `Nx` matched pure domain vocabulary ("straight 4x cuts" in a video-editing spec) so the shape was removed entirely; and explicit author disclaimers ("**Pricing (proposal, NOT validated):**") were unrecognized, while `proposal`/`proposed` alone was considered and rejected as too ambient. After the fixes: 7 warnings — 5 solid true positives (unsourced measured metrics, an unsourced corpus count, a competitor price with a named vendor and no citation, a derived noise percentage) and 2 the documented block-granularity ceiling. **`derivation-methodology` produced zero findings across all 17 documents** — the intended precision for a BLOCK-severity check. Selftest 21 → 27 assertions, every calibration finding pinned as a regression. No consumer file was modified; the runs were read-only. Origin: the Kepler verifiability scorecard (`docs/case-studies/case-study-verifiability-scorecard.md`), which found this as the one real gap — Blueprint checked that sources exist, not that numbers are right. That case study is corrected in the same wave (it listed ADR-0008's reviewer-dispatch-with-assertion-freshness as SCHEDULED; the machinery is SHIPPED at `stage-model.mjs:725–750` and only per-gate mapping calibration pends). Verification: reviewer selftest (15 assertions), `blueprint review doc-quality-auditor --target=.` PASS with `docs=3` proving real silence rather than an empty walk, `blueprint doctor` (`reviewers-loadable` 18→19, `lint-jurisdiction` still green), and `blueprint stage status` cursor unchanged.
- Wave 100 — **`encounter-audit` stops flagging URL query strings as reader-facing jargon.** Discovered from a consumer: a Let's Pepper social caption carrying `letspepper.com/signup?utm_source=facebook&utm_medium=album&utm_campaign=poblano_2026_finale&utm_content=jalapeno_highlights` tripped `opaque-token`, and because that surface runs `--strict`, the WARN blocked *every* publish through `post-now.mjs` / `post-reels.mjs` — including posts unrelated to the offending item. The only available remedy was appending the six tokens to that surface's `allowTerms` (the pattern `manny_camp` had already established), which is unbounded: campaign ids change every campaign, so the gate re-breaks on the next one and operators learn to whitelist reflexively — the opposite of what a copy gate is for. **`template/tools/lib/encounter-audit.mjs`**: NEW `stripUrlParams()` removes `?…`/`#…` from URL-shaped runs before `opaqueTokens()` extracts, on the reasoning that a query string renders as a clickable link and is machine plumbing, not vocabulary the reader parses. The **path is deliberately preserved** — a segment like `/second_screen` is genuinely reader-visible jargon and still fires; likewise bare prose tokens (`snake_case_token`) and `ADR-\d{3,}` references are unaffected. Consumer fallout: Let's Pepper's `reader-contract.json` needed **no** change at all and reverted to its committed state — the defect was in the detector, never the contract. Verification: unit-checked the five discriminating cases (utm stripped / path jargon preserved / dates untouched / ADR preserved / bare token preserved); ran the audit across all 13 consumers with a `reader-contract.json` before and after — **every verdict byte-identical** (11 PASS, `website-nc` 6-finding WARN, `adaptive-commerce-content` 2-finding WARN), so the change removes false positives without silencing a single real finding. Propagated to all 18 vendored copies; drift re-checked at 0. Freeze acknowledgment: `template/` was clean at edit time, no external consumer mid-migration, operator authorized the amendment explicitly. Consumer-sync: copy `tools/lib/encounter-audit.mjs` from the template; no contract or config migration required, and any `allowTerms` entry that exists only to silence `utm_*`/campaign ids can be deleted.
- Wave 101 — **Reader clarity is placed inside an explicit writing-decision stack rather than treated as a universal style pass.** A cross-register audit of Signal Dispatch, Slack, and email exposed the same failure the Mom Test pattern already names: a style rule can launder an unsupported claim or turn process theater into apparent intellectual movement. `plain-language-pass.md` now orders truth/evidence → reader/job → argument → cognitive load → voice → surface mechanics; defines “CTE-based writing” as local shorthand for respectful low-memory-load prose, not a formal framework or grade-level target; keeps Minto/SCQA as argument aids and Diátaxis as documentation-only; demotes counts, sentence lengths, and question rates to diagnostics unless a local contract promotes them; and forbids presenting session steering as a changed POV without changed evidence. The Mom Test pattern points back to this separation, and the Blueprint voice example replaces its inherited sentence quota with the same cognitive-load and scope discipline. Freeze acknowledgment: `blueprint fleet` was run before editing and reported 15 consumers (5 behind, 10 unpinned); the operator explicitly authorized codifying the guidance across canonical sources and mechanical consumers. Consumer-sync: additive documentation change; consumers inherit it on their normal authorized methodology receive/restamp path. Verification: full `test:core` plus documentation conflict scan.
- Wave 102 (`ed83d0b` → `d20cb83`, 7 commits, 2026-09-01) — **A rendered surface is verified against the rendered frame, and a redesign owes divergence before convergence.** Minder build 12 was reviewed twice against source, tokens, and passing tests, both reviews concluded "no revision needed", and the screen still lost a side-by-side to a one-screenshot chat prototype. The gap was at every layer: Stage 2 named a divergence venue and built none; `METHODOLOGY.md` deferred judgment claims to "the next consumer initiative", which a solo app never has; audit discipline's "grep the actual file" sent design reviews to source instead of the device frame; closeout was additive only. New canonical `template/docs/methodology/judged-screen-pattern.md`: authority by claim type (appearance → rendered frame, interaction → observed device behavior, accessibility → tree plus behavior, implementation → source and tests); `design_intent: preserve | refit | rethink` in `blueprint.yml` (only `rethink` owes two or more whole-screen concepts on the same states plus a human-attributed selection ADR; `refit` owes the experience brief and object/action/state matrix; `preserve` owes a direction pointer); two review kinds, `cold` (reviewer has seen no rationale, is not the implementer) and `conformance` ("did we build the selected idea?"); an evidence-ladder rung `rendered whole reviewed cold`; six acceptance receipts, none of which implies another; a subtractive closeout item; UI tests assert state and role, not copy, until a content read lands. New `screen-composition-reviewer` (mechanical half: per-surface cold and conformance records with `kind`, `cold`, `implementer` ≠ `reviewer`, captures committed not in tmp; `screen_review_policy: strict` in `blueprint.yml` promotes WARN to FAIL) and `design-principles-reviewer` checks 10–11 scoped by intent; both in `test:core`. `audit-discipline.md` gains the rendered-surface authority table; `confident-preview-rule.md`, `handoff-manifest-convention.md`, the stamper, and `prototype-smoke-runner.md` point at the pattern. Precedents cited at source: film-room ADR-0010/0042, forge-site 3.5, design-qa, the blog's 11-variant portal. Pilots: Minder (`rethink`; S0–S6 run, three concepts, ADR-0017 selected Marquee in both appearances) and yawn-app (`refit`, non-Blueprint, via DIRECTION.md; provisional pending installed-app captures). Mirrors landed the same day in specchain `f958c47`, design-qa `89bfc8c`, agentic-ways-of-working `bd5c70f` (Codex closeout runs the reviewer as advisory), and dotfiles `66f1097` (working-style, toolbox routing, signal-frontend-designer v2.2, ux-ui-auditor v2.1). Status: promotion candidate until a second product's cold review is recorded on device captures. Freeze acknowledgment: the operator directed the codification ("build durable design principles, codify the correct approaches") after rejecting a memo-only pass; no fleet run preceded the first commit. Consumer-sync: additive; `design_intent` defaults to unset, which the reviewers treat as "declare it" (WARN), so existing consumers see one new warning and no failure until they choose an intent. Minder restamped at `8cf2cf0` and `58f1d7c`; rally-hq, blog, film-room deliberately not restamped until promotion. Verification: `test:core` green at `d20cb83` (design-principles 73 assertions, screen-composition selftest, stamper smoke).

