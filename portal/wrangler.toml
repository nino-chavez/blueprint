name = "blueprint-methodology"
compatibility_date = "2026-03-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "."

# Project: Blueprint methodology dogfood (Blueprint applied to itself).
# Custom domain: methodology.ninochavez.co
# The methodology-source repo at ~/Workspace/dev/wip/blueprint distributes
# this portal as the canonical Pattern B chrome; this deploy is the
# dogfood (blueprint-redesign initiative) showing what that distribution
# looks like in stakeholder-review form.
