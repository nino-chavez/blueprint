# Blueprint methodology source — and its own first consumer

**Repo role: I am the Blueprint methodology source AND its reference-implementation consumer.** This repo holds two things that share one tree (folded 2026-06-05):

1. **The methodology distribution** (what external consumers pull): `METHODOLOGY.md`, `docs/`, and `template/` — the stamper substrate + reviewer agents + hooks shipped to consumers.
2. **Blueprint applied to itself** (the self-application / reference portal): a root `blueprint.yml`, `research/`, `decisions/`, `apps/portal/`, `packages/`, `tools/archaeology/`, `START-HERE.md` — the productization initiative, run *as* a Blueprint initiative, in this repo. The portal at `apps/portal/` is the live Pattern A reference implementation, demo, and onboarding knowledge base for new consumers.

The dual role is deliberate: a methodology proves itself by being its own first consumer — the compiler that compiles itself. Verify `pwd` ends in `tools/blueprint` before any commit.

## The boundary that matters is a DIRECTORY boundary, not a repo boundary

The rule that keeps the methodology reusable is NOT "no consumer artifacts in this repo" (the root IS a consumer now). It is:

- **`template/` is the stampable substrate — keep it clean.** External consumers stamp from `template/` and ONLY `template/`: `template/tools/blueprint-init/stamp.mjs` walks the passed `src` and never reads the repo root; its `mechanicalCheck` is scoped to the stamped target. The self-application's `blueprint.yml`, `research/`, `decisions/`, and `apps/portal/` at the root **never reach what a consumer stamps**, so they cannot pollute it. A change to `template/` is a methodology change; a change to the root is self-application work.
- **If you find yourself editing `template/` to make the self-application's portal build, stop — that's the leak.** Fix it at the root (`apps/portal/`, `packages/`). The portal under `apps/` is the *instance*; `template/apps/portal/` is the *boilerplate*. Keep them distinct.

## Operating invariants

- **Shell is throwaway; artifacts are forever**: "artifacts" here means BOTH the methodology (`METHODOLOGY.md`, `docs/`, `template/`) AND the self-application's evidence (`research/`, `decisions/`, `blueprint.yml`, committed `*.md` rationale). Scaffolding (`apps/portal/dist`, `node_modules`, `.wrangler`, `.astro`) is throwaway — regenerate it. Treat both artifact columns with the care a consumer gives its `research/`.
- **Methodology freeze during consumer migration**: when an EXTERNAL consumer is mid-migration picking up a methodology update, no `template/` edits land without an explicit operator waiver ("patch upstream now, consumer keeps local fixes"; see wave 5 commit `53fe1f0`). The in-repo self-application is pinned to `methodology_version: self` and does NOT trip this. The freeze is about concurrent *editing*, not repo topology — concurrent SESSIONS editing this repo still use worktrees (the `worktree-guard` hook enforces it; a solo session in main is never blocked).

Operator check before editing `template/`: confirm which external consumer initiatives are in flight and whether the edit has waiver authority. Editing the root self-application never trips the freeze. Other live external consumers: `apps/rally-hq/`, `apps/website-nc-v3/`, `apps/blog/blueprint/`, `wip/subs-initiative/` (all at rest as of the fold).

## Wave log

Methodology changes ship in waves (freeze-rule acknowledgment + consumer-sync command per wave). The full log moved to **`WAVE-LOG.md`** (2026-05-29) to keep this charter scannable — filter it with `node template/tools/wave-digest/digest.mjs --source=WAVE-LOG.md --since=N` / `--keyword=<regex>`.

**Current state:** wave 51 (`132eb59`) — **Mom Test validation bridge** — demand claims in generated assets carry evidence classes (repo-grounded / stakeholder-given / agent-hypothesis); `/blueprint-docs` emits a `validation-script.md` companion (riskiest assumptions, three scary questions, past-specific question plans, commitment asks) so the methodology hands operators the generated→real validation bridge instead of implying generated research is demand evidence; `/blueprint-triage` weighs feedback by what stakeholders GAVE (money/reputation/time/none — kudos never validates); fact-check gains the hypothetical-demand-claim sub-reviewer, research-completeness gains `JTBD_INVENTED_DESIRE`. Canonical: `template/docs/methodology/mom-test-validation-pattern.md`. Spec-side this wave; mechanical halves wait for a live initiative (second-instance rule). Prior: wave 50 (`b503ca0`) — **coherence + doc-currency gates (the Glass wave)** — two new fleet reviewers prompted by an external comparable (Ramp's Glass team independently rediscovering the first principle; `research/03-comparable-glass-ramp.md`): **`defrag-reviewer`** (mechanical fragmentation census — duplicate exports, component-stem clusters, identical function bodies, orphan modules — judged into CONSOLIDATE/KEEP/RENAME/DELETE plans; a cadence pass that NEVER blocks, because blocking on drift punishes in-flight work) and **`doc-currency-reviewer`** (broken internal links BLOCK, CLI mentions verified against the bin, unresolved path citations agent-verified — prescriptive-vs-rotten is semantic; wired into `doctor` as check 7, so the self-app gates on its own doc accuracy every run, automating the 2026-06 manual 184-doc sweep's mechanical class). `docs/variant-selection.md` gains the **entropy-wall litmus** (outward growth, seam bugs, unreviewable PRs, lagging docs → adopt midstream/brownfield; Tier 0 pre-empts the wall). Reviewer fleet now 14; doctor 7 checks, green. Deferred on the second-instance rule: Dojo-style git-invisible contribution UI. Prior: wave 49 (`e9f5f04`+`dc153f1`+`ba842dc`) — **Blueprint + hive team onboarding/adoption kit** — the methodology now answers "a real team adopts this," not just "the author dogfoods it." Surfaced by the first real engagement (T. adopting Blueprint + hive for a client the commerce platform build — a *team*, so the coordination substrate is in scope). Closes the *adoption* surface of the competitive-positioning weaknesses (full hive = 1 consumer; not productized; `bootstrap.sh` a placeholder that exits 1; identity = a spoofable shared bearer). **Three crawl→walk→run docs**: `docs/team-roles-and-conventions.md` (the three zero-infra conventions + the territory-DRI / cadence-synthesis / spec-via-proposal model + the litmus for escalating to the full substrate), `docs/hive-identity-gap.md` (honest shared-bearer disclosure + the per-engagement decision: trust-within-known-team + risk-register for ≤3, harden to per-session JWT before a 2nd team/external — carries its why-not-canonical sentence), `template/tools/hive/ONBOARDING.md` (productized; the three first-week trip-ups up top). **The keystone**: `template/tools/hive/bootstrap.mjs` + `blueprint hive setup --slug=<x>` — the manual 18-step bootstrap collapses into one command provisioning a fresh Hive (CF D1 + Worker + Pages) from a vendored ai-hive kit. Dependency-free, matching the `tools/lib/` libs: a pure declarative PLANNER (offline-testable) split from a side-effecting EXECUTOR, never throws, `--self-test` (25 assertions). terraform-plan discipline (dry-run PLAN default, `--execute` applies); idempotent (D1/Pages reuse, patches re-match); integrate-not-absorb (operates on `--hive-dir`, vendors nothing); bearer via `crypto.randomBytes`, secrets via stdin. The 3 inherently-manual steps (wrangler login — *checked* as a precondition; CF API-token; `hive_create_project`) live in productized `template/tools/hive/BOOTSTRAP.md`. `bin/blueprint.mjs` wires `hive setup` (same home-decoupling as review/cost/fleet/doctor). Verified: plan renders against the real subs-initiative `.hive` kit; all 6 lib self-tests + the hive bootstrap self-test green. Full hive RBAC/identity hardening + multi-operator chaos test deferred until a 2nd team/external joins. Prior: wave 48 — **bespoke-with-ADR portal escape automated in `doctor`** (the wave-46 second-instance trigger). The fold's product-site rebuild made the self-app's `apps/portal/` bespoke (dropped Pattern A's audience switcher), so `doctor` BLOCKED it on Pattern A conformance — the methodology failing its own gate. Fixed by automating, not reverting: `doctor` reads `portal_pattern`, and for `bespoke` runs neither conformance reviewer — it requires a divergence ADR in `decisions/` (present → pass, absent → fail; `template/tools/lib/doctor.mjs` `findDivergenceAdr`). Self-app declared `portal_pattern: bespoke` + NEW `decisions/02-portal-bespoke-product-site.md` → **`doctor` is GREEN (exit 0)**. Does NOT mint Pattern C (product homepage ≠ ops console — a 3rd frozen pattern still waits for a 2nd instance of the SAME archetype). Prior: wave 47 — **executable reviewer fleet build-out**: 12 of 15 reviewers now run outside Claude Code (was 2). 10 new dependency-free `.mjs` for every reviewer with a real mechanical check (ADR-0002 contract, guarded self-tests, never-throws, honest scoping — hybrids emit "agent-verified" for judgment criteria, never fake them); 3 left spec-only by design (`doc-quality-auditor`, `fact-check-loop-reviewer`, `research-reference-grader` — LLM judgment, a `.mjs` would manufacture the gate). `blueprint review --list` shows all 12; `doctor` → 12 reviewers load+validate. wave 46 (`9493cd1`) — bespoke-with-ADR escape / candidate Pattern C: a matrix lane + a "neither pattern fits" subsection in `docs/portal-and-tier-ladder.md`, the Tier-0 archetype-misfit loophole closed, the bespoke-with-ADR lane given teeth in the reviewer roster + selection rule, plus a dogfood amendment. Also: the Track E enablement front door shipped (`/learn` hub + getting-started + end-to-end tutorial + `docs/README.md` Diátaxis index) + LICENSE (MIT) added — closing the scope audit's two real gaps. wave 45 (`7bf554f`) **the fold** — `wip/blueprint-platform` folded into THIS repo as the in-repo self-application (root `blueprint.yml`, `research/`, `decisions/`, `apps/portal/`, `tools/archaeology/`); source≠consumer is a **DIRECTORY boundary** (`template/` is the stampable substrate external consumers pull), not a repo boundary; `apps/portal/` is now a bespoke product site (live at blueprint-platform.pages.dev, deploying from THIS repo via `.github/workflows/deploy-portal.yml`); the methodology repo is now **PUBLIC**. wave 44 (`582d04a`) best-practices hardening — reviewer over-engineering guard + a `/goal` Stage-4 exit gate. wave 43 (rationale promotion — the 5 productization ADRs promoted to `docs/decisions/ADR-0003..0007` + input analysis mirrored to NEW `docs/productization/`). wave 42 (`225d3ab`) `blueprint doctor` conformance capstone, build-order step 12 — the **build order is COMPLETE, every CLI command real**; doctor gates on real runtime verification (loads config + every reviewer + runs portal conformance). Recent: wave 41 (`96f60db`) org-authored reviewer discovery; wave 40 (`42369ab`) amendment/RFC triage; wave 39 (`ab4e9e2`) access + governance (operator action to bind: `docs/governance/apply-ruleset.sh`); wave 38 (`3d3904b`) `blueprint upgrade`; wave 37 (`1bfdc19`) consumer registry + `blueprint fleet`; wave 36 (`d95dadd`) cost telemetry + gate; wave 35 (`eb250e5`) cost dial. **Build order 0–13 COMPLETE** across tracks A–E. **The CLI is PUBLISHED** — `@nino-chavez-labs/blueprint-cli@0.1.0` is live on npm (MIT, public repo); `npx @nino-chavez-labs/blueprint-cli init` is the public scaffolder and `init/review/cost/fleet/upgrade/doctor` are all real (six dependency-free libs under `template/tools/lib/` with self-tests: cost-dial/telemetry/consumers-registry/upgrade/reviewer-registry/doctor). The `main-protection` ruleset is **bound + active** (2026-06-05; id 17343422 — PR + 1 code-owner review + linear history, admin-bypass during the solo phase). No operator-gated activation remains. blueprint-redesign at rest (`cc4f62f`); rally-hq at rest (`1aa3c35`); **blueprint-platform FOLDED in-repo** (no longer a separate consumer — it IS this repo's self-application). Pattern A consumers re-stamp to inherit the generic harness + author a `blueprint.yml portal:` block (Tier-0 default builds clean without one). Methodology freeze lifted. Pattern B consumer-sync (waves 8-16): `--mode=audit-chrome` to classify divergences, then `--mode=restamp-chrome --accept-overwrite=<LAG-files>` to pull forward safely.

## Methodology canonical docs

- `METHODOLOGY.md` — first-principles, agent-struggle-is-missing-capability
- `docs/variant-selection.md` — greenfield / midstream / brownfield decision tree
- `docs/portal-and-tier-ladder.md` — Pattern A / B portal + tier 0/1/2 ladder
- `docs/methodology/methodology-amendments-convention.md` — how consumers record candidate-for-promotion gaps
- `docs/productization/README.md` — **why Blueprint became a team-adoptable platform + how it maps to the code** (the goal, the gap scorecard, the six tracks, ADR→feature→wave map, mirrored research). The platform ADRs themselves live at `docs/decisions/ADR-0003..0007` (promoted from the `blueprint-platform` dogfood, wave 43).

## See also

- `template/CLAUDE.md` — the CLAUDE.md shipped to consumers (the inverse of this file)
