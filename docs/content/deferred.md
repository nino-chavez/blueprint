# Deferred feedback items

Acknowledged, not in the current slice. Each entry: excerpt, source,
deferral rationale, suggested timing.

## Bring-your-own-loop crosswalk

- **Excerpt**: R.'s hand-rolled loop — create-spec → adversarial review-spec
  ("build ready" flag, "contradictions") → implement-spec →
  review-implementation — maps near one-to-one onto Blueprint's stages, gates,
  and reviewer fleet. ("It's easy for everyone to have their own spec-driven
  process.")
- **Source**: `feedback/2026-06-10-eng-lead-thread.md` (item 3).
- **What it would be**: a docs page mapping a personal spec loop onto
  Blueprint primitives — the adoption path for practitioners with an incumbent
  loop. Positions Blueprint as primitives to standardize on, not a kitchen
  replacement; directly hedges assumption A6
  (`docs/content/validation-script.md`).
- **Why deferred**: derived prescription, not a stakeholder ask; `gave: none`
  defaults scope-adds to deferred per the triage weighting rules. One
  practitioner's loop is one instance.
- **Timing**: when a second practitioner with a personal loop engages (the
  second-instance rule applies to docs too) — or pull forward if the A6
  commitment ask lands (a working session mapping R.'s loop onto Blueprint).

## "Contradiction" as the named failure mode

- **Excerpt**: "I really like to highlight 'contradictions' (any time an idea
  conflicts with reality, but model still goes with it because the user asked
  for it) because I find this is AI's #1 failure mode."
- **Source**: `feedback/2026-06-10-eng-lead-thread.md` (item 3).
- **What it would be**: adopt "contradiction" as persona-resonant language in
  fact-check loop docs and portal copy. The fact-check loop + adversarial
  reviewers already gate exactly this class; the docs never name it the way a
  practitioner does.
- **Why deferred**: copy polish, not a gap; no scripted assumption rides on it.
- **Timing**: next docs pass or next wave touching the fact-check loop.
